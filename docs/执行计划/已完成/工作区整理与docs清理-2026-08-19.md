---
status: completed
updated: 2026-08-19
owner: workspace-governance
---

# 工作区整理与 docs 清理（2026-08-19）

## 背景

对工作区整体审查后发现：仓库处于未完成的合并冲突状态（`.coderabbit.yaml` 本地已删、远端 `e0077bba` 仅追加一行自述"验证后删除"的临时注释）；`frontend-admin-v3` 与 `frontend-user-v4-console` 各残留一整套 tdesign-vue-next-starter 模板文件（`.github` 全套 workflows/issue 模板、`.cnb`、`CHANGELOG.md`、`PUBLISH.md`、`docs/starter.png`）；`shared/` 存在 12 个零引用死文件及 barrel/`package.json` 中的对应映射；www 端入库约 2.1MB 一次性 Lighthouse 审计工件与一次性 PHP 生成脚本；`docs/` 存在日志归档主题状态漂移（计划已转技术债而设计/规格仍标 active）、4 项复核逾期、迁移台账 4 文件未被索引收录、执行计划归档规则与近期删除实践冲突等问题。仓库根还压着 `caiwu.zip`（约 482MB）与 `ZJMF-CBAP-10.4.6/` 参考副本两个被 ignore 的本地大文件。

## 范围与验收

- [x] 合并冲突解决：`.coderabbit.yaml` 保持本地删除决议（合并提交待用户确认后完成）。
- [x] 本地大文件移出仓库目录至 `E:\caiwu-archive\`。
- [x] admin-v3 与 v4-console 的 starter 模板遗留清理（22 + 21 个文件）。
- [x] v4-console `.gitignore` 的 `*.png` 全局忽略与已跟踪登录背景/Logo 的冲突修复。
- [x] www 端 Lighthouse 工件（4 个 html/json）、审计 html 与一次性 PHP 脚本清理，保留审计结论 md。
- [x] shared/ 12 个零引用死文件清理，`user-v3/components/index.ts` barrel 与 `package.json` exports 同步。
- [x] `npm run typecheck:shared && npm run test:shared` 与三端 `npm run build` 全部通过。
- [x] docs 状态对齐：日志归档主题设计文档与产品规格 active → tech-debt，catalog 同步并重设逾期 review_by。
- [x] docs 治理修订：执行计划 README 归档豁免条款、`docs/README.md` 状态语义补 `template`、产品规格 README 前瞻描述修正。
- [x] docs 小修：删迁移台账 4 文件并同步 catalog、BACKEND.md 本机绝对路径修正、报表中心方案冻结标注、逾期 review_by 顺延、已完成计划验收勾选补齐。
- [x] `npm run docs:check` 通过。

## 实施步骤

1. `git rm .coderabbit.yaml` 解决 DU 冲突；用户确认后完成合并提交。
2. 移动 `caiwu.zip`、`ZJMF-CBAP-10.4.6/` 到仓库外 `E:\caiwu-archive\`。
3. 删除两个 TDesign 端 starter 遗留（`.github/`、`.cnb*`、`CHANGELOG.md`、`PUBLISH.md`、`docs/starter.png`；admin-v3 另删 `playwright.local-reuse.config.ts`）；v4-console `.gitignore` 追加 `!src/assets/*.png`。
4. 删 www `docs/perf-audit/lighthouse/` 4 个产物与 `www-performance-audit.html`、`scripts/generate_hero_point_cloud_shapes.php`；`implementation-2026-08-15.md` 补注产物清理说明。
5. 删 shared 死文件：`components/`（StatusTag.vue、TicketChatPanel.vue）、`user-v3/components/` 7 个未用组件、`runtime/monitor.ts`、`utils/format.ts`、`vite-plugin-version.ts`；同步 barrel 与 `package.json` exports。
6. 验证 shared 与三端。
7. docs 状态对齐、治理条款修订、小修与 catalog 同步。
8. `npm run docs:check` 验证，计划完结移入已完成。

## 风险与回滚

- 所有删除文件均经 `git grep` 全仓库验证零引用，shared 与三端构建、测试全部通过；任何删除可经 Git 历史回溯恢复。
- 合并提交不推送，本地可 `git reset` 撤销。
- 大文件移动仅为本地文件系统操作，可随时移回。
- `.gitignore` 否定规则已用 `git check-ignore -v` 验证：`src/assets/*.png` 不再被忽略，其余 png 仍被忽略。

## 进度

- [x] 阶段 0-2：冲突解决、大文件移出、三端与 shared 清理及验证完成。
- [x] 阶段 3：docs 状态对齐与治理修订完成。
- [x] 阶段 4：docs:check 验证通过；提交待用户确认后完成。

## 决策日志

| 日期       | 决策                                                        | 原因                                                                                 |
| ---------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 2026-08-19 | 合并冲突保持删除 `.coderabbit.yaml`                         | 远端 `e0077bba` 仅为临时验证注释（自述验证后删除），本地 `6e49d8a1` 已确立删除决议。 |
| 2026-08-19 | shared 双轨收敛、userMessage 上收、theme.css 迁移等另立计划 | 影响 36+ 处引用，属代码重构而非清理；经用户确认拆分为独立执行计划。                  |
| 2026-08-19 | Lighthouse 工件与迁移台账删除、审计结论保留                 | 一次性工件，Git 历史可回溯；经用户确认。                                             |
| 2026-08-19 | needs-review 6 篇深度复核与复核期限错峰不在本次             | 需逐篇对照运行代码，工作量数倍于清理本身；列入后续任务。                             |

## 后续任务（技术债候选，不在本计划范围）

- shared 扁平 `.js` 与模块化双轨收敛（36 处引用）、`index.d.ts` 与 `index.ts` 同步、statusConfig 迁移 TS。
- admin-v3 `src/utils/userMessage.ts` 与 shared `runtime/http/userMessage.ts` 双实现上收。
- 根目录 `theme.css` 迁入 `shared/`（admin-v3 main.ts 与 v4-console less 两处引用）。
- www `pages/website` 与 `views/website` 冗余薄包装层合并。
- v4-console `invoice-detail` / `invoice-detail-view` 近重名目录整合。
- www 与 v4-console `public/img/os/` 19 个 OS 图标去重。
- needs-review 6 篇集中复核；2026-08-21 集中到期的 14+ 项 review_by 错峰。
