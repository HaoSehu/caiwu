# PHP 财务系统启动指南

## 环境要求

**必需组件：**
- PHP 8.2+
- Composer
- MySQL 8.0+
- Node.js 18+（当前版本：v24.14.0 ✅）

## 安装步骤

### 1. PHP 环境安装
**推荐方式：**

**XAMPP（优先推荐）**
1. 下载：https://www.apachefriends.org/download.html
2. 安装 PHP 8.2+ 版本
3. 将 `C:\xampp\php` 添加到系统 PATH 环境变量

**PhpStudy（国内推荐）**
1. 下载：https://www.xp.cn/
2. 安装后自动配置

### 2. Composer 安装
1. 下载安装：https://getcomposer.org/Composer-Setup.exe
2. 安装完成后重启终端

### 3. MySQL 安装
参考项目中的 `MySQL安装指南.md`

## 项目启动

### 后端启动（Laravel）

```bash
# 进入目录
cd backend

# 安装依赖
composer install

# 生成密钥
php artisan key:generate

# 配置数据库（编辑 .env 文件）
# DB_DATABASE=idc_finance
# DB_USERNAME=root
# DB_PASSWORD=你的密码

# 创建数据库并迁移
php artisan migrate
```

**开发环境启动：**
```bash
cd backend
php artisan app:serve
```

说明：`app:serve` 默认只启动以下进程：
- Laravel HTTP
- VNC Relay
- 队列 Worker

计划任务与 `app:serve` 分开运行，不默认内置调度。

如果你本地确实想临时一起带上调度，再手动执行：
```bash
php artisan app:serve --with-schedule
```

**宝塔部署：**
网站由宝塔站点直接指向 `backend/public`，不要把 `app:serve` 作为生产入口。

宝塔只需要添加一条计划任务，每分钟执行一次：
```bash
cd /你的项目/backend
php artisan schedule:run
```

说明：项目已将队列积压消费并入 Laravel 调度，这一条计划任务会同时驱动：
- 系统自动化任务
- 支付后的开通队列
- 推荐返佣等异步任务

代价：异步任务不是常驻实时消费，通常会有 0 到 60 秒延迟。

### 前端启动

默认推荐在仓库根目录通过 workspace 命令启动：

```bash
npm run dev:client
npm run dev:admin
```

也保留子项目内命令，兼容现有习惯。

**用户端（端口 5173）**
```bash
cd frontend-client
npm run dev
# 访问：http://127.0.0.1:5173
```

**管理端（端口 5174）**
```bash
cd frontend-admin
npm run dev
# 访问：http://127.0.0.1:5174
```

### 前端构建与校验

推荐在仓库根目录统一执行：

```bash
# 双前端构建
npm run build:frontends

# 双前端类型检查
npm run typecheck:frontends

# 双前端测试
npm run test:frontends

# 双前端完整校验
npm run verify:frontends
```

如果只校验单端，也可以继续在子项目目录内执行：

```bash
cd frontend-admin
npm run build
npm run typecheck
npm run verify:refactor

cd frontend-client
npm run build
npm run typecheck
npm run verify:refactor
```

## 环境验证

运行以下命令检查：
```bash
php -v
composer -V
node -v
mysql --version
```

## 常见问题

**1. Composer 安装慢**
```bash
composer config -g repo.packagist composer https://mirrors.aliyun.com/composer/
```

**2. npm 安装慢**
```bash
npm config set registry https://registry.npmmirror.com
```

**3. 数据库连接失败**
- 确认 MySQL 服务已启动
- 检查 `.env` 配置
- 确认数据库 `idc_finance` 已创建

## 下一步

环境准备完成后，运行 `php -v && composer -V` 验证。

确认无误后告诉我，我将协助你完成项目初始化。




cd backend
php artisan app:serve




cd frontend-client
npm run dev



cd frontend-admin
npm run dev







cd backend
php artisan migrate --force
php artisan invoices:backfill-from-orders --dry-run   # 先预览
php artisan invoices:backfill-from-orders              # 实际回填

php artisan migrate --force && php artisan invoices:backfill-from-orders


