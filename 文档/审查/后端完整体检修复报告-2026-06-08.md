# 后端完整体检修复报告

- 日期：2026-06-08
- 范围：Laravel 12 后端、`frontend-admin-v3`、`frontend-client`、项目文档与 caiwu skills
- 基线：`文档/审查/后端完整体检报告-2026-06-08.md`
- 结论：P0/P1 已按当前代码完成主要修复；数据库写入型回归测试受 `idc_test` 权限阻断，未执行数据库初始化、迁移或补跑历史迁移。

## 已修复清单

1. 公开 VNC token 风险
   - 公开 token 改为一次性消费，解析后换取短 TTL relay token。
   - 公开响应不返回 VNC `password`，日志只记录 hash/掩码和存在状态。
   - 证据：`backend/app/Services/ClientServiceConsole/ServiceVncService.php:159`、`:161`、`:240`

2. 退款、余额恢复和财务审计链
   - 支付宝原路退款、余额退款成功后同步账单退款投影。
   - 混合支付余额恢复不再删除 `balance_logs`，改为反向流水。
   - 管理员手动标记支付不再伪造真实第三方成功 Payment。
   - 证据：`backend/app/Services/Finance/PaymentService.php:1139`、`:1326`、`:1787`、`:1962`，`backend/app/Services/Order/OrderService.php:754`

3. 客户端账单白名单
   - 用户端账单详情改用 `clientDetail()`，不复用 admin detail。
   - 响应移除 `raw_status`、`payments.*.trade_no` 等内部字段。
   - 证据：`backend/app/Services/Finance/InvoiceService.php:329`，`backend/app/Http/Controllers/Client/InvoiceController.php:350`

4. 队列可靠性
   - 统一 `provision,referral,notification,coupon,default` 队列清单。
   - `RunScheduleTaskJob` 设置重试、超时、重叠锁和 failed 钩子。
   - `schedule`、手动 drain、`app:serve` 共用配置。
   - 证据：`backend/config/queue.php:6`，`backend/app/Jobs/RunScheduleTaskJob.php:52`、`:153`、`:170`，`backend/routes/console.php:281`，`backend/app/Console/Commands/ServeBackendStackCommand.php:183`

5. 回调签名与第三方错误
   - 实名回调升级为 HMAC-SHA256 + timestamp + nonce + replay cache。
   - 增加 `ProviderErrorMapper`、`IntegrationException`、`CallbackDispatcher`，第三方原文只进入脱敏日志。
   - 证据：`backend/app/Http/Middleware/VerifyCallbackSignature.php:22`、`:43`、`:51`、`:61`，`backend/app/Services/Integrations/Support/ProviderErrorMapper.php:7`，`backend/app/Services/Integrations/Callbacks/CallbackDispatcher.php:11`

6. 插件化与 provider-aware 缓存
   - 补 mail/storage provider contract 和 fake provider 骨架。
   - 魔方/主机面板缓存 key 改 provider-aware，并保留旧 key 双读、命中新 key 回写。
   - 证据：`backend/app/Contracts/Integrations/MailProviderInterface.php:7`，`backend/app/Contracts/Integrations/StorageProviderInterface.php:7`，`backend/app/Services/Integrations/Mail/FakeMailProvider.php:9`，`backend/app/Services/Integrations/Storage/FakeStorageProvider.php:9`，`backend/app/Services/ClientServiceConsole/ServiceTransformService.php:866`、`:896`，`backend/app/Services/ClientServiceConsole/ServiceDetailService.php:794`，`backend/app/Services/ClientServiceConsole/Concerns/HandlesClientServiceConsoleMonitoring.php:1387`

7. 简体中文体验
   - 后端监控、安全组、NAT、VNC 相关用户可见错误改为简体中文兜底。
   - 前端新增独立 `toUserMessage` 工具，禁止直接展示 axios/browser/第三方英文错误。
   - 证据：`frontend-client/src/utils/userMessage.ts:6`，`frontend-client/src/app/http/request.ts:14`、`:146`、`:180`，`frontend-admin-v3/src/utils/userMessage.ts:6`

8. 文档、skills 与 API 清单
   - 更新 API 格式规范、工程化治理方案、caiwu 主技能、后端审查技能和 governance reference。
   - API 清单通过脚本重新生成，接口数 308。
   - 证据：`文档/后端/API格式规范.md:381`、`:401`、`:411`，`文档/后端/后端规范化工程化治理方案.md:115`、`:191`、`:195`，`C:/Users/USER125536/.agents/skills/caiwu/SKILL.md:132`，`C:/Users/USER125536/.agents/skills/caiwu/code-review/SKILL.md:90`、`:117`、`:145`，`backend/scripts/export_api_inventory.php:75`

## 未修复清单及原因

1. 数据库写入型 Feature Test 未能完整验证。
   - 原因：`idc_test` 对当前 MySQL 用户不可写，测试报 `SQLSTATE[HY000] [1044] Access denied`。
   - 处理：未初始化、未迁移、未改库权限；保留为环境阻塞。

2. 第三方插件化仍是渐进状态。
   - 已落地统一异常、错误映射、回调 dispatcher、mail/storage contract 和 fake provider。
   - 真实 mail/storage provider、支付/实名/魔方完整 provider DTO 全量替换仍需后续阶段实施。

3. 全仓 `git diff --check` 仍有无关文件历史空白噪声。
   - 主要来自既有 `frontend-client/src/views/website/products/index.vue` 等非本轮目标文件。
   - 本轮关键文件定向 `git diff --check` 已通过。

## 测试和构建结果

- `cd backend && php artisan test --filter=ServiceVnc`：通过，6 passed，25 assertions。
- `cd backend && php artisan test --filter=BackendHealthFixRegressionTest`：通过，12 passed，1 skipped，35 assertions；跳过项为测试库不可写导致无法验证 GET settings 数据库断言。
- `cd backend && php artisan test --filter=ScheduleTaskExecutionCoverageTest`：通过，4 passed，59 assertions。
- `cd backend && php artisan test --filter=CallbackSignatureMiddlewareTest`：通过，4 passed，7 assertions。
- `cd backend && php artisan test --filter=Payment`：失败，17 failed，10 passed；失败均为 `idc_test` 写权限阻断，纯静态/单元支付检查已通过。
- `cd backend && php artisan test --filter=ClientInvoiceInvoiceOnlyRegressionTest`：失败，5 failed；均为 `idc_test` 写权限阻断。
- `cd frontend-admin-v3 && npm run build`：通过；存在依赖注释和大 chunk 警告。
- `cd frontend-client && npm run build`：通过；已消除 `toUserMessage` 循环重导出警告，仅剩依赖包注释警告。
- `php backend/scripts/export_api_inventory.php`：通过，生成 308 个接口。

## 数据库与敏感信息声明

- 数据库初始化：未执行。
- 数据库迁移：未执行。
- 历史迁移补跑：未执行。
- 新增迁移文件：仅创建 `backend/database/migrations/2026_06_08_000001_add_refund_projection_to_invoices_table.php`，未运行。
- 敏感信息：未读取、输出、复制 `.env`、secret、token、password、私钥等真实敏感值。

## 剩余风险

1. 需要给当前测试用户补 `idc_test` 写权限，或提供受控 sqlite/mysql 测试库后重跑数据库型 Feature Test。
2. 旧代码中仍可能存在历史乱码日志/提示，已优先修复本轮风险路径；建议后续做全量中文文案静态治理。
3. 插件化骨架已落地，但真实 provider 迁移需要按支付、实名、魔方、短信、邮件、对象存储分阶段推进，避免一次性破坏稳定链路。

## 后续插件化路线图

1. 支付：把创建、查询、关闭、退款、回调解析全部收敛到 gateway contract + DTO，`PaymentService` 只依赖 manager。
2. 实名：将 callback dispatcher 接入实名回调控制器，统一 HMAC/replay/审计和 provider 事件分发。
3. 魔方财务/魔方云：保持 `mofang_finance_api` 独立 provider key，继续补齐开通、续费、重启、同步 DTO 边界。
4. 邮件/对象存储：在现有 contract + fake provider 基础上补 registry、配置、真实 driver、错误映射和测试。
5. 审查门禁：把敏感字段、中文 message、第三方错误、VNC token、Payment/BalanceLog 不物理删除纳入固定 CI/静态扫描。

## 防漏审完整性自检

- 已读取并遵守当前代码优先、旧文档仅参考原则。
- 已覆盖 P0/P1：VNC token、退款/财务审计、设置只读、客户端账单、队列、回调、中文体验。
- 已覆盖 P2：插件化骨架、provider-aware 缓存、文档与 skills 更新。
- 已记录未覆盖项：数据库写权限阻断、真实 provider 迁移未完成、全仓无关空白噪声。
- 已输出证据行号、验证命令、失败项和剩余风险。
- 已确认未执行数据库初始化、迁移、历史迁移补跑。
