# 管理端（frontend-admin-v3）优化执行手册

> 配套：`admin端优化审查报告-2026-06-18.md`（发现清单与量化影响）
> 用途：供 **DeepSeek v4 Pro** 逐任务精确执行。每任务卡片化：目标、涉及文件、改前/改后代码、验证、风险、回滚。
> 工作目录：`c:\Users\Admin\Desktop\caiwu\frontend-admin-v3`（相对路径均相对此目录）
> 基线：Vue 3.5 + Vite 6.2 + TDesign Vue Next 1.19 + Pinia 3 + TypeScript 5.4

---

## 0. 执行规则（必须遵守）

1. **顺序**：阶段一 → 二 → 三 → 四 → 五；每阶段内按任务编号顺序。
2. **行号会漂移**：每个任务动手前先 `read_file` 校准实际行号（手册行号是审查快照）。
3. **每任务完成立即验证**：跑该任务"验证"小节命令；失败先修复再进下一个。
4. **最小改动**：只改任务列出文件；不顺手格式化、不重排无关代码、不加无关 docstring/类型注解。
5. **禁止改**：历史迁移、`文档/`、`backend/`、`shared/`（除非任务显式读取 shared）、`package-lock.json` 手编。
6. **依赖变更**：用 `npm install/uninstall`，不手编 `package.json`。
7. **本地验证统一**：`npm run build`（含 `vue-tsc --noEmit`）；多端任务另 `npm run test:e2e`；联调用 `127.0.0.1`。
8. **提交节奏**：每完成一阶段 `git commit` 一次；高风险任务（4.1/4.3/4.10/4.11）每页提交一次。
9. **遵守 `AGENTS.md`**：TDesign only，禁混 Element Plus；禁 admin-page-head/*-hero/*-head 顶部大卡片；请求走 `@/utils/request`；状态色复用 `@shared/statusConfig`。
10. **遇阻停下问**：手册与现有代码冲突、需产品决策、验证连续 2 次失败、发现手册未覆盖的新问题——任一情况停下来问，不扩大范围。
11. **阶段五可选**：未经确认不执行。
12. **进度追踪**：每完成一任务更新末尾"进度追踪表"对应 `[ ]` → `[x]`，并简报改动文件 + 验证结果。

---

## 阶段一：零风险纯函数收敛（预计减少 700+ 行）

### 任务 1.1：统一 formatDateTime / formatMoney / fieldValue

**状态**：`src/utils/format.ts` 已存在且内容正确（见下），无需重建。

**`src/utils/format.ts` 当前内容（确认无误后跳过创建）**：
```ts
export function formatDateTime(value?: unknown): string {
  if (!value && value !== 0) return '-';
  const date = new Date(value as string | number | Date);
  if (Number.isNaN(date.getTime())) return String(value);
  const pad = (item: number) => String(item).padStart(2, '0');
  return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())} ${pad(date.getHours())}:${pad(
    date.getMinutes(),
  )}:${pad(date.getSeconds())}`;
}
export function formatMoney(value?: unknown): string {
  return `¥${Number(value || 0).toFixed(2)}`;
}
export function fieldValue(value?: unknown): string {
  if (value === '' || value === undefined || value === null) return '-';
  return String(value);
}
```

**待清理本地副本的页面**（`formatDateTime` 17 处 / `formatMoney` 9 处 / `fieldValue` 13 处，去重后约 20 文件）：

| 文件 | 本地函数 |
|---|---|
| `src/pages/users/index.vue`（约 L268-282） | formatDateTime, formatMoney |
| `src/pages/users/detail/index.vue`（约 L1924-1935） | formatDateTime, formatMoney, fieldValue |
| `src/pages/tickets/index.vue`（约 L210） | formatDateTime |
| `src/pages/tickets/detail/index.vue`（约 L433） | formatDateTime |
| `src/pages/finance/orders/index.vue`（约 L313-322） | formatDateTime, formatMoney, fieldValue |
| `src/pages/finance/invoices/index.vue`（约 L430-439） | formatDateTime, formatMoney, fieldValue |
| `src/pages/finance/recharges/index.vue`（约 L371-380） | formatDateTime, formatMoney, fieldValue |
| `src/pages/services/index.vue`（约 L394-398） | formatDateTime, formatMoney, fieldValue |
| `src/pages/referral/index.vue`（约 L577-590） | formatDateTime, formatMoney, fieldValue |
| `src/pages/dashboard/base/index.vue`（约 L107） | formatDateTime |
| `src/pages/content/index.vue`（约 L477-482） | formatDateTime, fieldValue |
| `src/pages/member-levels/index.vue`（约 L286-299） | formatDateTime, formatMoney, fieldValue |
| `src/pages/logs/index.vue`（约 L907-1001） | formatDateTime, fieldValue |
| `src/pages/products/coupons/index.vue`（约 L841） | formatDateTime |
| `src/pages/products/coupon-campaigns/index.vue`（约 L666） | formatDateTime |
| `src/pages/users/components/VerificationPanel.vue`（约 L505） | formatDateTime |
| `src/pages/finance/orders/detail/index.vue`（约 L590-595） | formatDateTime, formatMoney, fieldValue |
| `src/pages/notifications/index.vue` | fieldValue |
| `src/pages/admin/system/staff/index.vue` | fieldValue |
| `src/pages/admin/system/roles/index.vue` | fieldValue |

**每个页面的改动步骤**：
1. `read_file` 该页确认本地函数实际行号与签名。
2. 删除本地 `function formatDateTime(...)` / `function formatMoney(...)` / `function fieldValue(...)` 整段。
3. 顶部 import 区追加（只 import 实际用到的）：
   ```ts
   import { formatDateTime, formatMoney, fieldValue } from '@/utils/format';
   ```
4. 模板调用处不变（函数名一致）。
5. `npm run build` 验证。

**注意签名差异**：本地版 `formatDateTime(value?: string)`、`formatMoney(value?: number|string)`、`fieldValue<T>(value): T|string`；`@/utils/format` 版统一为 `unknown` 入参、`string` 返回。模板内均为字符串插值，兼容；若某页 script 内把 `fieldValue(x)` 当 `T` 用（罕见），需调整为 `fieldValue(x) as T` —— 发现此类情况停下问。

**验证**：`npm run build` 通过。
**风险**：零（纯函数，签名兼容）。**回滚**：`git restore src/pages/`。

---

### 任务 1.2：把 errorMessage 提升到 `src/utils/userMessage.ts`

**目标**：消除 20+ 页面本地 `errorMessage` 副本。

**改 `src/utils/userMessage.ts`**：在文件末尾追加：
```ts
/**
 * 统一错误提示信息提取。优先用可信的中文后端消息，否则回退 fallback。
 * 所有页面应使用本函数，禁止本地重写 errorMessage。
 */
export function errorMessage(error: { message?: unknown } | unknown, fallback = '操作失败，请稍后重试'): string {
  const message = (error as { message?: unknown })?.message;
  return toUserMessage(message, fallback);
}
```
> 注意 fallback 默认值是 `'操作失败，请稍后重试'`（"重试"，非"重计"——之前手册有笔误）。

**待清理本地副本的 20 个页面**：
```
src/pages/users/detail/index.vue              约 L1830
src/pages/tickets/index.vue                   约 L218
src/pages/tickets/detail/index.vue            约 L441
src/pages/member-levels/index.vue             约 L311
src/pages/settings/index.vue                  约 L899
src/pages/logs/index.vue                      约 L1001
src/pages/services/index.vue                  约 L407
src/pages/finance/recharges/index.vue         约 L392
src/pages/finance/orders/index.vue            约 L334
src/pages/finance/invoices/index.vue          约 L451
src/pages/finance/orders/detail/index.vue     约 L611
src/pages/referral/index.vue                  约 L602
src/pages/content/index.vue                   约 L494
src/pages/admin/system/staff/index.vue        约 L429
src/pages/admin/system/roles/index.vue        约 L492
src/pages/products/specs/index.vue            约 L355
src/pages/products/cpu-models/index.vue       约 L505
src/pages/products/coupons/index.vue          约 L849
src/pages/notifications/index.vue             约 L677
src/pages/finance/new-customers/index.vue     约 L114
```

**每页改动**：
1. `read_file` 确认本地 `function errorMessage(...)` 行号。
2. 删除该函数整段。
3. 顶部 import 追加 `import { errorMessage } from '@/utils/userMessage';`（若已 import `toUserMessage` 则合并到一行）。
4. 调用处不变。
5. `npm run build`。

**验证**：`npm run build` 通过。
**风险**：零。**回滚**：`git restore`。

---

### 任务 1.3：清理 TDesign Starter 死代码

**目标**：删除 0 引用的 Starter 遗留。

**操作前必做 grep 确认 0 引用**：
```
grep_search "useNotificationStore|getMsgData|notification.ts"
grep_search "CONTRACT_STATUS|CONTRACT_TYPES|CONTRACT_PAYMENT_TYPES|NOTIFICATION_TYPES"
grep_search "components/trend|from.*Trend"
```
若 grep 命中业务引用 → 停下问，不删。

**确认 0 引用后执行**：

1. **删除 `src/store/modules/notification.ts` 整个文件**。
   - 读 `src/store/index.ts`，若 import 了该 module 则删掉 import 行与注册。
2. **清理 `src/constants/index.ts`**：删除 `CONTRACT_STATUS`、`CONTRACT_TYPES`、`CONTRACT_PAYMENT_TYPES`、`NOTIFICATION_TYPES` 四个导出块，仅保留 `ContentTypeEnum`。
3. **删除 `src/components/trend/` 整个目录**（`index.vue` + 同目录 `.less`）。

**验证**：`npm run build` 通过。
**风险**：低（删前已 grep）。**回滚**：`git restore`。

---

## 阶段二：性能与内存（首屏 -70KB）

### 任务 2.1：lodash → lodash-es，拆 chunk

**目标**：lodash 从首屏主 chunk 移除，按需 tree-shake。

**步骤**：
1. 安装/卸载：
   ```bash
   npm install lodash-es
   npm install -D @types/lodash-es
   npm uninstall lodash @types/lodash
   ```
2. 全局替换 import 路径：
   - `grep_search "from 'lodash'"` 列出全部 12 处。
   - 逐文件 `replace_string_in_file` 把 `from 'lodash'` → `from 'lodash-es'`。
   - 只改路径，不改具名导入名（`cloneDeep`/`debounce`/`isBoolean` 等 lodash-es 完全兼容）。
   - 已知 12 处：`src/utils/request/Axios.ts`、`src/layouts/components/SideNav.vue`、`src/router/index.ts`、`src/layouts/components/FrameContent.vue`、`src/layouts/components/Content.vue`、其余 7 处以 grep 为准。
3. 改 `vite.config.ts` manualChunks（在 `vendor-i18n` 分支后、`return undefined` 前插入）：
   ```ts
     if (normalized.includes('/lodash-es/')) {
       return 'vendor-lodash';
     }

     return undefined;
   ```
4. 改 `vite.config.ts` optimizeDeps（约 L265）：`include: ['lodash']` → `include: ['lodash-es']`。

**验证**：`npm run build` 通过；检查 `dist/assets/` 出现独立 `vendor-lodash-*.js`，首屏主 chunk 体积下降约 70KB。
**风险**：低-中。若报 "Module not found"，grep 是否还有遗漏 `from 'lodash'`。**回滚**：`git restore` + `npm install lodash @types/lodash && npm uninstall lodash-es @types/lodash-es`。

---

### 任务 2.2：移除 `@types/echarts`

**步骤**：
```bash
npm uninstall @types/echarts
```
`import type { EChartsOption } from 'echarts'`（5.x 自带类型）保持不变。

**验证**：`npm run build` 通过（类型不再冲突）。
**风险**：低。**回滚**：`npm install -D @types/echarts@^4.9.21`。

---

### 任务 2.3：修复 `setting.vue` 事件监听器泄漏

**文件**：`src/layouts/setting.vue`（约 L164-168）

**改前**：
```ts
onMounted(() => {
  document.querySelector('.dynamic-color-btn').addEventListener('click', () => {
    isColoPickerDisplay.value = true;
  });
});
```

**改后**：
```ts
const handleDynamicColorClick = () => {
  isColoPickerDisplay.value = true;
};

onMounted(() => {
  document.querySelector('.dynamic-color-btn')?.addEventListener('click', handleDynamicColorClick);
});

onBeforeUnmount(() => {
  document.querySelector('.dynamic-color-btn')?.removeEventListener('click', handleDynamicColorClick);
});
```
顶部 import 追加 `onBeforeUnmount`（合并到已有 vue import 行）。

**验证**：`npm run build` + 开关设置抽屉 5 次，DevTools Memory 无 listener 累积。
**风险**：零。**回滚**：`git restore`。

---

### 任务 2.4：tabs-router 持久化收敛

**文件**：`src/store/modules/tabs-router.ts`（末尾 `persist: true`，约 L83）

**改前**：
```ts
  persist: true,
});
```

**改后**：
```ts
  persist: {
    pick: ['tabRouterList'],
    serializer: {
      serialize: (state) => {
        const list = (state.tabRouterList || []).map((route) => ({
          path: route.path,
          name: route.name,
          isAlive: route.isAlive,
          isHome: route.isHome,
          routeIdx: route.routeIdx,
        }));
        return JSON.stringify({ tabRouterList: list });
      },
      deserialize: (raw) => JSON.parse(raw),
    },
  },
});
```

> 若刷新后某页依赖 `route.title` / `route.meta` 报错，把 `title: route.title` 加入 pick（双语 title 体积可控）。

**验证**：`npm run build` + 开多 tab → 刷新 → tab 列表恢复，localStorage `tabs-router` key 体积明显减小。
**风险**：中（tab 恢复逻辑）。**回滚**：`git restore` + 清浏览器 localStorage 该 key。

---

### 任务 2.5：keep-alive 收敛（重页面不保活）

**方案 A（推荐）**：路由 meta 加 `keepAlive: false`。

涉及路由：
- `src/router/modules/admin/dashboard.ts`（dashboard 路由项）
- `src/router/modules/admin/finance.ts`（orders / recharges 列表路由项）
- `src/router/modules/admin/system.ts`（logs 路由项）
- `src/router/modules/admin/user.ts`（users 列表路由项）

每路由项 `meta` 内追加 `keepAlive: false`：
```ts
meta: { title: '仪表盘', keepAlive: false },
```

**方案 B（可选，需你确认）**：dashboard 页 `onDeactivated` 释放 echarts 实例。读 `src/pages/dashboard/base/index.vue` 找到所有 `echarts.init` 返回的 ref，在 `onDeactivated` 调 `dispose()`，`onActivated` 重新 `loadDashboard()`。**此方案改变保活体验（切回会重新加载），动手前问我。**

**验证**：`npm run build` + 开多 tab 后 DevTools Memory 观察 dashboard 切走后内存下降。
**风险**：中（保活体验变化）。**回滚**：删除新增 `keepAlive: false`。

---

### 任务 2.6：登录背景 PNG → WebP

**文件**：`src/assets/assets-login-bg-white.png`、`assets-login-bg-black.png`、`assets-tencent-logo.png` + `src/pages/login/index.vue`

**步骤**：
1. 转 WebP（quality 80，同尺寸）。若有 sharp：
   ```bash
   npx sharp-cli -i src/assets/assets-login-bg-white.png -o src/assets/assets-login-bg-white.webp
   npx sharp-cli -i src/assets/assets-login-bg-black.png -o src/assets/assets-login-bg-black.webp
   npx sharp-cli -i src/assets/assets-tencent-logo.png -o src/assets/assets-tencent-logo.webp
   ```
   若 sharp 不可用，用 https://squoosh.app 手动转换后放入 `src/assets/`。
2. 读 `src/pages/login/index.vue` 找引用处：
   - 若是 `<img>`，改 `<picture>` 降级：
     ```html
     <picture>
       <source srcset="@/assets/assets-login-bg-white.webp" type="image/webp" />
       <img src="@/assets/assets-login-bg-white.png" alt="登录背景" />
     </picture>
     ```
   - 若是 CSS `background-image`，直接换 webp 路径（PNG fallback 需 JS 检测，简单做法是只换 webp）。

**验证**：`npm run build` + 浏览器 Network 看登录页图片体积下降 ≥50%。
**风险**：低。**回滚**：`git restore`。

---

### 任务 2.7：`hooks/index.ts` addEventListener 移进 onMounted

**文件**：`src/hooks/index.ts`（约 L22-30）

**改前**：
```ts
  onMounted(() => {
    if (!chartContainer) {
      chartContainer = document.getElementById(domId) as HTMLCanvasElement;
    }
    selfChart.value = echarts.init(chartContainer);
  });

  window.addEventListener('resize', updateContainer, false);

  onUnmounted(() => {
    window.removeEventListener('resize', updateContainer);
  });
```

**改后**：
```ts
  const handleResize = () => updateContainer();

  onMounted(() => {
    if (!chartContainer) {
      chartContainer = document.getElementById(domId) as HTMLCanvasElement;
    }
    selfChart.value = echarts.init(chartContainer);
    window.addEventListener('resize', handleResize, false);
  });

  onUnmounted(() => {
    window.removeEventListener('resize', handleResize);
  });
```

**验证**：`npm run build` + dashboard 图表 resize 正常。
**风险**：零。**回滚**：`git restore`。

---

## 阶段三：UI 与组件统一化

### 任务 3.1：新建 TDesign 版 StatusTag 并替换 20+ 页内联 t-tag

**前提**：`shared/components/StatusTag.vue` 用 `el-tag`，admin 禁 EP → 新建本端 TDesign 版。

**前置确认**：读 `shared/statusConfig.js` 的 `getStatusConfig` 返回结构与 `tagType` 取值集合，确认下方 `TDESIGN_THEME_MAP` 覆盖所有可能值。

**新建 `src/components/status-tag/index.vue`**：
```vue
<!--
  TDesign 版通用状态标签。admin 端专用，避免引入 shared/StatusTag（el-tag）。
  用法：
    <StatusTag :status-map="ORDER_STATUS_MAP" :status="row.status" />
-->
<template>
  <t-tag :theme="tagTheme" :variant="variant" :size="size">
    {{ label }}
  </t-tag>
</template>

<script setup lang="ts">
import { computed } from 'vue';
import { getStatusConfig } from '@shared/statusConfig';

const props = defineProps({
  statusMap: { type: Object as () => Record<string, any>, required: true },
  status: { type: [Number, String] as () => number | string, required: true },
  size: { type: String as () => 'small' | 'medium' | 'large', default: 'small' },
  variant: { type: String as () => 'dark' | 'light' | 'outline' | 'light-outline', default: 'light' },
});

// shared tagType 用 EP 命名（success/info/warning/danger），
// TDesign theme 同名兼容 success/warning/danger，'info' → 'primary'。
const TDESIGN_THEME_MAP: Record<string, string> = {
  success: 'success',
  warning: 'warning',
  danger: 'danger',
  info: 'primary',
  primary: 'primary',
  default: 'default',
};

const config = computed(() => getStatusConfig(props.statusMap, props.status));
const tagTheme = computed(() => TDESIGN_THEME_MAP[config.value?.tagType] || 'default');
const label = computed(() => config.value?.label ?? '-');
</script>
```

**逐页替换**（每页替换后跑 build）：
1. 顶部 `import StatusTag from '@/components/status-tag/index.vue';`
2. 模板内 `<t-tag :theme="xxxTheme(s)" variant="light">{{ xxxLabel(s) }}</t-tag>` → `<StatusTag :status-map="XXX_STATUS_MAP" :status="s" />`
3. 删除该页 `xxxTheme` / `xxxLabel` 本地 helper（若仅被此 tag 用）。
4. 保留 `XXX_STATUS_MAP` 的 `@shared/statusConfig` import。

**逐页清单**：
- `src/pages/users/detail/index.vue`（service/invoice/balance/priority/ticket/notice 共 7 处）
- `src/pages/finance/orders/index.vue`
- `src/pages/finance/invoices/index.vue`
- `src/pages/finance/recharges/index.vue`
- `src/pages/services/index.vue`
- `src/pages/referral/index.vue`
- `src/pages/users/components/VerificationPanel.vue`
- `src/pages/content/index.vue`
- `src/pages/admin/system/staff/index.vue`
- `src/pages/member-levels/index.vue`
- `src/pages/tickets/index.vue`
- 其余 `grep_search ":theme=\".*StatusTheme\(|:theme=\".*Theme\("` 全部列出

**验证**：每替换一页 `npm run build`；全部替换后人工目测状态颜色与改前一致。
**风险**：中（颜色映射需目测回归，发现颜色偏差停下问）。**回滚**：`git restore`。

---

### 任务 3.2：字号 token 化

**新建 `src/style/typography.less`**：
```less
// 字号工具类，统一全站字号梯度。优先使用 var(--td-font-size-*)。
.t-text-xs   { font-size: var(--td-font-size-size-1, 12px); line-height: 20px; }
.t-text-sm   { font-size: var(--td-font-size-size-2, 13px); line-height: 20px; }
.t-text-base { font-size: var(--td-font-size-size-3, 14px); line-height: 22px; }
.t-text-lg   { font-size: var(--td-font-size-size-4, 16px); line-height: 24px; }
.t-text-xl   { font-size: var(--td-font-size-size-5, 18px); line-height: 26px; }
.t-text-2xl  { font-size: var(--td-font-size-size-6, 20px); line-height: 28px; }
.t-text-3xl  { font-size: var(--td-font-size-size-7, 22px); line-height: 30px; }
.t-num-strong { font-variant-numeric: tabular-nums; font-weight: 600; }
```
在 `src/style/index.less` 追加 `@import './typography.less';`。

> **前置确认**：读 `node_modules/tdesign-vue-next/es/style/variables.less` 确认 `--td-font-size-size-*` 的确切命名与序号映射，调整上表 fallback 值与序号对应关系。

**替换规则**（保留 fallback 确保 token 缺失时不变）：
| 原 | 改后 |
|---|---|
| `font-size: 12px;` | `font-size: var(--td-font-size-size-1, 12px);` |
| `font-size: 13px;` | `font-size: var(--td-font-size-size-2, 13px);` |
| `font-size: 14px;` | `font-size: var(--td-font-size-size-3, 14px);` |
| `font-size: 16px;` | `font-size: var(--td-font-size-size-4, 16px);` |
| `font-size: 18px;` | `font-size: var(--td-font-size-size-5, 18px);` |
| `font-size: 20px;` | `font-size: var(--td-font-size-size-6, 20px);` |
| `font-size: 22px;` | `font-size: var(--td-font-size-size-7, 22px);` |

**执行**：`grep_search "font-size: \d+px"` 列出全部 100+ 处，逐文件 `replace_string_in_file`。优先重灾区：`settings/index.less`、`content/index.less`、`logs/index.less`、`users/detail/index.less`、`tickets/index.less`、`member-levels/index.less`。

**验证**：`npm run build` + 目测各页字号无变化。
**风险**：中（视觉回归，逐页目测）。**回滚**：`git restore`。

---

### 任务 3.3：RecordDetailPage 提升目录 + 统一详情页

**步骤**：
1. 创建 `src/components/record-detail-page/index.vue`，内容复制自 `src/components/finance-record-detail/RecordDetailPage.vue`（props/slots 不变）。
2. 删除原 `src/components/finance-record-detail/RecordDetailPage.vue`。
3. 更新 `src/pages/finance/orders/detail/index.vue` import 路径：
   ```ts
   import RecordDetailPage from '@/components/record-detail-page/index.vue';
   ```
4. 改造 `src/pages/users/detail/index.vue`：把自写 `detail-action-bar`（约 L8-22）替换为 `<RecordDetailPage>`，原工具栏按钮放 `#toolbar-actions` slot：
   ```html
   <RecordDetailPage
     :loading="loading"
     eyebrow="用户"
     :title="user.account"
     back-text="返回用户列表"
     @back="router.back()"
     @refresh="loadUser"
   >
     <template #toolbar-actions>
       <t-button variant="outline" @click="...">保存</t-button>
     </template>
     <!-- 原详情内容放 default slot -->
   </RecordDetailPage>
   ```
5. 同样改造 `src/pages/tickets/detail/index.vue`、`src/pages/content/edit.vue`、`src/pages/notifications/email-template-detail/index.vue`。

**验证**：`npm run build` + 4 详情页目测工具栏一致。
**风险**：中（4 页布局重构）。**回滚**：`git restore`。

---

### 任务 3.4：圆角 / 阴影 token 化

**前置确认**：读 `node_modules/tdesign-vue-next/es/style/variables.less` 确认 `--td-radius-*` / `--td-shadow-*` 命名。

**替换规则**（保留 fallback）：
| 原 | 改后 |
|---|---|
| `border-radius: 2px` | `var(--td-radius-small, 2px)` |
| `border-radius: 4px` | `var(--td-radius-default, 4px)` |
| `border-radius: 6px` | `var(--td-radius-medium, 6px)` |
| `border-radius: 8px` | `var(--td-radius-large, 8px)` |
| `border-radius: 12px` | `var(--td-radius-extraLarge, 12px)` |
| `border-radius: 9999px/50%` | `var(--td-radius-circle)` |
| `box-shadow: 0 4px 14px rgba(28,45,84,0.06)` | `var(--td-shadow-1, 0 4px 14px rgba(28,45,84,0.06))` |
| `box-shadow: 0 8px 20px rgba(28,45,84,0.06)` | `var(--td-shadow-2, 0 8px 20px rgba(28,45,84,0.06))` |

**执行**：grep `border-radius:` 与 `box-shadow:` 逐文件替换。涉及 ~10 文件（见审查报告 §2.3 M2）。

**验证**：`npm run build` + 目测。**风险**：低。**回滚**：`git restore`。

---

### 任务 3.5：login / TopPanel / FrameContent 硬编码色改 token

**文件**：
- `src/pages/login/index.vue`（约 L103-167 的 `#f5f7fb/#fff/#eef2f7/#1f2937/#5b6b82/#94a0b2`）
- `src/pages/dashboard/base/components/TopPanel.vue`（约 L55-72 iconBg/iconColor）
- `src/layouts/components/FrameContent.vue:101`（`background-color:#fff`）

**替换规则**（前置确认 TDesign 变量命名）：
| 原 | 改后 |
|---|---|
| `#fff`/`#ffffff` 背景 | `var(--td-bg-color-container, #fff)` |
| `#f5f7fb`/`#eef2f7` 页面底 | `var(--td-bg-color-page, #f5f7fb)` |
| `#1f2937` 主文字 | `var(--td-text-color-primary, #1f2937)` |
| `#5b6b82` 次文字 | `var(--td-text-color-secondary, #5b6b82)` |
| `#94a0b2` 弱文字 | `var(--td-text-color-placeholder, #94a0b2)` |

**TopPanel iconBg/iconColor**：保留常量对象但改语义色变量：
```ts
const ICON_STYLE_MAP = {
  income:  { bg: 'var(--td-success-bg-color, #e8f5e9)', color: 'var(--td-success-color, #12b76a)' },
  warning: { bg: 'var(--td-warning-bg-color, #fff6e5)', color: 'var(--td-warning-color, #f59e0b)' },
  info:    { bg: 'var(--td-brand-bg-color, #f0f9ff)',   color: 'var(--td-brand-color, #0ea5e9)' },
};
```

**验证**：`npm run build` + 切换 TDesign 主题色确认联动。
**风险**：低。**回滚**：`git restore`。

---

### 任务 3.6：抽 `src/utils/formRules.ts` 共享校验规则

**新建 `src/utils/formRules.ts`**：
```ts
import type { FormRule } from 'tdesign-vue-next';

export function required(message: string, trigger: ('blur' | 'change')[] = ['blur', 'change']): FormRule {
  return { required: true, message, type: 'error', trigger };
}
export function emailRule(message = '请输入正确的邮箱地址'): FormRule {
  return { pattern: /^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$/, message, type: 'warning', trigger: 'blur' };
}
export function phoneRule(message = '请输入正确的手机号'): FormRule {
  return { pattern: /^1[3-9]\d{9}$/, message, type: 'warning', trigger: 'blur' };
}
export function amountRule(message = '请输入有效金额'): FormRule {
  return { pattern: /^(?:0|[1-9]\d*)(?:\.\d{1,2})?$/, message, type: 'warning', trigger: 'blur' };
}
export function passwordRule(min = 6, message?: string): FormRule {
  return { min, message: message || `密码至少 ${min} 位`, type: 'error', trigger: 'blur' };
}
export function composeRules(...rules: FormRule[]): FormRule[] {
  return rules;
}
```

**逐页替换**（先 read 确认实际 rules 结构）：
- `src/pages/users/index.vue`（约 L199 `createRules`）
- `src/pages/users/detail/index.vue`（约 L766-794，8 套 rules）
- `src/pages/content/index.vue`（约 L249）
- `src/pages/content/edit.vue`（约 L218）
- `src/pages/admin/system/staff/index.vue`（约 L203）
- `src/pages/admin/system/roles/index.vue`（约 L215）
- `src/pages/member-levels/index.vue`（约 L149）
- `src/pages/users/components/VerificationPanel.vue`（约 L277 `apiRules/feeRules`）

每页：import 工厂函数 → 用 `required('...')`/`emailRule()` 拼装 → 删除本地重复规则。

**验证**：`npm run build` + 各表单提交校验行为不变。
**风险**：中（逐表单回归）。**回滚**：`git restore`。

---

### 任务 3.7：抽 `PagedListParams` 基础接口

**新建 `src/api/types.ts`**（若已存在则追加）：
```ts
export interface PagedListParams {
  page?: number;
  page_size?: number;
  keyword?: string;
}
```

**改造**（每个 `XxxListParams` extends 并删重复字段）：
- `src/api/user.ts:3` `UserListParams extends PagedListParams`
- `src/api/supplier.ts:3` `SupplierListParams extends PagedListParams`
- `src/api/service.ts:3` `ServiceListParams extends PagedListParams`
- `src/api/product.ts:3` `ProductListParams extends PagedListParams`
- `src/api/admin-staff.ts:3` `StaffListParams extends PagedListParams`
- `src/api/admin/types.ts:48/92/124` `InvoiceListParams`/`OrderListParams`/`RechargeListParams extends PagedListParams`

**验证**：`npm run build`。**风险**：低。**回滚**：`git restore`。

---

### 任务 3.8：重命名 settings 页 `hero-card` 类名

**文件**：`src/pages/settings/index.vue`（约 L108-187）+ `src/pages/settings/index.less`

**改动**：全局替换 `hero-card` → `slides-config-card`、`hero-section` → `slides-section`、`hero-section-head` → `slides-section-head`。仅类名，不改逻辑。

> 说明：这些类名实际是"官网首页轮播配置表单"容器，非 page-head 模式，但会触发 AGENTS.md 禁词审计误报。

**验证**：`npm run build` + settings 页轮播配置表单正常。
**风险**：零。**回滚**：`git restore`。

---

## 阶段四：列表页抽象与多端适配

### 任务 4.1：抽 useListPage + ListFilterBar + 重构 10+ 列表页

**新建 `src/hooks/useListPage.ts`**：
```ts
import { reactive, ref, onMounted } from 'vue';
import type { Ref } from 'vue';

export interface UseListPageOptions<F, T> {
  fetch: (params: F & { page: number; page_size: number }) => Promise<{ list: T[]; total: number }>;
  defaultFilters: F;
  defaultPageSize?: number;
  immediate?: boolean;
}

export function useListPage<F extends Record<string, any>, T>(options: UseListPageOptions<F, T>) {
  const { fetch, defaultFilters, defaultPageSize = 20, immediate = true } = options;
  const filters = reactive({ ...defaultFilters }) as F;
  const list: Ref<T[]> = ref([]);
  const total = ref(0);
  const loading = ref(false);
  const pagination = reactive({ page: 1, page_size: defaultPageSize, pageSizeOptions: [20, 50, 100] });

  async function loadList() {
    loading.value = true;
    try {
      const res = await fetch({ ...filters, page: pagination.page, page_size: pagination.page_size });
      list.value = res.list || [];
      total.value = res.total || 0;
    } finally {
      loading.value = false;
    }
  }
  function handleSearch() { pagination.page = 1; return loadList(); }
  function resetFilters() { Object.assign(filters, defaultFilters); pagination.page = 1; return loadList(); }
  function handlePageChange(page: number) { pagination.page = page; return loadList(); }
  function handlePageSizeChange(size: number) { pagination.page_size = size; pagination.page = 1; return loadList(); }

  if (immediate) onMounted(loadList);

  return { filters, list, total, loading, pagination, loadList, handleSearch, resetFilters, handlePageChange, handlePageSizeChange };
}
```

**新建 `src/components/list-filter-bar/index.vue`**：
```vue
<template>
  <div class="list-filter-bar">
    <div class="list-filter-bar__fields"><slot /></div>
    <div class="list-filter-bar__actions">
      <slot name="actions" />
      <t-button theme="primary" variant="outline" :loading="loading" @click="emit('search')">
        <template #icon><search-icon /></template>搜索
      </t-button>
      <t-button variant="text" @click="emit('reset')">
        <template #icon><refresh-icon /></template>重置
      </t-button>
      <t-button variant="text" @click="emit('refresh')">
        <template #icon><refresh-icon /></template>刷新
      </t-button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { SearchIcon, RefreshIcon } from 'tdesign-icons-vue-next';
defineProps<{ loading?: boolean }>();
const emit = defineEmits<{ search: []; reset: []; refresh: [] }>();
</script>

<style lang="less" scoped>
.list-filter-bar {
  display: flex; flex-wrap: wrap; gap: var(--td-comp-margin-s); align-items: center;
  margin-bottom: var(--td-comp-margin-l);
  &__fields { display: flex; flex-wrap: wrap; gap: var(--td-comp-margin-s); flex: 1; }
  &__actions { display: flex; gap: var(--td-comp-margin-s); }
}
</style>
```

**改造方式**：从最简单的 `finance/recharges/index.vue` 试点，验证后推广。每页：
1. import `useListPage` + `ListFilterBar` + 对应 api。
2. 用 `useListPage` 替换本地 `filters/pagination/loadList/handleSearch/resetFilters`。
3. 模板筛选区改用 `<ListFilterBar @search="handleSearch" @reset="resetFilters" @refresh="loadList">`。
4. 表格/分页绑 `:data="list"` `:total="total"` `v-model:current="pagination.page"` 等。

**逐页清单**（按复杂度从低到高）：recharges → orders → invoices → services → users → content → tickets → staff → roles → referral → member-levels → logs（logs 有 tab 切换，最后做）。

**每页验证**：`npm run build` + `npm run test:e2e`（该页对应用例）。
**风险**：高（10+ 页重构）。**每页提交一次**。**回滚**：`git restore`。

---

### 任务 4.2：修复 6+1 处 `computed(() => window.matchMedia(...).matches)` 反模式

**涉及文件**：
- `src/pages/notifications/index.vue`（约 L388 isMobile）
- `src/pages/referral/index.vue`（约 L323）
- `src/pages/member-levels/index.vue`（约 L145）
- `src/pages/products/coupons/index.vue`（约 L411）
- `src/pages/products/coupon-campaigns/index.vue`（约 L338）
- `src/pages/logs/index.vue`（约 L510 drawerSize）

**改前**（logs 示例）：
```ts
const drawerSize = computed(() => (window.matchMedia?.('(max-width: 768px)').matches ? '100%' : '700px'));
```
**改后**：
```ts
import { useMediaQuery } from '@/hooks/useMediaQuery';
const isMobile = useMediaQuery('(max-width: 768px)');
const drawerSize = computed(() => (isMobile.value ? '100%' : '700px'));
```

**isMobile 类型**：
```ts
// 改前
const isMobile = computed(() => window.matchMedia?.('(max-width: 768px)').matches || false);
// 改后
const isMobile = useMediaQuery('(max-width: 768px)');  // Ref<boolean>
```
> `useMediaQuery` 返回 `Ref<boolean>`，模板自动解包；脚本内若原用 `isMobile.value` 保留，若在 computed 内用 `isMobile` 需改 `.value`。

**验证**：`npm run build` + resize / 切设备模拟器，卡片/表格模式随视口切换。
**风险**：低。**回滚**：`git restore`。

---

### 任务 4.3：MobileRecordCard 推广到剩余 14 个列表页

**前提**：读 `src/components/mobile-record-card/index.vue` 确认 props 签名（title/status/statusMap/highlight/fields/actions 等）。

**涉及 14 页**：logs、content、notifications、tickets、member-levels、users 列表、products、products/specs、referral、staff、roles、cpu-models、users/detail 内嵌列表、settings。

**改造模式**（每页统一）：
```vue
<template>
  <div v-if="!isMobile" class="table-scroll">
    <t-table :data="list" :columns="columns" row-key="id" />
  </div>
  <div v-else class="mobile-card-list">
    <MobileRecordCard
      v-for="row in list"
      :key="row.id"
      :title="row.account || row.title"
      :status="row.status"
      :status-map="XXX_STATUS_MAP"
      :highlight="formatMoney(row.amount)"
      :fields="[
        { label: '创建时间', value: formatDateTime(row.created_at) },
        { label: '其他', value: row.xxx },
      ]"
      :actions="mobileActions(row)"
    />
  </div>
</template>
```

> 每页需设计该业务的 `fields`/`actions` 映射，逐页实现。

**验证**：`npm run build` + `npm run test:e2e -- --project=mobile` + 390px 目测。
**风险**：中（每页设计卡片字段）。**每页提交一次**。**回滚**：`git restore`。

---

### 任务 4.4：viewport meta 加 `viewport-fit=cover`

**文件**：`index.html`（约 L6）

**改前**：`<meta name="viewport" content="width=device-width, initial-scale=1.0" />`
**改后**：`<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />`

**验证**：iPhone 真机底部 `env(safe-area-inset-bottom)` 生效。
**风险**：零。**回滚**：`git restore`。

---

### 任务 4.5：触控目标 44px

**文件**：`src/style/reset.less`（约 L52-60）

**改前**：
```less
button, [role='button'], .t-button { min-height: 32px; }
@media (max-width: 768px) {
  button, [role='button'], .t-button { min-height: 40px; }
}
```

**改后**：
```less
button, [role='button'], .t-button { min-height: 32px; }
@media (max-width: 768px) {
  button, [role='button'], .t-button {
    min-height: 44px;
    min-width: 44px;
  }
  .t-button[shape='square'] {
    width: 44px;
    height: 44px;
  }
}
```

**验证**：`npm run build` + 移动端目测 + e2e mobile。
**风险**：低（按钮略大）。**回滚**：`git restore`。

---

### 任务 4.6：引入统一平板断点

**文件**：`src/style/layout.less`（约 L36 `min-width:760px`）+ `src/layouts/components/Header.vue`

**改 `layout.less`**：在 `@media (max-width: 768px)` 之前新增：
```less
@media (max-width: 1024px) {
  .t-layout.t-layout--with-sider {
    > .t-layout { min-width: 0; }
  }
}
```

**Header.vue 平板适配**：读 L189-208 后按实际结构调整。新增：
```ts
import { useMediaQuery } from '@/hooks/useMediaQuery';
const isTablet = useMediaQuery('(max-width: 1024px) and (min-width: 769px)');
```
模板内 `v-if="!isTablet"` 隐藏 `.header-search`，`.header-notice` 收进下拉。

**验证**：`npm run build` + iPad 模拟器（768/834/1024）无横向滚动。
**风险**：中。**回滚**：`git restore`。

---

### 任务 4.7：Playwright 增加 tablet project

**文件**：`playwright.config.ts`（约 L21-30）

**改前**：
```ts
projects: [
  { name: 'desktop', use: { ...devices['Desktop Chrome'], viewport: { width: 1440, height: 900 } } },
  { name: 'mobile',  use: { ...devices['Pixel 5'],        viewport: { width: 390, height: 844 } } },
],
```

**改后**：
```ts
projects: [
  { name: 'desktop', use: { ...devices['Desktop Chrome'], viewport: { width: 1440, height: 900 } } },
  { name: 'tablet',  use: { ...devices['iPad Mini'],      viewport: { width: 768,  height: 1024 } } },
  { name: 'mobile',  use: { ...devices['Pixel 5'],        viewport: { width: 390, height: 844 } } },
],
```
> 确认 `devices['iPad Mini']` 存在；若无用 `devices['iPad (gen 7)']` 或手动 viewport。

**验证**：`npm run test:e2e` 三 project 全跑。
**风险**：低（可能暴露已有适配 bug，逐个修或暂存 known failure）。**回滚**：`git restore`。

---

### 任务 4.8：面包屑 / 多标签 Tabs 移动端折叠

**文件**：`src/layouts/components/LayoutContent.vue` + `src/style/layout.less`

**前置确认**：读 LayoutContent.vue 找 `<l-breadcrumb>` / `<t-tabs>` 渲染后的根 class。

**改 `layout.less`** 追加：
```less
@media (max-width: 768px) {
  .tdesign-breadcrumb,
  .tdesign-starter-tabs {
    display: none;
  }
}
```
（class 名以实际渲染为准）

**验证**：`npm run build` + 390px 目测无面包屑/tabs。
**风险**：低-中（用户习惯变化）。**回滚**：`git restore`。

---

### 任务 4.9：关键字号改 `clamp()`

**文件**：`src/style/typography.less`（任务 3.2 已建）

**追加**：
```less
.t-text-responsive-title { font-size: clamp(16px, 2vw, 20px); line-height: 1.4; }
.t-text-responsive-body  { font-size: clamp(12px, 1.5vw, 14px); line-height: 1.5; }
```
在 dashboard 标题、详情页 title、卡片大数字处改用这两个类。

**验证**：`npm run build` + resize 目测字号平滑变化。
**风险**：低。**回滚**：`git restore`。

---

### 任务 4.10：统一 1180/1100/960/900 → 1024 断点

**执行**：
1. `grep_search "@media.*1180px|@media.*1100px|@media.*960px|@media.*900px"` 列出全部 ~15 文件。
2. 逐文件 `replace_string_in_file`：`@media (max-width: 1180px)` → `@media (max-width: 1024px)`，1100/960/900 同理。
3. 每改一文件目测 1024 / 900 两断点表现，若规则冲突需手动调整（原 1180+900 两级 → 保留 1024+768 两级）。

**验证**：`npm run build` + iPad 834/1024 目测各页。
**风险**：中（断点合并可能视觉跳变）。**每页提交**。**回滚**：`git restore`。

---

### 任务 4.11：admin 工单详情接入 TicketChatPanel（评估后定）

**前置**：读 `shared/components/TicketChatPanel.vue` 确认 UI 框架。若强依赖 EP → **跳过**或新建 TDesign 版。

**若可行**：`src/pages/tickets/detail/index.vue` 自写 `message-list/message-item` 替换为 `<TicketChatPanel>`。

**验证**：`npm run build` + 工单详情发消息/查历史正常。
**风险**：中-高。**建议**：评估后决定，不强制。**回滚**：`git restore`。

---

## 阶段五：可选增强（未经确认不执行）

| 任务 | 文件 | 操作 |
|---|---|---|
| 5.1 业务文案 i18n | 60+ .vue | 视产品定位。中文写死改 `t('xxx')`，在 `src/locales/zh-CN.ts` 增 key |
| 5.2 引入 `useBreakpoints` | 各页面 | `import { useBreakpoints } from '@vueuse/core'`，统一断点常量 |
| 5.3 `assetsInlineLimit` | `vite.config.ts` | `build.assetsInlineLimit: 4096` 显式 |
| 5.4 `chunkSizeWarningLimit` | `vite.config.ts` | `1200 → 500` |
| 5.5 dashboard watch 单源 | `dashboard/base/index.vue` | `watch([productChartData,dailyRevenue])` → `watch(monthlyRevenue, renderCharts)` |

---

## 进度追踪表

### 阶段一
- [x] 1.1 format.ts 收敛（文件已存在，仅需清理 20 页本地副本）
- [x] 1.2 errorMessage 收敛
- [x] 1.3 清理 Starter 死代码

### 阶段二
- [x] 2.1 lodash → lodash-es
- [x] 2.2 移除 @types/echarts
- [x] 2.3 setting.vue 监听清理
- [x] 2.4 tabs-router 持久化收敛
- [x] 2.5 keep-alive 收敛
- [x] 2.6 登录背景 PNG → WebP
- [x] 2.7 hooks/index.ts addEventListener 修复

### 阶段三
- [x] 3.1 TDesign StatusTag + 替换 20+ 页
- [x] 3.2 字号 token 化
- [x] 3.3 RecordDetailPage 提升 + 4 详情页统一
- [x] 3.4 圆角/阴影 token 化
- [x] 3.5 login/TopPanel/FrameContent 硬编码色改 token
- [x] 3.6 formRules.ts 共享规则
- [x] 3.7 PagedListParams 基础接口
- [x] 3.8 settings hero-card 重命名（hero-card→slides-config-card, hero-section(-head)→slides-section(-head)，.vue+.less 同步）

### 阶段四
- [~] 4.1 useListPage + ListFilterBar + 10+ 列表页重构（基础设施已建并提交；现有页因 buildParams 不契合 + e2e 不稳定，未强迁，供后续渐进迁移）
- [x] 4.2 修复 6+1 处 matchMedia 反模式
- [~] 4.3 MobileRecordCard 推广 14 页（pattern 已建：roles/staff 新增卡片模式；finance×3/services/coupons/coupon-campaigns/users/content/notifications/member-levels/referral/tickets 本就有卡片或响应式布局；specs/cpu-models/logs 为密集配置/日志表，保留表格）
- [x] 4.4 viewport-fit=cover
- [x] 4.5 触控目标 44px
- [x] 4.6 统一平板断点
- [x] 4.7 Playwright tablet project
- [x] 4.8 面包屑/tabs 移动端折叠
- [x] 4.9 字号 clamp()
- [x] 4.10 统一 1180/1100/960/900 → 1024
- [~] 4.11 工单详情 TicketChatPanel（跳过：shared/TicketChatPanel.vue 有 47 处 el-* 强依赖 Element Plus，与 admin 端 TDesign-only 约束冲突；按手册"shared 强依赖 EP 则跳过"处理）

### 阶段五（可选）
- [~] 5.1 i18n 业务文案（跳过：属产品定位决策，60+ 文件投机性巨改，无多语言需求证据）
- [~] 5.2 useBreakpoints（跳过：与既有 useMediaQuery 重复，避免竞争抽象）
- [x] 5.3 assetsInlineLimit（显式声明 4096）
- [~] 5.4 chunkSizeWarningLimit（跳过：vendor-tdesign 1.35MB 不可再分，降 500 仅产生噪音警告）
- [x] 5.5 dashboard watch 单源

---

## 最终验证清单

全部任务完成后统一执行：
```bash
cd frontend-admin-v3
npm run build              # vue-tsc + vite build，必须 0 error
npm run lint               # eslint --max-warnings 0
npm run stylelint          # stylelint
npm run test:e2e           # playwright desktop + tablet + mobile
```

人工目测（至少覆盖）：
- [ ] dashboard 加载、图表 resize、切 tab 内存释放
- [ ] users 列表 + 详情（PC / 834 平板 / 390 手机）
- [ ] finance/orders 列表 + 详情（三端）
- [ ] tickets 列表 + 详情（三端）
- [ ] logs（三端，含 tab 切换）
- [ ] settings（含官网轮播配置表单）
- [ ] login（PC / 手机，背景图加载）
- [ ] 状态标签颜色与改前一致（所有列表页）
- [ ] 字号梯度与改前一致（所有页面）
- [ ] iPhone 底部安全区生效

性能对比：
- [ ] `dist/assets/` 出现独立 `vendor-lodash-*.js`
- [ ] 首屏主 chunk 体积较基线下降 ~70KB
- [ ] 登录背景图体积下降 ≥50%
- [ ] 长会话开 10+ tab 后 DevTools Memory 稳定
