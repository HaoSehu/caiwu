# 待判定文件清单

- 执行日期：2026-05-23
- 说明：以下文件因信息不足、依赖不明确或涉及运行中进程，暂不移动，需后续人工判定

## 待判定项

| # | 路径 | 类型 | 说明 | 建议后续动作 |
|---|------|------|------|-------------|
| 1 | `png_to_svg.py` (根) | Unknown | PNG→SVG 转换工具，无代码引用，可能是素材处理用的一次性脚本 | 确认用途后决定保留或归档到 `文档/归档/历史资料/` |
| 2 | `frontend-admin/_halo_build.bat` | Unknown | 构建包装脚本，调用 `npm run build` 并记录状态。可能被外部 Halo 部署工具按路径调用 | 确认部署链路是否依赖此路径，若不依赖则可归档 |
| 3 | `frontend-admin/_halo_status.txt` | Archive | `_halo_build.bat` 的输出文件 | 随 `_halo_build.bat` 一起判定 |
| 4 | `frontend-client/_halo_build.bat` | Unknown | 同上，用户端的构建包装脚本 | 同上 |
| 5 | `frontend-client/_halo_status.txt` | Archive | 同上，输出文件 | 同上 |
| 6 | `frontend-client/_seo_build.bat` | Unknown | SEO 构建包装脚本 | 确认是否仍在使用的 SEO 流程 |
| 7 | `frontend-client/_seo_status.txt` | Archive | SEO 构建输出文件 | 随 `_seo_build.bat` 一起判定 |
| 8 | `frontend-client/npm` | Unknown | 0 字节空文件，来源不明 | 确认无用途后加入建议删除清单 |
| 9 | `backend/.phpunit.cache/` | Generated | PHPUnit 缓存目录（含单个 test-results 文件），运行 `php artisan test` 可重建 | 建议后续删除（已列入建议删除清单） |
| 10 | `frontend-admin/dist/` | Generated | Vite 构建产物目录（6.6MB），运行 `npm run build` 可重建 | 建议后续删除（已列入建议删除清单） |
| 11 | `frontend-client/dist/` | Generated | Vite 构建产物目录，运行 `npm run build` 可重建 | 建议后续删除（已列入建议删除清单） |
| 12 | `docs/` (空目录) | Archive | 内容已全部移走（仅剩空的 `优化demo/` 子目录），无代码引用 | 建议收口到 `文档/` 后删除。AGENTS.md 和 CLAUDE.md 均使用 `文档/`，无 `docs/` 引用 |

## 判定优先级

| 优先级 | 项 | 原因 |
|--------|-----|------|
| 高 | `docs/` 空目录 | 内容已清空，需决定是删除还是保留为历史目录 |
| 高 | `_halo_*` / `_seo_*` 文件 | 如涉及部署流程，误移可能影响构建 |
| 中 | `dist/` 和 `.phpunit.cache/` | 可再生产物，清理后无影响但需确认 |
| 低 | `png_to_svg.py` | 无引用，不影响任何流程 |
| 低 | `frontend-client/npm` | 0 字节空文件 |
