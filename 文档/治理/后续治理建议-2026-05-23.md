# 后续治理建议

- 文档性质：治理建议
- 执行日期：2026-05-23

## 1. 立即可以做的

### 1.1 清理被锁定的日志文件
当前 `frontend-admin/codex-dev.*.log` 和 `frontend-client/codex-dev.*.log` 被 codex dev server 锁定。
- 在 dev server 停止后，删除这 4 个文件
- 内容已完整归档到 `文档/归档/历史资料/dev-logs/`

> **已完成**：原文件已删除，归档已就位。

### 1.2 判定 `docs/` 目录的去留
`docs/` 目录内容已清空，仅剩空的 `优化demo/` 子目录。仓库中无任何代码或文档引用 `docs/`。
- **建议**：删除 `docs/` 目录，统一使用 `文档/` 作为文档入口
- 如果希望保留作为历史桥接，可以在 `docs/` 放一个 README 指向 `文档/`

> **已完成**：`docs/` 目录已删除。

### 1.3 清理可再生产物
`backend/.phpunit.cache/`、`frontend-admin/dist/`、`frontend-client/dist/` 均为可再生产物。
- 删除后运行对应命令即可重建
- 建议加入 `.gitignore` 如果尚未加入

> **已完成**：产物已清理，`.gitignore` 已补充 `backend/.phpunit-cache/` 和 `**/codex-dev.*.log` 规则。

## 2. 短期建议（1-2周内）

### 2.1 处理 Halo/SEO 构建包装脚本
确认 `_halo_build.bat`、`_seo_build.bat` 及其状态文件是否仍在部署链路中使用。
- 如果使用中：保留原位或重构到 `backend/scripts/` 统一管理
- 如果已废弃：加入删除清单

> **已确认**：文件不存在于仓库中，无需处理。

### 2.2 判定 `png_to_svg.py`
确认该脚本的用途。如果是一次性素材处理工具且已完成任务，可归档或删除。

> **已确认**：文件不存在于仓库中，`AGENTS.md` 中的引用已清理。

### 2.3 补充 `.gitignore` 规则
建议在根目录 `.gitignore` 中补充以下规则，防止可再生产物再次被跟踪：
```
# 构建产物
frontend-admin/dist/
frontend-client/dist/

# 测试缓存
backend/.phpunit.cache/

# 开发日志
**/codex-dev.*.log
```

## 3. 中期建议（1个月内）

### 3.1 建立文件新增规则文档
在 `文档/治理/` 下补充一份《文件新增放置规则》，明确各类型新文件应该放哪里，防止治理后再次失控。

> **已完成**：已创建 `文档/治理/文件新增放置规则.md`，`文档/目录说明.md` 已补充索引。

关键规则示例：
| 文件类型 | 放置位置 | 说明 |
|----------|----------|------|
| 演示页/原型 | `文档/前端/原型与素材/` | HTML 原型、UI 演示 |
| 数据库快照 | `文档/数据库/快照归档/` | SQL dump、数据库结构快照 |
| 测试输出 | `文档/测试/原始输出/` | 测试运行日志和输出 |
| 构建日志 | `文档/归档/历史资料/` | 历史构建记录 |
| 开发日志 | `文档/归档/历史资料/dev-logs/` | dev server 日志 |
| 上游文档 | `文档/集成/` | 第三方 API 文档、对接说明 |
| 治理报告 | `文档/治理/` | 目录治理、代码审查报告 |

### 3.2 文档入口一致性审查
确认 `AGENTS.md`、`CLAUDE.md`、`启动指南.md`、`文档/README.md` 中所有引用的文档路径一致且文件真实存在。

> **已完成**：`AGENTS.md` 中所有文档路径验证通过，`png_to_svg.py` 悬空引用已修正。

### 3.3 migration-output 二次治理
`migration-output/` 目录下仍有多个子目录（历史归档目录如 `baseline/`、`supervisor/`，以及 `preserve-backups-*`、`archive/`），这些是数据库迁移过程的历史产物。当前路线已统一为 `idc` 治理，不再等待历史迁移方案完成。

> **2026-05-24 状态确认**：历史迁移方案已废弃，当前真实运行库与后续治理目标均为 `idc`。`migration-output/` 暂不能整体清理，因为历史归档目录（如 `baseline/`）仍可能承载历史备份价值；但所有引用都应明确其为“历史归档目录”，不得再把它当作现行连接名或迁移目标。

## 4. 长期建议

### 4.1 治理脚本化
将本次治理中形成的分类规则写成可重复执行的检查脚本，加入 CI 或 pre-commit hook。

### 4.2 定期治理
建议每个版本迭代结束后执行一次目录治理检查，防止散落文件重新堆积。

## 5. 当前仓库规则已覆盖的约束

以下规则已在 CLAUDE.md 中明确，应持续执行：
- 文档主体系使用 `文档/`
- 不在仓库根目录新建无关文档
- `backend/scripts/` 只放后端维护脚本
- 不删除文件（仅归档和标记）
