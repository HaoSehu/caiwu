# 建议删除清单（不执行删除）

- 文档性质：建议报告
- 执行日期：2026-05-23
- 重要提示：**本清单仅列出建议，实际不执行任何删除操作。** 所有删除决策需要人工确认后单独执行。

## 建议删除项

### 第一类：可再生产物（删除后可通过标准命令重建）

| # | 路径 | 当前大小 | 重建命令 | 风险 |
|---|------|----------|----------|------|
| 1 | `backend/.phpunit.cache/` | 81KB | `cd backend && php artisan test` | 无 |
| 2 | `frontend-admin/dist/` | 6.6MB | `cd frontend-admin && npm run build` | 确认当前无人从 dist 目录手动部署 |
| 3 | `frontend-client/dist/` | - | `cd frontend-client && npm run build` | 同上 |

### 第二类：历史上游文件副本

| # | 路径 | 说明 | 风险 |
|---|------|------|------|
| 4 | `migration-output/archive/paymentservice_head.php` | 历史上游网关 PHP 副本 | 确认无引用后安全 |
| 5 | `migration-output/archive/paymentservice_index.php` | 同上 | 同上 |

### 第三类：历史构建日志和状态文件

| # | 路径 | 说明 | 风险 |
|---|------|------|------|
| 6 | `frontend-admin/_halo_status.txt` | Halo 构建状态记录 | 确认部署不依赖后安全 |
| 7 | `frontend-client/_halo_status.txt` | 同上 | 同上 |
| 8 | `frontend-client/_seo_status.txt` | SEO 构建状态记录 | 同上 |

### 第四类：空文件和测试残留

| # | 路径 | 说明 | 风险 |
|---|------|------|------|
| 9 | `frontend-client/npm` | 0 字节空文件 | 无 |
| 10 | `migration-output/archive/write-test.txt` | 测试残留文件 | 无 |
| 11 | `文档/测试/原始输出/test_results.txt` | 空测试输出文件（0 字节） | 无 |
| 12 | `文档/测试/原始输出/test_output.txt` | 乱码路径的测试输出 | 无 |

### 第五类：开发日志

| # | 路径 | 说明 | 风险 |
|---|------|------|------|
| 13 | `frontend-admin/codex-dev.err.log` | 开发日志（0 字节，已被 dev server 锁定） | 进程停止后可删 |
| 14 | `frontend-admin/codex-dev.out.log` | 开发日志（已被 dev server 锁定） | 同上 |
| 15 | `frontend-client/codex-dev.err.log` | 开发日志（0 字节，已被 dev server 锁定） | 同上 |
| 16 | `frontend-client/codex-dev.out.log` | 开发日志（已被 dev server 锁定） | 同上 |

### 第六类：历史遗留目录

| # | 路径 | 说明 | 风险 |
|---|------|------|------|
| 17 | `docs/` | 内容已清空，仅剩空的 `优化demo/` 子目录。无代码引用 | 确认 `docs/` 无硬依赖后安全 |
| 18 | `png_to_svg.py` | PNG→SVG 转换脚本，无引用 | 确认无用途后安全 |

## 建议删除执行顺序

1. **先删最安全的**：空文件、测试残留（第四类）
2. **再删可再生的**：dist/、.phpunit.cache/（第一类）
3. **确认后删**：历史上游文件、构建日志（第二、三类）
4. **最后处理**：历史目录、独立工具脚本（第五、六类）

## 预计清理效果

| 类别 | 预计释放空间 |
|------|-------------|
| `dist/` 目录 | ~15MB+ |
| SQL 快照（已归档） | ~45MB |
| `.phpunit.cache/` | ~81KB |
| 其他零散文件 | <1MB |
| **合计约** | **~60MB** |
