# 前后端字段名一致性审计报告

- 审计日期：2026-07-17
- 数据基线：`migration-output/idc-current-table-structure.md` → `文档/开发文档/数据库/当前数据库结构.md`
- 数据库：`idc`，60 张表，830 个字段

---

## 目录

1. [第一轮：初扫](#一第一轮初扫)
2. [第二轮：后端 Model 深度排查](#二第二轮后端-model-深度排查)
3. [第三轮：前端字段引用深度排查](#三第三轮前端字段引用深度排查)
4. [第四轮：迁移文件与实库差异](#四第四轮迁移文件与实库差异)
5. [最终汇总](#五最终汇总)

---

# 一、第一轮：初扫

## 🔴 严重：后端会产生 SQL 错误

### 1.1 payments 表 — `gateway` vs `gateway_key`

`payments` 表只有 `gateway_key` 列，没有 `gateway` 列。V1 控制器直接对查询构建器使用 `where('gateway', ...)`，**会报 SQL 错误**。

Payment 模型有 `gateway()` 属性访问器将 get/set 映射到 `gateway_key`，但**只对模型实例有效，查询构建器的 `where()` 不走访问器**。V2 控制器已正确使用 `whereGatewayKeyIn()` 作用域。

| # | 文件 | 行号 | 错误写法 | 正确写法 |
|---|------|------|---------|---------|
| 1 | `app/Http/Controllers/Client/PaymentController.php` | 23 | `whereIn('gateway', ...)` | `->whereGatewayKeyIn(...)` |
| 2 | `app/Http/Controllers/Client/PaymentController.php` | 30 | `where('gateway', ...)` | `->whereGatewayKey(...)` |
| 3 | `app/Http/Controllers/Client/PaymentController.php` | 75 | `whereIn('gateway', ...)` | `->whereGatewayKeyIn(...)` |
| 4 | `app/Http/Controllers/Client/PaymentController.php` | 98 | `whereIn('gateway', ...)` | `->whereGatewayKeyIn(...)` |
| 5 | `app/Http/Controllers/Client/RechargeController.php` | 35 | `where('gateway', PaymentGatewayCode::ALIPAY)` | `->whereGatewayKey(...)` |
| 6 | `app/Http/Controllers/Client/RechargeController.php` | 58 | `where('gateway', PaymentGatewayCode::ALIPAY)` | `->whereGatewayKey(...)` |
| 7 | `app/Http/Controllers/Client/InvoiceController.php` | 269 | `where('gateway', PaymentGatewayCode::ALIPAY)` | `->whereGatewayKey(...)` |
| 8 | `app/Http/Controllers/Client/InvoiceController.php` | 312 | `where('gateway', PaymentGatewayCode::ALIPAY)` | `->whereGatewayKey(...)` |
| 9 | `app/Http/Controllers/Client/InvoiceController.php` | 342 | `where('gateway', PaymentGatewayCode::ALIPAY)` | `->whereGatewayKey(...)` |

### 1.2 AccountMigrationService — 原始 SQL 选了不存在的列

| # | 文件 | 行号 | 问题 |
|---|------|------|------|
| 10 | `app/Services/System/AccountMigrationService.php` | 917 | `SELECT id, product_snapshot_json FROM invoices` — 列不存在，应为 `config_snapshot` |
| 11 | `app/Services/System/AccountMigrationService.php` | 940 | 同上 |

---

## 🟠 中等：Model `$fillable` 包含虚拟/不存在的列

### 2.1 Product 模型

| 文件 | 字段 | 状态 |
|------|------|------|
| `app/Models/Product.php` | `name` | 虚拟 — `setNameAttribute()` 丢弃写入值，`getNameAttribute()` 从 `custom_display_name` 读取 |
| `app/Models/Product.php` | `type` | 虚拟 — `setTypeAttribute()` 映射到 `product_type` |
| `app/Models/Product.php` | `supplier_product_name` | 可能不存在 — 直接写入 `$this->attributes['supplier_product_name']`，DB 无此列 |

### 2.2 Invoice 模型

| 文件 | 字段 | 状态 |
|------|------|------|
| `app/Models/Invoice.php` | `product_snapshot_json` | 虚拟 — 读写 `config_snapshot`，`$casts` 也声明了 `array` |

### 2.3 Payment 模型

| 文件 | 字段 | 状态 |
|------|------|------|
| `app/Models/Payment.php` | `gateway` | 虚拟 — `Attribute::make()` 映射到 `gateway_key` |

---

## 🟡 已弃用的孤立模型（引用不存在的表）

| 模型文件 | 指向的表 | 当前状态 |
|---------|---------|---------|
| `app/Models/ServiceInstance.php` | `service_instances` | 表不存在，被 ServiceLifecycleLog/ServiceOperationLog/ServiceRemoteSnapshot 引用 |
| `app/Models/AccountBalanceSnapshot.php` | `account_balance_snapshots` | 表不存在，已标 `@deprecated` |
| `app/Models/AccountLedger.php` | `account_ledgers` | 表不存在，当前活跃是 `account_transactions` |
| `app/Models/ServiceLifecycleLog.php` | `service_lifecycle_logs` | 表不存在 |
| `app/Models/ServiceOperationLog.php` | `service_operation_logs` | 表不存在 |
| `app/Models/ServiceRemoteSnapshot.php` | `service_remote_snapshots` | 表不存在 |

---

## 🟢 Product `toMigrationPayload()` 遗留字段

`app/Models/Product.php` 第 419-433 行在 `$setIfColumnExists()` 中写入了 `name`、`slug`、`summary`、`purchase_requires_json`、`supplier_product_name`，均不是当前表列。有 `$setIfColumnExists` 保护不会报错，但是无效代码。

---

## 🔵 前端问题

### 3.1 admin-v3：死代码组件

| 文件 | 问题 |
|------|------|
| `frontend-admin-v3/src/components/product-card/index.vue` L20-21 | `product.name`、`product.description` — 全仓库无导入 |

### 3.2 console-v4：无效兜底字段

| # | 文件 | 行号 | 问题 |
|---|------|------|------|
| 12 | `src/types/client.ts` | 55 | `ClientUserInfo.username?: string` — `users` 表无 `username` 列 |
| 13 | `src/store/modules/user.ts` | 14 | `raw.username` 兜底 — 应为 `raw.nickname` |
| 14 | `src/pages/client/dashboard/index.vue` | 368 | `avatar`/`avatar_url`/`headimg` 兜底链 — API 均不返回 |
| 15 | `src/domains/finance/useRecords.ts` | 54 | `BILLING_CYCLE_KEYS` 含 `billingcycle`、`billingcycle_zh`、`period` 旧键名 |

### 3.3 www-v3：未发现严重问题

`contentNormalizer.js` 的 `createdAt` 兜底属于防御性编码。

### 3.4 shared：未发现数据字段问题

---

## ✅ 已验证正常

- API Resource 同时返回 `gateway` 和 `gateway_key`（均源于 `gateway_key` 列），前端兜底链正确
- `admin_users` 表有 `username` 列，API 中 `username` 均指管理员
- `users` 的 `nickname`：API 统一返回，前端未混用
- `product.name`：`AdminProductListResource` 返回计算后的 `adminDisplayName`，前端安全
- 未发现 `avatar`、`is_agent`、`first_name`、`last_name`、`address`、`gender`、`birthday` 等字段
- 未发现 `transaction_id`、`total` 替代 `amount`、camelCase 字段问题

---

# 二、第二轮：后端 Model 深度排查

## 🔴 严重问题

### 2.1 Withdrawal 模型 — 表名错误

| 文件 | 问题 |
|------|------|
| `app/Models/Withdrawal.php` L18 | `$table = 'withdrawals'` — 表不存在，DB 实际表为 `referral_withdrawals` |

### 2.2 ContentArticle — fillable 包含 4 个不存在的列

| 文件 | 错误列 | 实际列 |
|------|--------|--------|
| `app/Models/ContentArticle.php` L49 | `type` | `content_type` |
| `app/Models/ContentArticle.php` L49 | `node_type` | 不存在 |
| `app/Models/ContentArticle.php` L49 | `content_category_id` | `category_id` |
| `app/Models/ContentArticle.php` L52 | `category` | `category_name` |

且 `$casts` 中声明了 `node_type => string`，但该列不存在。

### 2.3 SystemSetting — fillable 包含 4 个不存在的列

| 文件 | 错误列 | 说明 |
|------|--------|------|
| `app/Models/SystemSetting.php` | `legacy_setting_id` | 不存在 |
| `app/Models/SystemSetting.php` | `value_type` | 不存在 |
| `app/Models/SystemSetting.php` | `is_encrypted` | 不存在 |
| `app/Models/SystemSetting.php` | `updated_by_admin_id` | 不存在 |

DB `settings` 表只有 `id, group_key, item_key, item_value` 4 列。

### 2.4 NotificationTemplate — fillable 严重不匹配

| 类型 | 字段 |
|------|------|
| **多余的 8 个列** | `channel`, `code`, `description`, `audience`, `provider_template_id`, `is_enabled`, `is_custom`, `sort_order` |
| **缺失的 5 个列** | `slug`, `type`, `status`, `created_by`, `updated_by` |

DB `notification_templates` 表实际列为：`id, name, slug, type, subject, content, variables_json, provider_variables_json, status, created_by, updated_by, created_at, updated_at`

### 2.5 ServiceRuntimeSnapshot / ServiceConnectionSnapshot / ServiceProvisionAttempt — 外键列名不匹配

| 模型 | 关系方法中的外键 | DB 实际列 |
|------|---------------|----------|
| `ServiceRuntimeSnapshot::upstreamBinding()` | `service_upstream_binding_id` | `binding_id` |
| `ServiceConnectionSnapshot::upstreamBinding()` | `service_upstream_binding_id` | `binding_id` |
| `ServiceProvisionAttempt::upstreamBinding()` | `service_upstream_binding_id` | `binding_id` |

### 2.6 Product::setNameAttribute — 写入被静默丢弃

`app/Models/Product.php` L132: `setNameAttribute()` 执行 `unset($this->attributes['name'])`，任何对 `$product->name = 'xxx'` 或 `Product::create(['name' => ...])` 的赋值都会被**静默丢弃**。

---

## 🟠 中等问题

### 2.7 FirstProductGroup — fillable 缺少 DB 列

| 文件 | 缺失列 | DB 类型 |
|------|--------|--------|
| `app/Models/FirstProductGroup.php` | `product_type` | `varchar(50)` |
| `app/Models/FirstProductGroup.php` | `legacy_product_type` | `varchar(50)` |

### 2.8 SupplierPluginBinding — has_secret_json 类型 cast 错误

`app/Models/SupplierPluginBinding.php` L38: `'has_secret_json' => 'array'` — 该列应为 `tinyint` 布尔标记，不应 cast 为 `array`。

同样问题出现在 `IntegrationPluginConfig` (L21) 和 `ServiceConnectionSnapshot` (L29)。

### 2.9 Invoice — fillable 缺少 remark, operator

DB `invoices` 表有 `remark`、`operator` 列，但 Invoice 模型的 `$fillable` 未包含这两个字段。

### 2.10 旧式 `$casts` 属性（非方法）

以下 5 个模型使用旧式 `protected $casts = [...]` 替代 Laravel 12 推荐的 `protected function casts(): array`：

| 模型 |
|------|
| `IntegrationPlugin` |
| `IntegrationPluginConfig` |
| `NotificationTemplate` |
| `SmsLog` |
| `EmailLog` |

---

## 🟡 孤立模型（引用不存在的表）

| 模型 | 声明表 | 说明 |
|------|--------|------|
| `AccountBalanceSnapshot` | `account_balance_snapshots` | 已标 `@deprecated` |
| `AccountLedger` | `account_ledgers` | 当前活跃是 `account_transactions` |
| `Permission` | `permissions` | 表不存在，引用 `role_permissions` 中间表 |
| `ProductConfigOption` | `product_config_options` | 表不存在 |
| `ProductPrice` | `product_prices` | 表不存在 |
| `ProductPricingPlan` | `product_pricing_plans` | 表不存在 |
| `ReferralRelation` | `referral_relations` | 表不存在 |
| `Refund` | `refunds` | 表不存在 |
| `Server` | `servers` | 表不存在 |
| `ServiceInstance` | `service_instances` | 表不存在 |
| `ServiceLifecycleLog` | `service_lifecycle_logs` | 表不存在 |
| `ServiceOperationLog` | `service_operation_logs` | 表不存在 |
| `ServiceRemoteSnapshot` | `service_remote_snapshots` | 表不存在 |
| `SmsLog` | `sms_logs` | 表不存在，sync 写 `notification_logs` |
| `TicketAttachment` | `ticket_attachments` | 表不存在 |
| `TicketDepartment` | `ticket_departments` | 表不存在 |
| `TicketMessage` | `ticket_messages` | 表不存在 |
| `UserReferral` | `user_referrals` | 多处 `Schema::hasTable` 保护 |

### User.id_card cast 不一致

`VerificationHistory` 模型用 `casts()` 声明 `id_card`，而 `User` 模型用手动 `getIdCardAttribute` 访问器调用 `LegacyEncrypted`。

---

## ✅ 已验证正确的项

- 所有 join 子句使用正确的表和列别名
- 所有 `DB::raw` 引用使用正确的 DB 列名
- `user_referrals` 引用均有 `Schema::hasTable` 保护
- `FirstProductGroup.code`、`is_visible` 查询正确
- `Payment.gateway()` Attribute::make 正确映射

---

# 三、第三轮：前端字段引用深度排查

> 待填写

---

# 四、第四轮：迁移文件与实库差异

> 待填写

---

# 五、最终汇总

> 待填写

### 1.2 AccountMigrationService — 原始 SQL 选了不存在的列

`invoices` 表不存在 `product_snapshot_json` 列，实际列是 `config_snapshot`。

| # | 文件 | 行号 | 问题 |
|---|------|------|------|
| 10 | `app/Services/System/AccountMigrationService.php` | 917 | `SELECT id, product_snapshot_json FROM invoices` |
| 11 | `app/Services/System/AccountMigrationService.php` | 940 | 同上 |

### 1.3 SystemSetting 模型 — 4 个不存在的列

`settings` 表实际列仅：`id, group_key, item_key, item_value`。

`app/Models/SystemSetting.php` L14-21 的 `$fillable` 包含：

| 字段 | 状态 |
|------|------|
| `legacy_setting_id` | ❌ 列不存在，无 accessor |
| `value_type` | ❌ 列不存在，无 accessor |
| `is_encrypted` | ❌ 列不存在，无 accessor |
| `updated_by_admin_id` | ❌ 列不存在，无 accessor |

> 当前未发现 `SystemSetting::create()` 调用，但属于休眠炸弹。

### 1.4 ContentArticle 模型 — `node_type` 列不存在

`content_articles` 表无 `node_type` 列（迁移中添加过但实库中缺失）。模型在 L36, L43, L78 仍引用：

- `$fillable` 包含 `node_type`
- `$casts` 声明 `'node_type' => 'string'`
- 定义了 `NODE_TYPE_ARTICLE = 'article'` 常量

无 getter/setter，若执行 `ContentArticle::create(['node_type' => 'article'])` 将触发 SQL 错误。

---

## 🟠 中等：Model `$fillable` 虚拟字段 / 数据丢失 / 列不存在

### 2.1 Product 模型 — `supplier_product_name` 列不存在

`app/Models/Product.php` L44, L99, L138：

- `$fillable` 包含 `supplier_product_name`
- `getSupplierProductNameAttribute()` / `setSupplierProductNameAttribute()` 存在
- setter 直接写入 `$this->attributes['supplier_product_name']`
- **但 `products` 表无此列**

`Product::create(['supplier_product_name' => 'xxx'])` 会触发 SQL 错误。

### 2.2 Product 模型 — `name` 静默数据丢失（影响 3 个控制器）

`app/Models/Product.php` L36, L133：

- `$fillable` 包含 `name`
- `setNameAttribute()` 执行 `unset($this->attributes['name'])` —— 静默丢弃
- **无 `getNameAttribute()`**
- `$product->name` 始终返回 `null`

受影响代码：

| 文件 | 行号 | 问题 |
|------|------|------|
| `Admin/ReferralRewardController.php` | 51 | `'name' => $item->product->name` → 始终 null |
| `Client/ReferralController.php` | 56 | `'name' => $item->product?->name` → 始终 null |
| `Client/V2/ReferralController.php` | 56 | `'name' => $item->product?->name` → 始终 null |

### 2.3 ProductAdminService — 拆分商品 `name` 被丢弃

`app/Services/ProductCatalog/ProductAdminService.php` L873：

```php
'name' => (string) $variant['name'],  // 被 setNameAttribute 丢弃！
```

`buildSplitVariantName()` 生成了有意义的名称，但 payload 中**没有设置 `custom_display_name`**，实际名称完全依赖回退解析器生成。

### 2.4 Invoice 模型 — `product_snapshot_json` 虚拟字段

`app/Models/Invoice.php` L28：`$fillable` 包含 `product_snapshot_json`，通过 `HandlesProductSnapshot` trait 读写 `config_snapshot`。

### 2.5 Payment 模型 — `gateway` 与 `gateway_key` 同时存在

`app/Models/Payment.php` L31-32：`$fillable` 同时包含 `gateway`（虚拟 accessor）和 `gateway_key`（真实 DB 列）。冗余混淆。

### 2.6 ContentArticle 模型 — 完整虚拟字段列表

| fillable 字段 | 映射到 DB 列 | 有 accessor |
|--------------|-------------|------------|
| `type` | `content_type` | ✅ getTypeAttribute / setTypeAttribute |
| `node_type` | — | ❌ 无 |
| `content_category_id` | `category_id` | ✅ getContentCategoryIdAttribute |
| `category` | `category_name` | ✅ getCategoryAttribute |

### 2.7 ContentCategory 模型

| fillable 字段 | 映射到 DB 列 | 有 accessor |
|--------------|-------------|------------|
| `type` | `content_type` | ✅ getTypeAttribute / setTypeAttribute |

### 2.8 OperationLog 模型

| fillable 字段 | 映射到 DB 列 | 有 accessor |
|--------------|-------------|------------|
| `target_id` | `subject_id` | ✅ getTargetIdAttribute / set… |
| `detail` | `context` | ✅ getDetailAttribute / set… |

### 2.9 Product 模型 `toMigrationPayload()` 遗留字段

`app/Models/Product.php` L419-L433（`$setIfColumnExists` 保护）：

| 行号 | 字段 | 正确列名 |
|------|------|---------|
| 419 | `name` | 列不存在 |
| 424 | `slug` | 列不存在 |
| 425 | `summary` | 列不存在 |
| 428 | `purchase_requires_json` | `purchase_requires` |
| 433 | `supplier_product_name` | 列可能不存在 |

`CatalogMigrationService.php` L343 同样引用了 `purchase_requires_json`。

---

## 🟡 低：已弃用的孤立模型（表不存在）

| 模型文件 | 引用表 | 状态 |
|----------|--------|------|
| `app/Models/ServiceInstance.php` | `service_instances` | 表不存在，被 ServiceLifecycleLog 等引用 |
| `app/Models/AccountBalanceSnapshot.php` | `account_balance_snapshots` | 表不存在，标记 `@deprecated` |
| `app/Models/AccountLedger.php` | `account_ledgers` | 表不存在，当前活跃是 `account_transactions` |
| `app/Models/ServiceLifecycleLog.php` | `service_lifecycle_logs` | 表不存在，引用 ServiceInstance |
| `app/Models/ServiceOperationLog.php` | `service_operation_logs` | 表不存在，引用 ServiceInstance |
| `app/Models/ServiceRemoteSnapshot.php` | `service_remote_snapshots` | 表不存在，引用 ServiceInstance |
| `app/Models/Server.php` | `servers` | 表不存在，仅被 ServiceInstance::server() 引用 |

---

## 🟢 低：死代码 / 防御性兜底问题

### 4.1 AdminUser `->name` 死代码（20+ 处）

大量 V1/V2 控制器使用回退链：
```php
$request->user()?->username ?? $request->user()?->name ?? $request->user()?->email
```

`AdminUser` 模型**没有 `name` 列**，也没有 `getNameAttribute()`。`->name` 始终返回 `null`，总是回退到 `->email`。不产生 bug，但属于死代码。

涉及文件（部分）：`AdminStaffController.php:88`、`CouponCampaignController.php:89`、`InvoiceController.php:37`、`ProductController.php:152` 等。

### 4.2 `frontend-admin-v3` — 死代码组件

`src/components/product-card/index.vue` L20-21：使用 `product.name`、`product.description`，**全仓库无任何导入**。

### 4.3 `frontend-user-v4-console` — 用户信息 `username` 兜底

| 文件 | 行号 | 问题 |
|------|------|------|
| `src/types/client.ts` | 55 | `ClientUserInfo.username?: string` — `users` 表只有 `nickname` |
| `src/store/modules/user.ts` | 14 | `raw.username` 作为兜底 — API 不返回 |

### 4.4 `frontend-user-v4-console` — Dashboard 头像兜底链

`src/pages/client/dashboard/index.vue` L368：
```
userInfo.value.avatar || userInfo.value.avatar_url || userInfo.value.headimg
```
**三个字段 API 都不返回**，永远为空。

### 4.5 `frontend-user-v4-console` — 账单周期旧键名

`src/domains/finance/useRecords.ts` L54：`BILLING_CYCLE_KEYS` 含 `billingcycle`、`billingcycle_zh`、`period`（旧字段名），DB 列是 `billing_cycle`。

### 4.6 `frontend-admin-v3` — 日志/充值页面 `gateway` 无回退

管理端直接使用 `row.gateway`，不像用户控制台有 `gateway_label || gateway_key || gateway` 防御链：

| 文件 | 行号 |
|------|------|
| `src/pages/finance/recharges/index.vue` | 75, 309, 348 |
| `src/pages/logs/index.vue` | 133, 169 |
| `src/pages/logs/components/LogDetailDrawer.vue` | 66, 139 |

### 4.7 `frontend-admin-v3` — 商品表单发送 `display_name` / `name` 冗余字段

`src/pages/products/components/ProductCatalog.vue` L2276-2278 和 `src/pages/products/edit.vue` L813-814 在 payload 中发送 `display_name` 和 `name`，但 DB 只有 `custom_display_name`，`name` 被后端 `setNameAttribute` 丢弃。

---

## ✅ 已验证正常（排除误报）

| 检查项 | 结论 |
|--------|------|
| API `gateway` 字段 | Resource 同时返回 `gateway` + `gateway_key`（均来自 `gateway_key` 列），前端兜底链正确 |
| `username` → admin_users | `admin_users` 表有 `username` 列，API 全部正确区分 |
| `users.nickname` | API 统一返回 `nickname`，前端未混用 |
| `product.name` 在 API | `AdminProductListResource` 同时返回 `name`(计算值) + `custom_display_name`，前端使用 `name` 安全 |
| `avatar` / `is_agent` / `first_name` / `last_name` / `address` / `gender` / `birthday` | 后端和前端数据查询中均未发现 |
| `transaction_id` | 代码统一使用 `trade_no` |
| `total` 替代 `amount` | 未发现 |
| `createdAt` 等驼峰 | `contentNormalizer` 中兜底为防御性编码，正常路径用 `created_at` |
| 旧字段模式搜索 | `is_pay`, `add_time`, `update_time`, `delete_time`, `create_time`, `reg_date`, `expire_time`, `pay_status`, `total_fee`, `product_desc`, `product_price`, `money` 等均未发现 |
| ticket_replies / invoice_items / gateway_logs / message_logs / notification_templates / notice_reads / operation_logs / schedule_task_runs / member_levels / 三级商品分组 | Controller 中字段使用均与 DB 一致 |
| FormRequest 验证规则 | 未发现对不存在列的验证 |
| Migration `renameColumn` | 0 处，无历史字段重命名遗留 |
| DB::raw / DB::select | 所有列名经审查均为合法列 |
| shared/ 目录 | statusConfig / StatusTag / TicketChatPanel 均无字段引用问题 |
| Plugin 目录 | 字段名均为合法列名 |
| `real_name` / `referral_code` / `alipay_*` / `phone` / `nickname` / `billing_cycle` / `trade_no` 拼写 | 前端类型定义均正确 |
| `_at` 后缀字段 | 未发现缺失后缀（如 `created`、`updated`、`paid`、`verified`） |
| JSON 字段类型 | 未发现 `pricing` / `config_options` 被错误标记为 `string` |

---

## 问题统计

| 严重级别 | 数量 | 分类 |
|----------|------|------|
| 🔴 严重 (SQL错误) | 16 | payments.gateway(9)、product_snapshot_json(2)、SystemSetting幻列(4)、ContentArticle.node_type(1) |
| 🟠 中等 (数据丢失/风险) | 13 | Product.name数据丢失(3)、Product.supplier_product_name、ProductAdminService.name、Invoice.product_snapshot_json、Payment.gateway冗余、ContentArticle/Category虚拟字段(4)、Product.toMigrationPayload(4字段)、CatalogMigrationService |
| 🟡 低 (孤立模型) | 7 | ServiceInstance、AccountBalanceSnapshot、AccountLedger、ServiceLifecycleLog、ServiceOperationLog、ServiceRemoteSnapshot、Server |
| 🟢 低 (死代码/兜底) | 7 类 | AdminUser.name(20+处)、product-card组件、username/avatar兜底、billingcycle键名、gateway无回退、商品表单冗余字段 |
| **总计** | **38** | |

---

## 复审确认（2026-07-17 基于实库 DDL 快照逐表核对）

> 复审方法：将报告中的每一个"正确字段名"与 `当前数据库结构.md`（`2026-07-17 13:29:58` 版，直连 `information_schema` 生成）逐一比对。

### 逐项核对结果

| 报告条目 | 报告所述"正确字段/列不存在" | 实库确认 | 结论 |
|----------|---------------------------|---------|------|
| §1.1 payments.gateway 列不存在 | `gateway_key` 是正确列 | ✅ payments 表 18 列，有 `gateway_key`(varchar 120)，无 `gateway` | **正确** |
| §1.2 invoices.product_snapshot_json 列不存在 | `config_snapshot` 是正确列 | ✅ invoices 表 33 列，有 `config_snapshot`(json)，无 `product_snapshot_json` | **正确** |
| §1.3 settings 表仅 4 列 | `legacy_setting_id/value_type/is_encrypted/updated_by_admin_id` 不存在 | ✅ settings 表仅 `id, group_key, item_key, item_value` | **正确** |
| §1.4 content_articles.node_type 列不存在 | 列不存在 | ✅ content_articles 表 26 列，无 `node_type` | **正确** |
| §2.1 products.supplier_product_name 列不存在 | 列不存在 | ✅ products 表 17 列，无 `supplier_product_name` | **正确** |
| §2.2 products.name 列不存在 | `custom_display_name` 是正确列 | ✅ products 表无 `name` 列，有 `custom_display_name`(varchar 190) | **正确** |
| §2.3 ProductAdminService.name 被丢弃 | 应使用 `custom_display_name` | ✅ 同上 | **正确** |
| §2.4 Invoice.product_snapshot_json | 虚拟字段映射到 `config_snapshot` | ✅ 确认 invoices.config_snapshot 存在 | **正确** |
| §2.5 Payment.gateway 与 gateway_key 冗余 | `gateway` 是虚拟 accessor | ✅ payments 表只有 `gateway_key`，无 `gateway` | **正确** |
| §2.6 ContentArticle 虚拟字段 | `type→content_type`, `content_category_id→category_id`, `category→category_name` | ✅ 实库列名与报告一致 | **正确** |
| §2.7 ContentCategory.type | → `content_type` | ✅ content_categories 表有 `content_type`(varchar 20)，无 `type` | **正确** |
| §2.8 OperationLog 虚拟字段 | `target_id→subject_id`, `detail→context` | ✅ operation_logs 表有 `subject_id`, `context`(json) | **正确** |
| §2.9 Product.toMigrationPayload | `name/slug/summary` 不存在, `purchase_requires_json→purchase_requires` | ✅ products 表无 `name/slug/summary/purchase_requires_json`，有 `purchase_requires`(json) | **正确** |
| §3 孤立模型 | 7 个模型引用不存在的表 | ✅ 实库 60 张表中无 `service_instances/account_balance_snapshots/account_ledgers/service_lifecycle_logs/service_operation_logs/service_remote_snapshots/servers` | **正确** |
| §4.1 AdminUser->name | `admin_users` 表无 `name` 列 | ✅ admin_users 表有 `username`、`nickname`，无 `name` | **正确** |
| §4.2 product-card 死代码 | `product.name/product.description` 不存在 | ✅ products 表无 `name/description` 列 | **正确** |
| §4.3 username 兜底 | users 表只有 `nickname` | ✅ users 表有 `nickname`(varchar 50)，无 `username` | **正确** |
| §4.4 avatar 兜底 | users 表无 avatar 相关列 | ✅ users 表 35 列，无 `avatar/avatar_url/headimg` | **正确** |
| §4.5 billingcycle 旧键名 | DB 列是 `billing_cycle` | ✅ orders/invoices 表均为 `billing_cycle` | **正确** |
| §4.6 gateway 无回退 | 管理端直接用 `row.gateway` | ⚠️ API Resource 层同时返回 `gateway`+`gateway_key`，不会出错但有隐患 | **描述准确** |
| §4.7 商品表单冗余字段 | `name/display_name` 不在 DB 中 | ✅ 确认 products 表无这两个列 | **正确** |

### DB 结构新发现（复审中注意到的新增细节）

1. **gateway_logs 表同时有 `gateway` 和 `gateway_key`**：`gateway`(varchar 50, 网关标识如 alipay_f2f) 和 `gateway_key`(varchar 120) 是两个独立列。报告 §1.1 特指 `payments` 表，不涉及 `gateway_logs`，不会混淆。

2. **member_levels 表列名与旧假设不同**：报告第二轮的旧假设字段 `min_order_count/min_total_spent/discount_percent/description` 确实不存在，实际为 `sales_amount_min/sales_amount_max/reward_rate/remark`。业务代码中未见引用不存在的旧字段。

3. **notice_reads 表用 `article_id` 而非 `notice_id`**：与第二轮审计结论一致，`notice_reads` 外键引用 `content_articles.id`，用 `article_id` 命名是正确的。

4. **ticket_replies 表用 `is_staff` 而非 `admin_id`**：报告第二轮正确指出列名差异。

5. **products 表 `purchase_requires` 是 JSON 类型**：报告 §2.9 中 `purchase_requires_json → purchase_requires` 的映射正确，旧代码中 `purchase_requires_json` 是遗留命名。

### 复审结论

- **38 个问题全部经实库 DDL 快照逐项核对，无误报**
- 所有"正确字段名"建议均与实库列名一致
- 所有"列不存在"断言均经实库字段清单确认
- Agent 审计时使用的旧的 DB 结构描述与实库有部分差异（如 `invoice_items` 的旧描述字段 `product_id/description/amount/billing_cycle` 实际为 `item_name/item_type/line_amount`），但第二轮审计已正确识别这些差异并修正，**未将 Agent 输入的旧字段描述误判为代码问题**
