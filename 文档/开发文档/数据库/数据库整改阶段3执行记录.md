# 数据库整改阶段3执行记录

- 执行日期：2026-07-08
- 阶段目标：统一邮件、短信和通知发送日志到 `message_logs`
- 迁移文件：`backend/database/migrations/2026_07_08_030000_create_message_logs_and_drop_legacy_logs.php`
- 迁移批次：`73`

## 执行内容

1. 新增 `message_logs` 表和 `App\Models\MessageLog`。
2. 将 `notification_logs` 全量迁入 `message_logs`。
3. 将未被 `notification_logs.origin_type/origin_id` 覆盖的 `email_logs`、`sms_logs` 迁入 `message_logs`。
4. 删除旧表：`email_logs`、`sms_logs`、`notification_logs`。
5. 删除旧模型：`EmailLog`、`SmsLog`、`NotificationLog`。
6. 切换邮件/短信发送、后台日志查询、用户日志查询、日志清理、日志归档、插件回填和内容系统通知日志迁移目标。

## 验收结果

| 项 | 结果 |
| --- | --- |
| `message_logs` 行数 | `1,716` |
| 当前表数量 | `59` |
| 当前字段数量 | `824` |
| 当前索引数量 | `313` |
| 数据库级外键 | `81` |
| 旧三表 | 已删除 |
| 旧模型 | 已删除 |
| `db:audit-foreign-keys --strict` | 通过，0 个可补 FK，0 个未分类字段 |
| `db:audit-core --json` | 核心孤儿计数为 0，核心 `trace_id_missing` 为 0 |

## 验证命令

```bash
cd backend
php artisan migrate --no-ansi
php artisan migrate --env=testing --no-ansi
php artisan db:table message_logs --no-ansi
php artisan db:audit-foreign-keys --strict --no-ansi
php artisan db:audit-core --json --no-ansi
php artisan test tests\Feature\NotificationServiceEmailLogFallbackTest.php tests\Feature\NotificationTemplateApiTest.php tests\Feature\SmsLogSanitizationTest.php tests\Feature\AdminLogBoundaryTest.php tests\Feature\V2AdminLogApiTest.php tests\Feature\AdminSecurityBoundaryRegressionTest.php tests\Feature\ContentSystemMigrationServiceTest.php
php backend\scripts\export_database_structure.php
```

测试结果：41 个测试通过，1517 个断言。

## 后续注意

- 历史 migration 仍会出现旧表名，禁止修改历史 migration。
- 内容系统旧库仍以 `notification_logs` 为源表，但当前库目标表已切换为 `message_logs`。
- `user_notifications` 是用户站内收件箱状态表，不属于本次发送日志合并范围。
