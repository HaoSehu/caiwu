# Caiwu 后端生产审查报告（2026-07-02）

## 生产就绪结论

**有条件上线。**

修复后已解除本轮发现的本地上线门禁：`composer test`、`composer analyse`、`composer format:check`、`composer audit` 均通过，缺失的 `smtp` 插件清单已恢复，`phpstan-baseline.neon` 已重建。用户确认只需要魔方财务上游插件，`hosting_panel_api` 主机面板接口插件已移除，不再出现在插件扫描/安装列表；Client 黑洞/管理工具已从后端路由、Controller、Request、Service、测试、用户控制台入口和 API 目录中下线；Stay33 实名认证出站 HTTPS 已改为生产环境强制 SSL 证书校验。

仍建议按“有条件上线”执行：本地代码门禁已经通过，Sentry Laravel SDK 已接入但需要生产配置真实 DSN 并执行事件投递验证；生产 Redis/cache lock、生产 settings/插件启用状态、CA bundle 与宝塔调度还需要上线前环境核验。

审查使用本地 Caiwu skills 与已安装 Laravel/PHP skills 作为框架，优先级为：`AGENTS.md`、Caiwu 本地 backend skills、当前代码、现有测试。外部 Laravel skills 仅作为参照，未引入 JWT、Pest、Horizon、Laravel 13、Sail 或 starter-kit 结构。本轮未发现应用层 `unserialize()` / `phar://` / Phar 入口，因此未展开 `php-unserialize-audit` 专项。

## 审查基线

| 项目 | 结果 |
| --- | --- |
| 审查范围 | `backend/`，仅在 API 契约影响前端时记录影响 |
| 写入范围 | 后端插件清单、PHPStan baseline、Client 黑洞/管理工具下线、仅保留魔方财务上游插件、Stay33 SSL policy、相关测试、用户控制台隐藏工具入口、API 清单与本报告 |
| Git dirty baseline | `git status --short` 共 126 条既有变更/删除，涉及 `.agents`、`.codex/skills`、`.github`、`AGENTS.md`、`CLAUDE.md`、frontend、backend 脚本与文档；本轮未回滚、未整理 |
| 后端版本 | Laravel Framework 12.62.0 |
| PHP | PHP 8.3.8 CLI，本地 composer 约束为 `php: ^8.2` |
| Composer | 2.10.1 |
| 核心依赖 | `laravel/framework ^12.0`、`laravel/sanctum ^4.0`、PHPUnit 11、Pint、Larastan/PHPStan |
| 测试文件 | `backend/tests/**/*.php` 共 165 个 |
| 路由基线 | 修复后 `php artisan route:list --json` 共 353 条：`api/admin` 206、`api/client` 119、`api/site` 20、`api/health` 1、其他 7 |

## 命令证据表

| 命令 | 结果 | 耗时 | 证据/失败原因 |
| --- | --- | --- | --- |
| `php artisan --version` | 通过 | - | Laravel Framework 12.62.0 |
| `php -v` | 通过 | - | PHP 8.3.8 CLI |
| `composer --version` | 通过 | - | Composer 2.10.1 |
| `composer test` | 修复后通过，exit 0 | 215.6s | 549 passed、3 skipped、3179 assertions |
| `composer analyse` | 修复后通过，exit 0 | 55.2s | 已重建 `phpstan-baseline.neon`，PHPStan 显示 `No errors` |
| `composer format:check` | 修复后通过，exit 0 | 3.8s | Pint 显示 `pass`；26 个既有格式文件已由 `composer format` 修复 |
| `composer audit` | 修复后通过，exit 0 | 6.6s | `No security vulnerability advisories found.` |
| `php artisan route:list --json` | 修复后通过，exit 0 | 2.1s | 353 条路由；`/api/client/blackhole/*` 已无注册 |
| `php artisan schedule:list` | 通过，exit 0 | 1.0s | 16 个调度任务，含自动续费、推荐奖励释放、账单维护、队列积压消费、VNC Relay 守护、缓存预热 |
| `php artisan payment:audit-third-party-boundary --strict` | 通过，exit 0 | 0.692s | third-party gateways: `alipay,wechat,stripe`；third_party_payment_count=201；historical_non_third_party_payment_count=66；未超过 baseline |
| `php artisan test --filter=ClientBlackholeRemovalTest` | 修复后通过，exit 0 | 0.53s | 确认 `api/client/blackhole*` 路由列表为空 |
| `php artisan list sentry` | 通过，exit 0 | 1.1s | 已注册 `sentry:publish` 与 `sentry:test` |
| `php artisan config:show sentry --no-interaction` | 通过，exit 0 | 1.1s | `dsn=null`、`send_default_pii=false`、`ignore_transactions=/up` |
| `npm run build`（用户控制台） | PowerShell 策略失败 | 0.6s | `npm.ps1` 被执行策略拦截；改用 `npm.cmd` |
| `npm.cmd run build`（用户控制台） | Vite 配置加载失败 | 17.4s | esbuild 读取上级目录被拒绝；改用 `--configLoader runner` 拆分验证 |
| `npx.cmd vue-tsc --noEmit` | 通过，exit 0 | 13.7s | 用户控制台类型检查通过 |
| `npx.cmd vite build --mode release --configLoader runner` | 通过，exit 0 | 42.01s | 用户控制台生产构建通过 |

## 入口与 API 契约审查

| 面 | 证据路径 | 审查结论 |
| --- | --- | --- |
| Admin 路由 | `backend/routes/admin.php:46-49` | 仅 `/api/admin/login` 公开并限流 `throttle:5,1`；其余 admin 路由在 `auth:sanctum` + `ensure.admin` 分组内 |
| Admin 权限 | `backend/routes/admin.php:56-113`、`:317-335` | 关键模块使用 `permission:{code}` 分组；用户、实名、返佣、提现等路径有权限边界 |
| Client 路由 | `backend/routes/client.php:27-49`、`:98-181` | 登录/注册/验证码/回调/VNC token 公开，其余 client 业务在 `auth:sanctum` + `ensure.client` 内；写操作多数带业务 throttle |
| 回调签名 | `backend/routes/client.php:36-44`、`backend/bootstrap/app.php:65-73` | 实名、支付宝、通用支付回调均挂签名中间件 alias |
| 统一异常 | `backend/bootstrap/app.php:76-109` | API 鉴权、校验、模型缺失、404 转统一 JSON 错误；422 返回 `errors` |
| 成功响应 | `backend/app/Support/ApiResponseBuilder.php:14-22` | 成功固定 `code=0` |
| 分页响应 | `backend/app/Support/ApiResponseBuilder.php:34-45` | 分页固定 `list`、`total`、`page`、`page_size` |
| VNC token | `backend/routes/client.php:46`、`backend/app/Services/ClientServiceConsole/ServiceVncService.php:87-93` | 公开 VNC token 仅用于独立页面交换，设置 TTL、scope、client single-use |
| Client 黑洞/管理工具 | `backend/routes/client.php`、`frontend-user-v4-console/src/router/modules/client.ts`、`文档/开发文档/后端/后端API清单.md` | 已按功能下线删除；`route:list` 与 API 清单均不再包含 `/api/client/blackhole/*` |

未确认到接口契约层 P0/P1 破坏：成功码、分页结构、校验错误形态、admin/client token 分端、回调中间件均符合 Caiwu 规则。风险主要集中在生产配置硬化、可观测性和生产 provider 数据核验。

## 风险表

| 编号 | 严重度 | 模块 | 证据路径 | 影响 | 根因 | 修复建议 | 验证建议 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | 已修复 | 全量测试 / 插件运行时 | `backend/plugins/mail/smtp/config.php`；`backend/plugins/servers/mofang_finance/config.php` | 原插件清单失败已解除；默认 `smtp` 可被插件扫描器解析；上游插件按产品决策仅保留魔方财务 `mofang_finance` | 邮件插件清单缺失；上游插件范围曾误补通用主机面板插件 | 已恢复 `smtp` 插件；已移除 `hosting_panel_api` 插件目录，保留 `mofang_finance` | `composer test` 通过；相关过滤测试均通过 |
| R-002 | 已修复 | 静态分析 | `backend/phpstan-baseline.neon`；`backend/phpstan.neon.dist:1-3` | PHPStan 门禁恢复可运行 | 缺失 baseline 文件 | 已执行 `php vendor/bin/phpstan analyse --memory-limit=1G --generate-baseline` 重建 baseline；后续只允许缩小 baseline | `composer analyse` 通过 |
| R-003 | 已修复 | 代码风格 / 可维护性 | `backend/composer.json:40-44`；`composer format:check` | 原 26 个格式债已清理，release gate 不再被 Pint 阻断 | 多个 app、plugin、migration、test 文件未按 Pint 格式 | 已执行 `composer format` 并复跑 `composer format:check` | `composer format:check` 通过 |
| R-004 | 已修复 | 邮件配置 / 通知链路 | `backend/plugins/mail/smtp/config.php`；`backend/plugins/mail/smtp/lib/SmtpMailService.php` | 默认 `smtp` 插件清单已存在，相关通知测试通过 | 插件化后清单缺失 | 已补齐 `smtp` 插件 | `NotificationServiceEmailLogFallbackTest` 通过 |
| R-005 | 已调整为产品决策 | 上游 provider / 开通链路 | `backend/plugins/servers/mofang_finance/config.php`；`backend/tests/Feature/SupplierInterfaceTypeAliasRegressionTest.php`；`backend/tests/Feature/PluginRuntimeRegistryIntegrationTest.php` | 插件扫描/安装列表只保留魔方财务上游插件；`hosting_panel_api` 不再作为可安装插件暴露 | 用户确认只需要魔方财务插件；通用主机面板插件会造成后台误解和误启用 | 已删除 `backend/plugins/servers/hosting_panel_api/`；测试改为断言 provider types 不包含 `hosting_panel_api` 插件选项 | `PluginRuntimeRegistryIntegrationTest`、`SupplierInterfaceTypeAliasRegressionTest` 通过 |
| R-006 | 已修复 | 实名认证 Stay33 出站 HTTPS | `backend/plugins/certification/stay33/logic/Stay33Client.php`；`backend/plugins/certification/stay33/config.php`；`backend/tests/Feature/PluginRuntimeRegistryIntegrationTest.php` | 生产环境不能再通过插件配置关闭 SSL verify | Stay33 插件未强制生产 SSL verify | 已在 production 强制 `resolveSslVerify()` 返回 true，并更新配置说明 | `PluginRuntimeRegistryIntegrationTest::test_stay33_client_forces_ssl_verification_in_production` 通过 |
| R-007 | 代码侧已修复 / 待生产 DSN 验证 | 生产可观测性 | `backend/composer.json`；`backend/composer.lock`；`backend/bootstrap/app.php`；`backend/config/sentry.php`；`backend/.env.example`；`backend/tests/Feature/BackendHealthFixRegressionTest.php:214` | 后端异常已具备进入 Sentry Laravel SDK 的代码入口；无 DSN 时本地不会投递事件；生产仍需验证真实 DSN、release、environment 与采样率 | 原仓库未接入 error tracking；本轮新增 SDK、配置文件和异常处理注册 | 生产配置 `SENTRY_LARAVEL_DSN`、`SENTRY_ENVIRONMENT`、`SENTRY_RELEASE`，按流量设置 `SENTRY_TRACES_SAMPLE_RATE`；保持 `SENTRY_SEND_DEFAULT_PII=false`，确认日志/事件不带敏感字段 | `php artisan list sentry`、`php artisan config:show sentry`、`BackendHealthFixRegressionTest::test_sentry_is_configurable_without_enabling_default_pii` 均通过；生产 DSN 配好后执行 `php artisan sentry:test` |
| R-008 | 代码侧已修复 / 待环境核验 | 队列/调度韧性 | `backend/routes/console.php:24-40`、`:89-116`、`:491-508`；`backend/app/Jobs/SyncInvoiceCouponUsageJob.php:22-35`；`backend/tests/Feature/BackendHealthFixRegressionTest.php:184`、`:204`；`backend/tests/Feature/ScheduleTaskOverviewTest.php:12` | 所有 ShouldQueue Job 已具备 timeout；调度概览已暴露 mutex degraded 状态；仍需生产确认 Redis/cache lock 可用，避免降级时关键任务重复执行 | 项目为兼容本地/宝塔调度保留降级路径，生产可靠性取决于环境配置 | 保留代码侧 timeout 与 runtime 状态回归；上线前强制核验 `CACHE_STORE=redis` 或等价可靠锁、`redis_volatile`、宝塔每分钟 `schedule:run` 和 `queue.caiwu_worker_timeout` | `BackendHealthFixRegressionTest::test_queue_jobs_define_timeout_policy`、`ScheduleTaskOverviewTest::test_schedule_task_overview_exposes_mutex_and_automation_config_state`、`php artisan schedule:list` 均通过 |
| R-009 | 已修复 | Client 黑洞/管理工具 | `backend/routes/client.php`；`backend/tests/Feature/ClientBlackholeRemovalTest.php`；`frontend-user-v4-console/src/router/modules/client.ts`；`frontend-user-v4-console/src/api/client.ts` | 已收缩生产 API 暴露面和前端入口 | 历史工具不再符合产品范围 | 已删除 client 路由、Controller、FormRequest、Service、旧测试、配置、用户控制台隐藏页面/API 封装；已重生成后端 API 清单并同步管理端 API 目录 | `php artisan route:list --json` 无 blackhole；`ClientBlackholeRemovalTest`、`composer test`、用户控制台类型检查/构建通过 |

## 功能下线项

| 模块 | 当前入口 | 处理决定 | 删除范围 | 验证建议 |
| --- | --- | --- | --- | --- |
| 黑洞/管理工具 | 已无 `/api/client/blackhole/*` 注册入口 | 用户确认不再需要；已按功能下线删除 | 已删除 `routes/client.php` 对应路由、`Client/BlackholeController.php`、`Requests/Client/Blackhole/*`、`Services/Security/BlackholeService.php`、`config/blackhole.php`、旧黑洞测试、用户控制台隐藏工具页/API 封装、API 目录生成物 | `route:list` 无 blackhole；`ClientBlackholeRemovalTest` 通过；`composer test` 通过；API 清单已重生成 |

## P0/P1 复现路径

| 风险 | 复现/核查命令 | 期望 |
| --- | --- | --- |
| R-001 插件清单漂移 | `cd backend && composer test -- --filter=NotificationServiceEmailLogFallbackTest`；`composer test -- --filter=PluginRuntimeRegistryIntegrationTest`；`Get-ChildItem plugins -Recurse -Filter config.php` | 测试应通过；默认邮件能解析到真实 manifest；上游插件列表只包含产品允许的插件 |
| R-002 PHPStan baseline 缺失 | `cd backend && composer analyse`；`Test-Path phpstan-baseline.neon` | `composer analyse` exit 0，baseline 存在或配置不再 include 缺失文件 |
| R-003 Pint 门禁 | `cd backend && composer format:check` | exit 0；无未格式化文件 |
| R-004 邮件默认驱动 | `cd backend && php artisan tinker` 中解析 `app(App\Services\Mail\MailDriverManager::class)` 并触发测试邮件；或跑修复后的邮件 Feature 测试 | 未配置 `notification.mail_driver` 时不会落到未注册 `smtp` |
| R-005 上游 provider | 扫描 `suppliers.interface_type`、`products.provision_module`、`services.provision_data->provider`，确认生产数据已迁移到 `mofang_finance_api` | 生产仅允许魔方财务上游插件；如仍有历史 `hosting_panel_api` 数据，需先迁移或停用相关商品/供应商 |
| R-006 Stay33 SSL | production 环境设置 `ssl_verify=false` 后跑新增 SSL policy test | 生产仍强制校验证书；CA 问题只能通过 `ca_bundle` 修复 |
| R-007 可观测性 | `php artisan list sentry`；`php artisan config:show sentry --no-interaction`；生产配置 DSN 后跑 `php artisan sentry:test` | 仓库已接入 Sentry Laravel SDK；生产 DSN 投递成功且事件不含敏感字段 |

## 高风险业务链路矩阵

| 领域 | 资金/数据不变量 | 幂等、事务、锁 | 审计/补偿 | 现有覆盖 | 缺口建议 |
| --- | --- | --- | --- | --- | --- |
| 支付回调 / 充值 / 账单支付 | Payment 只记录第三方真实资金流入；金额、商户号、订单/账单归属必须一致 | `PaymentService` 回调有签名复验、`Cache::lock`、`lockForUpdate`、事务；重复成功回调走幂等分支 | `payment_callbacks` upsert、`callback_raw`、trace_id；已支付/取消/过期账单捕获金额转余额而非重复履约 | `payment:audit-third-party-boundary --strict` 通过；支付边界/回调投影/余额支付不建 Payment 等测试存在 | 修复全量测试后补一组“金额不一致、商户号不一致、重复回调、已取消账单回调转余额”的组合回归 |
| 下单 / 订单 / 库存 / 优惠券 | quote token、订单金额、库存、优惠券占用不可被重放 | `CheckoutService` 要求 `quote_token` + `X-Idempotency-Key`，按用户/幂等键加锁，产品 `lockForUpdate`，优惠券 reserve | 影子订单、库存扣减、操作日志；取消时释放 coupon | 订单数量、零元订单、优惠券释放等测试存在 | 增加“相同幂等键不同 payload 拒绝”和“并发 checkout 只扣一次库存/券”测试 |
| 自动续费 / 手动续费 | 同一服务同一到期窗口不可重复扣费/重复延长 | `AutoRenewService` 用 `AutomationLog::recordOnce` + `Cache::lock`；`ServiceRenewService` 对 paid renew invoice 用 `lock:renew:invoice` 和 last invoice id 判重 | 失败时 forget record 允许重试；`renew_fulfillment_status`、`last_renew_invoice_id` 记录补偿状态 | `AutoRenewServiceTest`、`ServiceRenewInvoiceOnlyIdempotencyTest` 等存在 | 增加“队列重放 + 支付回调重放 + 自动续费调度重入”组合测试 |
| 服务开通 / 上游插件 | paid order 只能开通一次；上游购物车串行；provider key 不得错误归一化 | `PaidOrderBusinessFlowDispatcher` 分发 referral/provision job；`ProvisionService` 对供应商购物车加 `lock:supplier:cart`，开通前回查已有 upstream host | 开通失败记录 `provision_error`；调度有 orphan failed provision dry-run 告警 | provider resolver、不把 `mofang_finance_api` 别名到 `hosting_panel_api` 的测试存在；插件运行时只启用魔方财务上游插件 | 新增生产 provider preflight：禁止新建/启用非 `mofang_finance_api` 上游供应商，生产历史 `hosting_panel_api` 数据必须迁移后上线 |
| 推荐奖励 / 提现 | 冻结、可提现、提现中、已提现四个余额桶总额守恒；退款必须逆转奖励或阻止退款 | 奖励创建/逆转用 `Cache::lock` + `DB::transaction`；提现申请/审批用 user/account/withdrawal 行锁 | 返佣流水、提现操作日志、退款阻断错误码；提现审批可转余额或外部支付宝线下处理 | 返佣生命周期、账户投影、关键词查询测试存在 | 补 client 提现申请并发、admin approve/reject 幂等、提现后订单退款阻断的 Feature 测试 |
| 工单 / 上传附件 | 附件只能是图片，路径不能越权，临时引用必须属于上传者 | FormRequest 限制 `image`、mimetype、5MB；`TicketService` 用 `UploadedImage` 推断扩展名，`SecureAsset` 临时 URL，normalize 限制 `private/tickets/` 与 `uploads/tickets/` | 工单通知走 Job，不直接同步发信；上传引用由 `UploadedAssetReferenceService` 签发 | `UploadSecurityTest` 覆盖伪 PHP 文件名、路径穿越、普通图片 | 增加大文件边界、SVG/非图片伪装、过期 token 附件提交测试 |
| VNC | 公开页面不得拿到明文密码；client token 单次交换；relay token TTL | `ServiceVncService` 公开 token 24 字节随机、TTL、client single-use；`resolvePublicVncTokenPayload` 不返回 password；relay 仅日志 hash | `VncRelayCommand` 连接失败日志 mask token，不记录 password；调度每分钟守护 relay | `ServiceVncTokenSecurityTest` 覆盖单次消费、不泄露密码、admin relay token 复用 | 生产需确认 `redis_volatile`、`idc.vnc_relay.ssl_verify`、CA bundle；增加 relay token TTL 过期端到端测试 |
| 实名认证 | 回调必须验签、防重放；认证收费不能重复扣 | `VerifyCallbackSignature` 调 driver 验签，nonce replay cache；`VerificationService` 多处事务锁用户 | 回调失败记录 certify_id hash、nonce hash；插件返回 replay_key | `CallbackSignatureMiddlewareTest`、Verification DTO/driver 测试存在 | 增加 Stay33 生产 SSL 强制、回调重复投递、收费失败补偿测试 |
| 数据库维护 / 历史恢复 | Payment 不物理删除；危险恢复命令必须显式确认 | 未发现 `payments` delete；`DatabaseEngineeringService` 测试明确不删除 payments；`MofangBillingRestoreService` 会删除 `balance_logs`/`invoices`，但入口命令要求 `--confirm` 且支持 dry-run | 恢复命令有确认短语 `RESTORE_UPSTREAM_BILLING`；数据库清理对日志类分块删除 | `BackendHealthFixRegressionTest`、`UpstreamBillingRestoreCommandTest` 覆盖 | 生产环境禁用或限制破坏性恢复命令；恢复前强制备份、dry-run 和二次审批 |

## 确认问题 / 待验证问题 / 非问题

**确认问题**

- R-007 代码侧已接入 Sentry Laravel SDK，剩余为生产 DSN 和真实事件投递验证。
- R-008 代码侧已完成 Job timeout 与调度 runtime 状态暴露，剩余风险降级为上线前环境核验项。

**待验证问题**

- 生产 `settings.notification.mail_driver`、已启用插件表、`suppliers.interface_type`、`products.provision_module`、`services.provision_data.provider` 的真实分布需要连生产或准生产库验证。
- 生产 cache/Redis/`redis_volatile` 是否稳定可用、调度是否处于 degraded mode，需要部署环境验证。
- Sentry SDK 已接入仓库；生产仍需配置真实 `SENTRY_LARAVEL_DSN`、`SENTRY_ENVIRONMENT`、`SENTRY_RELEASE` 并执行 `php artisan sentry:test`。

**非问题或已被反证**

- 未发现 Controller 直接 `Http::*` 调第三方；`Http::` 出现在 Service 或插件层。
- 未发现应用层 `unserialize()`、`phar://`、Phar 元数据入口。
- `ProviderResolver::normalizeKey()` 保持原 key，不会把 `mofang_finance_api` 归一化为 `hosting_panel_api`。
- Payment 第三方边界专项命令通过，且未发现 `payments` 表物理删除路径。
- VNC 公开 token 交换不返回 password，相关测试覆盖单次消费与日志脱敏。
- 黑洞/管理工具已删除，不再作为需要继续加固的功能。

## 测试覆盖缺口

| 建议测试名 | 覆盖目标 |
| --- | --- |
| `PluginRuntimeProductionPreflightTest::test_enabled_defaults_resolve_to_existing_manifests` | 默认 mail/upstream provider 必须有插件 manifest 且启用 |
| `NotificationMailDriverDefaultTest::test_unconfigured_mail_driver_uses_registered_driver` | 未设置 `notification.mail_driver` 时不会落到不存在的 `smtp` |
| `UpstreamProviderBindingPreflightTest::test_supplier_and_product_provider_keys_are_registered` | supplier/product/service provider 与插件运行时一致 |
| `ReferralWithdrawalLifecycleTest::test_concurrent_withdrawal_apply_creates_single_pending_record` | 并发提现申请只占用一次可提现余额 |
| `ReferralWithdrawalLifecycleTest::test_approve_and_reject_are_idempotent_under_replay` | 审批/拒绝重复提交不重复挪动余额 |
| `AutoRenewReplayTest::test_scheduler_and_callback_replay_do_not_double_extend_service` | 调度重入 + 回调重放不重复续费 |
| `ScheduleRuntimePreflightTest::test_production_scheduler_requires_reliable_cache_lock` | 生产不允许 schedule mutex degraded 无告警上线 |
| `SentryInstrumentationTest::test_backend_exceptions_are_reportable_with_trace_id` | 异常追踪携带 trace_id 且敏感字段脱敏 |
| 已补：`PluginRuntimeRegistryIntegrationTest::test_stay33_client_forces_ssl_verification_in_production` | production 下 `ssl_verify=false` 不能关闭证书校验 |
| 已补：`ClientBlackholeRemovalTest::test_client_blackhole_routes_are_not_registered` | 删除黑洞/管理工具后，确保 `/api/client/blackhole/*` 路由不再注册 |

## 残余风险

- 本地工作区 dirty baseline 很大，包含文档、skills、frontend、backend 脚本等既有改动；本报告没有判断这些变更是否应进入 release。
- 本地环境不是生产环境；数据库实表结构、生产 settings、Redis/cache、宝塔调度、插件启用表、文件权限、CA bundle 均需要上线前环境核查。
- `php artisan route:list --json` 只能证明路由中间件挂载，不能代替真实权限矩阵与角色数据验证。
- Sentry 真实投递未在本地验证，因为本地未配置生产 DSN；上线前必须用生产或准生产 DSN 跑 `php artisan sentry:test` 并确认事件、trace_id、敏感字段脱敏策略。
- PHPStan baseline 当前记录 1739 个既有错误；本轮恢复了门禁，但后续治理应逐步缩小 baseline。

## 后续整改顺序

1. **上线前环境核验**：生产 settings、插件启用状态、supplier/product/service provider 分布、Redis/cache/queue、schedule runtime、CA bundle、错误追踪。
2. **补生产韧性**：加入插件/provider preflight、schedule runtime config test、Job timeout 静态回归。
3. **补业务组合测试**：提现并发/审批幂等、自动续费重放、支付回调异常组合、调度锁降级。
4. **P2 纳入维护债**：逐步缩小 PHPStan baseline、持续收敛 PII 日志暴露面、完善危险恢复命令的运维审批流程。
