# 代码规范化执行记录

- 文档性质：治理执行记录
- 执行日期：`2026-06-30`
- 当前分支：`fix/cleanup-and-ticket-features`
- 执行计划：`文档/开发文档/治理/全栈代码规范化工程化治理计划.md`
- 覆盖矩阵：`文档/开发记录/治理/代码规范化覆盖矩阵-20260630.md`

## 1. 执行状态

| Task | 状态 | 记录 |
| --- | --- | --- |
| Task 1 冻结基线与保护脏工作区 | 已完成 | 已记录本轮首次 `git status --short` 摘要和保护清单 |
| Task 2 建立全仓源码覆盖矩阵 | 已完成 | 已生成逐文件覆盖矩阵；当前文件、Git 删除中的指定根文件和源码根内资产均已逐行纳入，集合校验漏项 0 |
| Task 3 后端 API 契约审计 | 已完成 | 已完成搜索审计，确认裸 JSON 命中多为统一响应构造器、health、文件/回调例外 |
| Task 4 后端请求入口治理 | 已完成 | 已将供应商与会员等级写入口迁移到 FormRequest |
| Task 5 后端响应与敏感字段治理 | 已完成 | 已补供应商 `api_key` 空值保留与响应不泄露回归测试 |
| Task 6 后端服务层结构治理 | 已完成 | 已复核 Service 落点、第三方调用和临时命名，未做稳定命名空间整体迁移 |
| Task 7 三端请求层统一审计 | 已完成 | 未新增 axios 实例；修复 www/v4 对后端 `data.errors` 的 422 字段错误读取 |
| Task 8 前端 API 类型与 normalizer 收敛 | 已完成 | 字段错误提取收敛到 shared 纯函数，不重写 JS 项目结构 |
| Task 9 shared 复用治理 | 已完成 | 新增 shared runtime `extractValidationErrors` 并补测试 |
| Task 10 页面与领域结构治理 | 已完成 | 修复 v4 服务中心固定 px 断点，其他页面结构候选只记录不机械迁移 |
| Task 11 必要注释与文档同步 | 已完成 | 已更新治理计划、逐文件覆盖矩阵和本执行记录 |
| Task 12 最终验收与剩余风险清单 | 已完成 | 已运行最终验收命令并登记剩余风险 |

## 2. Task 1：冻结基线与保护脏工作区

### 2.1 基线摘要

本轮首次执行 `git status --short` 时，工作区已有 67 个非 clean 条目。新增计划文档和覆盖矩阵属于本轮治理产物，不计入用户已有改动。

| 分类 | 数量 | 处理策略 |
| --- | ---: | --- |
| 既有源码修改/新增/删除 | 25 | 保护，不回滚，不覆盖；确需改同文件时先逐段核对 diff |
| 迁移输出、审计报告、根级临时文件删除项 | 42 | 保护，不恢复，不清理，不纳入本轮源码治理 |
| 本轮新增治理文档 | 2 | 仅用于计划与覆盖记录；执行记录为本文件 |

### 2.2 受保护源码改动

以下路径在本轮开始前已处于非 clean 状态，默认视为用户已有改动：

```text
M  backend/app/Http/Controllers/Admin/MediaFileController.php
M  backend/app/Http/Requests/Admin/MediaFile/StoreRequest.php
M  backend/app/Services/Content/HomeHeroService.php
M  backend/app/Services/Content/MediaFileService.php
M  backend/tests/Feature/AdminHomeHeroControllerTest.php
M  backend/tests/Feature/UploadSecurityTest.php
M  frontend-admin-v3/src/api/admin/content.ts
M  frontend-admin-v3/src/api/admin/types.ts
M  frontend-admin-v3/src/pages/settings/index.less
M  frontend-admin-v3/src/pages/settings/index.vue
M  frontend-admin-v3/src/router/modules/admin/content.ts
M  frontend-admin-v3/src/store/modules/permission.ts
D  frontend-user-v3-www/src/pages/website/products/machineSpecOptions.js
D  frontend-user-v3-www/src/pages/website/products/machineSpecResolver.js
D  frontend-user-v3-www/src/pages/website/products/useWebsiteProductCheckout.js
D  frontend-user-v3-www/src/pages/website/products/useWebsiteProductConfigurator.js
D  frontend-user-v3-www/src/pages/website/products/useWebsiteProductsCatalog.js
D  frontend-user-v3-www/src/pages/website/products/useWebsiteProductsPage.js
D  frontend-user-v3-www/src/pages/website/products/useWebsiteProductsPurchase.js
?? backend/app/Console/Commands/OrganizeMediaUploadsCommand.php
?? backend/app/Services/Content/MediaLibraryOrganizer.php
?? backend/tests/Feature/AdminMediaLibraryControllerTest.php
?? backend/tests/Feature/MediaOrganizeUploadsCommandTest.php
?? frontend-admin-v3/src/pages/content/media-library.less
?? frontend-admin-v3/src/pages/content/media-library.vue
```

### 2.3 受保护非源码改动

以下删除项属于迁移输出、历史安全审计报告或根级临时产物，本轮不会恢复或清理：

```text
D  migration-output/idc_backup_before_legacy_migration_20260524_225639.sql
D  migration-output/idc_backup_probe.sql
D  migration-output/paymentservice_head.php
D  migration-output/paymentservice_index.php
D  migration-output/preserve-backups-20260419-005158/content_articles.json
D  migration-output/preserve-backups-20260419-005158/content_categories.json
D  migration-output/preserve-backups-20260419-005158/email_logs.json
D  migration-output/preserve-backups-20260419-005158/notification_logs.json
D  migration-output/preserve-backups-20260419-005158/settings.json
D  migration-output/preserve-backups-20260419-005158/sms_logs.json
D  migration-output/preserve-backups-20260419-030302/content_articles.json
D  migration-output/preserve-backups-20260419-030302/content_categories.json
D  migration-output/preserve-backups-20260419-030302/email_logs.json
D  migration-output/preserve-backups-20260419-030302/notification_logs.json
D  migration-output/preserve-backups-20260419-030302/settings.json
D  migration-output/preserve-backups-20260419-030302/sms_logs.json
D  migration-output/write-test.txt
D  security-report/20260627/evidence/auth-dynamic-matrix.json
D  security-report/20260627/evidence/idor-and-sensitive-checks.json
D  security-report/20260627/fix-summary.md
D  security-report/20260627/logs/admin-settings-secret-check.json
D  security-report/20260627/logs/auth-dynamic-summary.md
D  security-report/20260627/logs/auth-dynamic-tests.json
D  security-report/20260627/logs/route-list-admin.txt
D  security-report/20260627/logs/route-list-client.txt
D  security-report/20260627/reports/dev-repro-checklist.md
D  security-report/20260627/reports/remediation-list.md
D  security-report/20260627/reports/security-test-report.md
D  tmp-check-icons.cjs
D  tmp-hero-after.png
D  tmp-hero-before.png
D  tmp-hero-check.png
D  tmp-idcmax-home.png
D  tmp-idcmax-info.json
D  tmp-seo-cloud-desktop-wait.png
D  tmp-seo-cloud-desktop.png
D  tmp-seo-cloud-mobile.png
D  tmp-seo-cloud-style.json
D  tmp-seo-preview-check.json
D  tmp-www-browser-check.png
D  tmp-www-large-current.png
D  tmp-www-security-current.png
```

## 3. Task 2：全仓源码覆盖矩阵

### 3.1 扫描根

覆盖范围按治理计划执行：

- `backend/app`
- `backend/routes`
- `backend/tests`
- `frontend-admin-v3/src`
- `frontend-admin-v3/tests`
- `frontend-admin-v3/scripts`
- `frontend-user-v3-www/src`
- `frontend-user-v3-www/tests`
- `frontend-user-v3-www/scripts`
- `frontend-user-v4-console/src`
- `frontend-user-v4-console/tests`
- `frontend-user-v4-console/scripts`
- `shared`

### 3.2 覆盖结果

| 指标 | 数量 |
| --- | ---: |
| 指定根当前 `rg --files` 文件数 | 1254 |
| 指定根 Git 删除文件数 | 7 |
| 纳入逐文件矩阵条目数 | 1261 |
| 当前 Git 非 clean 覆盖对象数 | 40 |
| 启发式候选和已治理复核文件数 | 318 |
| 本轮已治理源码文件数 | 15 |
| 逐文件漏项 | 0 |

本次复查将 3 个源码根内 PNG 资产和 7 个用户已有删除状态的 www 源码也纳入矩阵逐行结论；删除项保持删除状态，仅按 `HEAD:<path>` 读取可用历史内容做风险标记，不恢复、不覆盖。

### 3.3 矩阵验证

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 指定根枚举 | `rg --files <指定根>` | 当前文件数 1254 |
| Git 删除项纳入 | `git status --porcelain=v1 --untracked-files=all` 映射指定根 | 7 个已删除 www 源码纳入矩阵且标记为保护删除 |
| 逐文件矩阵生成 | 覆盖矩阵生成脚本按当前文件 + 指定根删除项打标 | 矩阵条目数 1261 |
| 逐文件漏项反查 | 覆盖对象集合与矩阵路径集合对比 | `coverage_subjects = 1261`、`matrix_rows = 1261`、`missing_rows = 0`、`extra_rows = 0` |

## 4. Task 3-6：后端第一批治理

### 4.1 审计命中与处置

| 审计项 | 命令 | 结论 |
| --- | --- | --- |
| 裸 JSON / 非统一响应 | `rg -n "response\\(\\)->json|return response\\(|->json\\(" backend/app backend/routes` | 命中主要是 `ApiResponseBuilder`、`routes/api.php` health、文件响应、支付回调和中间件纯文本响应；本批不改 |
| Controller 请求读取/校验 | `rg -n "\\$request->(validate|input|query|get)\\(" backend/app/Http/Controllers` | `Admin\\SupplierController` 存在 `$request->validate()`，且涉及供应商凭据，高优先级治理 |
| 直接 `Http::` 调用 | `rg -n "\\bHttp::" backend/app/Http/Controllers backend/app/Services backend/app/Integrations` | 未发现 Controller 直接调用；命中位于 Service/Transport 层，本批不改 |
| `per_page` 兼容 | `rg -n "\\bper_page\\b" backend/app backend/routes` | 命中集中在既有兼容请求和日志服务；本批记录，暂不破坏兼容 |

### 4.2 已修改文件

| 文件 | 目的 |
| --- | --- |
| `backend/app/Http/Requests/Admin/Supplier/IndexRequest.php` | 新增供应商列表查询 FormRequest，统一 `page_size`、`keyword`、`status` 校验 |
| `backend/app/Http/Requests/Admin/Supplier/UpsertRequest.php` | 新增供应商创建/更新 FormRequest，承载 provider key 归一化、URL 安全校验、空敏感字段保留 |
| `backend/app/Http/Controllers/Admin/SupplierController.php` | 移除控制器内 `$request->validate()` 和请求归一化私有逻辑，改用请求对象 |
| `backend/tests/Feature/SupplierInterfaceTypeAliasRegressionTest.php` | 补充供应商更新时空 `api_key` 不覆盖旧值、响应不返回 `api_key` 的回归测试 |

### 4.3 验证结果

| 命令 | 结果 |
| --- | --- |
| `cd backend && php artisan test --filter=SupplierInterfaceTypeAliasRegressionTest` | 通过，3 tests / 14 assertions |
| `cd backend && php -l app/Http/Requests/Admin/Supplier/UpsertRequest.php && php -l app/Http/Requests/Admin/Supplier/IndexRequest.php && php -l app/Http/Controllers/Admin/SupplierController.php` | 通过，无语法错误 |
| `cd backend && php vendor\\bin\\pint --test app/Http/Controllers/Admin/SupplierController.php app/Http/Requests/Admin/Supplier/IndexRequest.php app/Http/Requests/Admin/Supplier/UpsertRequest.php tests/Feature/SupplierInterfaceTypeAliasRegressionTest.php` | 通过 |
| `rg -n "\\$request->validate\\(" backend/app/Http/Controllers/Admin/SupplierController.php` | 通过，无命中 |

### 4.4 后端第二批：会员等级请求入口修复

审计发现 `Admin\\MemberLevelController` 的 `store` / `update` 调用 `validatedPayload()`，但该方法只剩注释且声明返回 `array`，实际会在创建会员等级时返回 `null` 并触发 500。

| 文件 | 目的 |
| --- | --- |
| `backend/app/Http/Requests/Admin/MemberLevel/SaveRequest.php` | 新增会员等级保存 FormRequest，承载创建/更新字段校验 |
| `backend/app/Http/Requests/Admin/MemberLevel/DestroyRequest.php` | 改为兼容别名，避免重复维护错误命名类里的保存规则 |
| `backend/app/Http/Controllers/Admin/MemberLevelController.php` | store/update 改用 `SaveRequest::validated()`，删除空 `validatedPayload()` |
| `backend/tests/Feature/AdminMemberLevelControllerTest.php` | 增加接口级红绿测试，覆盖创建和更新会员等级 |

验证过程：

| 命令 | 结果 |
| --- | --- |
| `cd backend && php artisan test --filter=AdminMemberLevelControllerTest`（修复前） | 失败，500，`validatedPayload(): Return value must be of type array, none returned` |
| `cd backend && php artisan test --filter=AdminMemberLevelControllerTest`（修复后） | 通过，1 test / 7 assertions |
| `cd backend && php vendor\\bin\\pint --test app/Http/Controllers/Admin/MemberLevelController.php app/Http/Requests/Admin/MemberLevel/SaveRequest.php app/Http/Requests/Admin/MemberLevel/DestroyRequest.php tests/Feature/AdminMemberLevelControllerTest.php` | 通过 |
| `cd backend && php artisan test tests/Feature/SupplierInterfaceTypeAliasRegressionTest.php tests/Feature/AdminMemberLevelControllerTest.php` | 通过，4 tests / 21 assertions |

### 4.5 Task 6：后端服务层结构复核

| 审计项 | 命令 | 结论 |
| --- | --- | --- |
| Controller 直调第三方 HTTP | `rg -n "\\bHttp::" backend/app/Http/Controllers backend/app/Services backend/app/Integrations` | 未发现 Controller 直接调用；命中均在 Service / Transport 层 |
| Payment 物理删除 | `rg -n "Payment::.*delete|payments?->delete|DB::table(...payments...).*delete|...truncate..." backend/app backend/routes backend/tests` | 无命中 |
| 临时/版本化服务命名 | `rg -n "class .*(_v2|V2|New)|_v2|\\.new|New[A-Z].*Service" backend/app/Services backend/app/Http/Controllers backend/app/Http/Requests` | 仅命中既有 `NewCustomerDailySummaryRequest`，不是新增 Service 临时命名，本批不改 |
| Service 直接返回 HTTP Response | `rg -n "return .*Response|JsonResponse|Illuminate\\\\Http\\\\Response|Symfony\\\\Component\\\\HttpFoundation\\\\Response" backend/app/Services` | 命中为业务变量名或上游响应数组处理，未发现 Service 返回 HTTP Response |

结论：本轮不做稳定服务命名空间整体迁移；新增/调整的后端文件已按现有 `Requests/Admin/<Domain>` 与 `Services/Content` 领域落点。

## 5. Task 7-10：前端与 shared 治理

### 5.1 审计命中与处置

| 审计项 | 命令 | 结论 |
| --- | --- | --- |
| axios 实例 | `rg -n "axios\\.create|import\\s+axios|from ['\\\"]axios" frontend-* shared` | 命中集中在既有请求层和 shared runtime，未发现业务页新增 axios 实例 |
| storage 访问 | `rg -n "localStorage|sessionStorage" ...` | 命中主要是 session/runtime、路由重载保护、网站购买暂存和 locale 偏好；本批不机械迁移 |
| 422 字段错误 | `rg -n "data\\.errors|\\.errors"` | www/v4 请求层只读取顶层 `errors`，与后端标准 `data.errors` 不一致，已修复 |
| shared 复用 | 对比 www/v4 请求层重复逻辑 | 将字段错误提取收敛到 `shared/runtime/http/validationErrors.ts` |
| v4 refactor 门禁 | `cd frontend-user-v4-console && npm.cmd run verify:refactor` | 发现 `useServiceCenter.ts` 固定 `768px` 断点；已改为等价 `48rem` 查询常量 |

### 5.2 已修改文件

| 文件 | 目的 |
| --- | --- |
| `shared/runtime/http/validationErrors.ts` | 新增纯函数，兼容后端标准 `data.errors` 和历史顶层 `errors` |
| `shared/runtime/index.ts` | 从 `@caiwu/shared/runtime` 导出字段错误提取能力 |
| `shared/tests/http-validation.test.mjs` | 覆盖标准结构、历史结构和空结构 |
| `shared/package.json` | 将新测试纳入 `npm run test:shared` |
| `frontend-user-v3-www/src/app/http/request.ts` | HTTP 422 时读取 `data.errors` 字段错误，保留中文可信提示过滤 |
| `frontend-user-v4-console/src/utils/request.ts` | 同步 v4 console 请求层字段错误提取逻辑 |
| `frontend-user-v4-console/src/domains/services/useServiceCenter.ts` | 将服务中心移动端视图恢复断点从固定 `768px` 改为 `48rem` 查询常量，通过 v4 refactor 门禁 |

### 5.3 验证结果

| 命令 | 结果 |
| --- | --- |
| `npm.cmd run typecheck:shared` | 通过 |
| `npm.cmd run test:shared` | 通过，`runtime` / `sanitizer` / `http-validation` 均通过 |
| `cd frontend-user-v4-console && npm.cmd run build` | 通过，存在 Vite/Rollup 第三方注释警告，不影响构建 |
| `cd frontend-user-v3-www && npm.cmd run build` | 通过，已生成 sitemap 和 prerender 产物；未新增 tracked dist 状态 |
| `cd frontend-user-v3-www && npm.cmd run verify:refactor` | 通过，内部执行 build、sitemap 与 prerender；出现 Node `DEP0190` 和第三方注释警告，不影响结果 |
| `cd frontend-user-v4-console && npm.cmd run verify:refactor` | 修复前失败于固定 `768px`；修复后通过 |
| `npm.cmd run build:frontends` | 通过，已在 v4 断点修复后重新执行；admin、www、console 均构建通过 |

## 6. 验证记录

| 阶段 | 命令 | 结果 | 说明 |
| --- | --- | --- | --- |
| Task 1 | `git status --short` | 通过 | 已记录基线并分类 |
| Task 2 | `rg --files` 覆盖根扫描 | 通过 | 已生成矩阵并记录数量 |
| Task 3-5 后端第一批 | 见上方验证表 | 通过 | 供应商请求入口治理与敏感字段回归 |
| Task 4 后端第二批 | 见上方验证表 | 通过 | 会员等级写入口 500 修复与 FormRequest 收敛 |
| Task 7-10 前端/shared | 见上方验证表 | 通过 | www/v4 422 字段错误契约收敛到 shared |
| Task 12 后端完整测试 | `cd backend && php artisan test` | 通过 | 486 passed / 3 skipped / 2818 assertions |
| Task 12 后端局部格式 | `cd backend && php vendor\\bin\\pint --test <本轮后端文件>` | 通过 | 覆盖供应商、会员等级 Controller/FormRequest/Test |
| Task 12 后端全量格式 | `cd backend && php vendor\\bin\\pint --test` | 未通过 | 失败文件均不属于本轮后端治理改动，详见剩余风险 |
| Task 12 shared | `npm.cmd run typecheck:shared && npm.cmd run test:shared` | 通过 | runtime / sanitizer / http-validation 测试通过 |
| Task 12 前端聚合构建 | `npm.cmd run build:frontends` | 通过 | v4 断点修复后重新执行；admin、www、console 均通过 |
| Task 12 前端 refactor 门禁 | `cd frontend-user-v3-www && npm.cmd run verify:refactor`; `cd frontend-user-v4-console && npm.cmd run verify:refactor` | 通过 | www 通过；v4 修复固定 px 后通过 |

### 6.1 再检查追加验证

| 验证项 | 命令 | 结果 | 说明 |
| --- | --- | --- | --- |
| 逐文件矩阵漏项反查 | 覆盖对象集合与矩阵路径集合对比 | 通过 | `coverage_subjects = 1261`、`matrix_rows = 1261`、`missing_rows = 0`、`extra_rows = 0` |
| 后端完整测试第一次复跑 | `cd backend && php artisan test` | 超时 | 124 秒超时，无完整测试摘要 |
| 后端完整测试第二次复跑 | `cd backend && php artisan test` | 未通过 | 394 passed / 92 failed / 3 skipped；失败集中在共享 `idc_testing` 测试库临时结构状态 |
| 测试库结构只读确认 | `cd backend && php artisan tinker --execute=... --env=testing` | 通过 | 确认 `idc_testing`、`users`、`user_coupons.uid`、`settings` 存在 |
| 首个失败文件单独复跑 | `cd backend && php artisan test tests/Feature/AdminInvoiceNotificationRegressionTest.php` | 通过 | 2 tests / 10 assertions |
| 本轮后端相关测试复跑 | `cd backend && php artisan test tests/Feature/SupplierInterfaceTypeAliasRegressionTest.php tests/Feature/AdminMemberLevelControllerTest.php tests/Feature/AdminMediaLibraryControllerTest.php tests/Feature/MediaOrganizeUploadsCommandTest.php` | 通过 | 6 tests / 34 assertions |
| 后端完整测试最终复跑 | `cd backend && php artisan test` | 通过 | 486 passed / 3 skipped / 2818 assertions |
| 后端本轮文件 Pint | `cd backend && php vendor\\bin\\pint --test <本轮后端文件>` | 通过 | 返回 `{"result":"pass"}` |
| 后端全量 Pint | `cd backend && php vendor\\bin\\pint --test` | 未通过 | 失败文件不属于本轮新增治理范围，详见剩余风险 |
| shared 类型检查 | `npm.cmd run typecheck:shared` | 通过 | `tsc -p tsconfig.json --noEmit` 通过 |
| shared 测试 | `npm.cmd run test:shared` | 通过 | runtime / sanitizer / http-validation 均通过 |
| 三端聚合构建 | `npm.cmd run build:frontends` | 通过 | admin、www、console 均通过；存在第三方 Rollup 注释警告 |
| www refactor 门禁 | `cd frontend-user-v3-www && npm.cmd run verify:refactor` | 通过 | build、sitemap、prerender 通过；存在 Node `DEP0190` 与第三方 Rollup 注释警告 |
| v4 refactor 门禁 | `cd frontend-user-v4-console && npm.cmd run verify:refactor` | 通过 | `verify:refactor passed`；存在 npm workspace config 提示 |

### 6.2 再检查横切扫描

| 审计项 | 命令 | 结论 |
| --- | --- | --- |
| Controller 直调第三方 HTTP | `rg -n "\\bHttp::" backend/app/Http/Controllers backend/app/Services backend/app/Integrations` | 仅命中 `Services/Auth`、`Services/PaymentGateway`、`Services/Security`、`Services/Upstream` 等服务/传输层，未命中 Controller |
| Payment 物理删除 | `rg -n "Payment::.*delete...|payments...delete..." backend/app backend/routes backend/tests` | 无命中 |
| axios 实例边界 | `rg -n "axios\\.create|import\\s+axios|from\\s+['\\\"]axios['\\\"]" frontend-admin-v3/src frontend-user-v3-www/src frontend-user-v4-console/src shared` | 命中均为既有请求层、shared runtime 或类型声明；未发现业务页新增 axios 实例 |
| UI 框架混用 | `rg -n "element-plus|@element-plus" frontend-admin-v3/src frontend-user-v4-console/src`；`rg -n "tdesign-vue-next|tdesign-icons" frontend-user-v3-www/src` | 均无命中 |
| 后端响应例外 | `rg -n "response\\(\\)->json|return\\s+response\\s*\\(|->json\\s*\\(" backend/app backend/routes` | 命中为 `ApiResponseBuilder`、health、文件响应、回调纯文本/HTML、服务内响应体解析等既有例外；本轮不改 |

## 7. 已修复问题

| 问题 | 涉及文件 | 结果 |
| --- | --- | --- |
| `Admin\\SupplierController` 在控制器内完成供应商校验、接口类型归一化和敏感字段保留逻辑 | `backend/app/Http/Controllers/Admin/SupplierController.php`、`backend/app/Http/Requests/Admin/Supplier/*Request.php` | 已迁移到 FormRequest，控制器改用 `$request->validated()` / `payload()` |
| 供应商更新时空 `api_key` 可能覆盖旧凭据，且响应需要持续证明不返回原始 `api_key` | `backend/tests/Feature/SupplierInterfaceTypeAliasRegressionTest.php` | 已补回归测试，验证空值保留和响应白名单 |
| `Admin\\MemberLevelController::validatedPayload()` 声明返回 `array` 但实际无返回，创建/更新会触发 500 | `backend/app/Http/Controllers/Admin/MemberLevelController.php`、`backend/app/Http/Requests/Admin/MemberLevel/SaveRequest.php`、`backend/tests/Feature/AdminMemberLevelControllerTest.php` | 已改用 `SaveRequest` 并补接口测试 |
| www / v4 请求层只读取历史顶层 `errors`，无法完整消费后端标准 `data.errors` | `shared/runtime/http/validationErrors.ts`、`frontend-user-v3-www/src/app/http/request.ts`、`frontend-user-v4-console/src/utils/request.ts` | 已收敛到 shared 纯函数并覆盖测试 |
| v4 服务中心存在固定 `768px` 断点，触发 refactor 门禁 | `frontend-user-v4-console/src/domains/services/useServiceCenter.ts` | 已改为 `48rem` 查询常量，`verify:refactor` 通过 |

## 8. 当前剩余风险

| 风险 | 等级 | 处理计划 |
| --- | --- | --- |
| 工作区已有源码改动较多，后续若修同文件存在覆盖风险 | 高 | 默认避开；确需触碰时先读取 diff 并只追加最小必要改动 |
| 全量 `php vendor\\bin\\pint --test` 仍失败 | 中 | 失败文件均不属于本轮治理改动：`WarmupSiteCache.php`、`Client/OrderController.php`、`LoginByCodeRequest.php`、`Client/Order/IndexRequest.php`、`ServiceDetailService.php`、`CouponService.php`、`HandlesProductCatalogHelpers.php`、`ProductFullPathResolver.php`、`UploadedAssetReferenceService.php`、`CacheKey.php`、`2026_06_19_195114_add_performance_indexes_for_site_queries.php`、`OrderSnapshotSemanticsTest.php`、`backend/tmp_verify.php`；后续单独清理，禁止本轮全仓格式化 |
| 后端完整测试对共享 MySQL 测试库状态敏感 | 低 | 本轮出现一次超时后复跑失败，随后单文件复跑和最终完整复跑通过；后续长测中断后建议先单跑首个失败文件或重建测试库状态再下结论 |
| 部分 Controller 仍有直接 `$request->input/query` 分页或简单查询读取 | 中 | 已在矩阵候选中登记；后续按业务域逐批迁移 FormRequest，不机械一次性改动 |
| TS 项目仍存在历史 `any/as any` 热点 | 中 | 已在矩阵候选中登记；优先处理请求层、领域 normalizer 和高频财务/服务页面 |
| 前端 www 有 7 个源码删除项 | 中 | 已按 `HEAD:<path>` 纳入覆盖矩阵并标记为保护删除；不重建、不恢复；如后续引用断裂，仅报告风险 |
| Vite/Rollup 构建存在第三方注释警告，www verify 存在 Node `DEP0190` 警告，v4 verify 存在 npm workspace config 提示 | 低 | 构建和门禁结果均通过；警告来自依赖/脚本调用方式或 workspace 配置读取，后续可单独治理 |
