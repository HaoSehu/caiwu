# 工作区深度清洁盘点

- 文档性质：治理执行记录
- 执行日期：`2026-06-23`
- 当前分支：`fix/cleanup-and-ticket-features`
- 现场快照：`C:\Users\Admin\Desktop\caiwu-local-artifacts\workspace-cleanup-20260623-173142`

## 1. 本次已执行范围

本次完成现场保护、盘点记录和低风险物理移动。未执行删除、`git rm --cached`、回滚、提交或业务代码修改。

已生成的仓库外快照包括：

- `git-status-short.txt`
- `git-status-porcelain.txt`
- `git-diff-stat.txt`
- `git-diff-name-status.txt`
- `git-untracked.txt`
- `tracked-testing-sqlite.txt`
- `tracked-root-tools.txt`
- `root-list.txt`
- `root-largest-files.txt`
- `dirty-worktree.patch`

## 2. 当前工作区事实

### 2.1 执行前 Git 状态摘要

| 指标 | 数量 |
| --- | ---: |
| 工作区总条目 | 1013 |
| 已修改 | 538 |
| 已删除 | 154 |
| 未跟踪 | 321 |
| 已跟踪 diff 文件数 | 570 |
| 插入行 | 14308 |
| 删除行 | 58435 |

### 2.2 变更集中目录

| 目录或分组 | 条目数 | 初步判断 |
| --- | ---: | --- |
| `backend` | 549 | 后端业务、迁移、测试、运行产物混杂 |
| `frontend-user-v3-www` | 131 | 官网/旧用户入口改动 |
| `frontend-user-v4-console` | 107 | 新用户控制台改动 |
| 根目录或被引号转义路径 | 103 | 文档、中文路径、根级文件混杂 |
| `.github` | 36 | agent/技能/规则相关新增 |
| `frontend-admin-v3` | 31 | 管理端改动 |
| `.agents` | 30 | agent 技能库相关新增 |
| `shared` | 12 | 共享层改动 |
| `.codex` | 9 | Codex 技能相关改动 |

### 2.3 根目录高风险文件

| 文件 | 大小 | 初步处理 |
| --- | ---: | --- |
| `mysql_idc_filtered.sql` | 60606646 | 已移出仓库根目录，归档到本地 artifacts |
| `mysql_idc_20260612212623.sql` | 60050224 | 已移出仓库根目录，归档到本地 artifacts |
| `mysql_idc_20260612212623_stripped.sql` | 60050163 | 已移出仓库根目录，归档到本地 artifacts |
| `php.bat` | 128 | 已移出仓库根目录，归档到本地 artifacts |
| `png_to_svg.py` | 26652 | 已移出仓库根目录，归档到本地 artifacts |
| `theme.css` | 23514 | 现役跨端主题资产，暂保留根目录 |

### 2.4 已完成的物理移动

| 来源 | 目标 |
| --- | --- |
| `mysql_idc_filtered.sql` | `C:\Users\Admin\Desktop\caiwu-local-artifacts\database-dumps\2026-06-23\mysql_idc_filtered.sql` |
| `mysql_idc_20260612212623.sql` | `C:\Users\Admin\Desktop\caiwu-local-artifacts\database-dumps\2026-06-23\mysql_idc_20260612212623.sql` |
| `mysql_idc_20260612212623_stripped.sql` | `C:\Users\Admin\Desktop\caiwu-local-artifacts\database-dumps\2026-06-23\mysql_idc_20260612212623_stripped.sql` |
| `zjmf-manger-decoded-main/` | `C:\Users\Admin\Desktop\caiwu-local-artifacts\third-party-source\zjmf-manger-decoded-main\` |
| `php.bat` | `C:\Users\Admin\Desktop\caiwu-local-artifacts\local-tools\2026-06-23\php.bat` |
| `png_to_svg.py` | `C:\Users\Admin\Desktop\caiwu-local-artifacts\local-tools\2026-06-23\png_to_svg.py` |

### 2.5 Git 索引污染

`backend/storage/framework/testing/*.sqlite` 当前有 220 个文件被 Git 跟踪。该目录属于测试运行产物位置，不应默认纳入版本管理。

引用检查结论：

- `billing-automation-*`、`service-lifecycle-*` 为历史测试运行产物，当前测试已改用 SQLite 内存库；
- `identity-user-accounts-*.sqlite`、`reconcile-account-domain-*.sqlite`、`reconcile-identity-domain-*.sqlite` 会在对应测试执行前 `unlink` 并 `touch` 重建；
- `idc_v2-refactor-schema.sqlite` 未发现代码引用。

本次已将 220 个 sqlite 文件移到仓库外归档目录：

`C:\Users\Admin\Desktop\caiwu-local-artifacts\testing-sqlite\2026-06-23`

验证结果：

- `IdentityUserAccountsMigrationCommandTest` 通过；
- `ReconcileAccountDomainCommandTest` 通过；
- `ReconcileIdentityDomainCommandTest` 通过。

上述测试会重建 6 个固定命名 sqlite。验证完成后，已将这 6 个新生成文件移到：

`C:\Users\Admin\Desktop\caiwu-local-artifacts\testing-sqlite\2026-06-23-after-test`

后续提交时应将这些文件作为删除项纳入清理提交，并保持 `.gitignore` 中的 `backend/storage/framework/testing/*.sqlite` 规则。

### 2.6 换行符风险

`git diff --shortstat` 输出大量 `LF will be replaced by CRLF` 警告。当前 `.editorconfig` 已要求 `end_of_line = lf`。本次已新增根级 `.gitattributes`，用于统一后续文本换行符和二进制文件处理规则，但未对现有文件做批量重写。

### 2.7 根目录已跟踪文件判定

| 文件 | 引用检查 | 判定 | 下一步 |
| --- | --- | --- | --- |
| `php.bat` | 未发现项目代码引用 | 本机 PHP shim，且 `.gitignore` 已包含 `php.bat` | 已移入本地 artifacts，后续作为删除项提交 |
| `png_to_svg.py` | 未发现外部引用，仅脚本内部自引用 | 一次性图片转换工具，含硬编码本机路径 | 已移入本地 artifacts，后续作为删除项提交 |
| `theme.css` | `frontend-admin-v3/src/main.ts` 与 `frontend-user-v4-console/src/style/index.less` 直接引用 | 现役跨端主题资产，不是临时文件 | 暂保留根目录；若后续迁移，需要同步更新 import 并执行前端构建 |
| `zjmf-manger-decoded-main/` | 部分文档引用旧系统源码路径 | 已移出仓库根目录 | 后续将文档引用改为“本地归档参考源码”，避免误认为仓库内目录 |

### 2.8 环境文件分类

已检查以下前端环境文件的键名和值类型，未在输出中暴露任何实际值：

- `frontend-user-v3-www/.env.production`
- `frontend-user-v3-www/.env.production.example`
- `frontend-user-v4-console/.env`
- `frontend-user-v4-console/.env.development`

结论：

- 当前键名均为 `VITE_*` 公共构建变量，未发现 password、token、secret、private key 类真实密钥键；
- 值类型主要为 URL、路径、布尔值和普通文本，属于会进入前端构建的公开配置范畴；
- 不在本批次移除现有 tracked Vite 环境文件，避免破坏现有构建约定；
- 已在 `.gitignore` 中新增前端 `.env.local` / `.env.*.local` 忽略规则，用于承载后续本地覆盖和敏感环境差异。

## 3. 清理分组

### 3.1 保留并分组提交

- 后端真实业务代码与测试。
- `frontend-admin-v3` 管理端真实改动。
- `frontend-user-v3-www` 官网/旧用户入口真实改动。
- `frontend-user-v4-console` 新控制台真实改动。
- `shared` 共享层真实改动。
- 经过确认的项目规则、agent 配置、文档治理规则。

### 3.2 移出仓库或本地归档

- 根目录 SQL dump。
- 第三方源码副本。
- 临时脚本、临时样式、一次性转换工具。
- 运行日志、测试输出、构建产物。

### 3.3 停止 Git 跟踪候选

- `backend/storage/framework/testing/*.sqlite`（已移出仓库，待作为删除项提交）
- 真实 `.env` 或本地环境配置（本批已补 `.env.local` 忽略规则，tracked Vite 公共配置暂保留）。
- 本机专用 shim，例如 `php.bat`。

### 3.4 需人工或后续任务确认

- `.agents`、`.github/skills`、`.codex/skills` 是否需要多源并存。
- `png_to_svg.py` 是否仍是正式维护脚本。
- `theme.css` 是否属于正式视觉资产。
- 大量文档删除是否全部对应到新路径。

## 4. 下一批执行队列

### Task 1：根目录物理清理

目标：移走不应留在仓库根目录的本地大文件和临时资产。

当前状态：根目录 SQL dump、`zjmf-manger-decoded-main/`、`php.bat`、`png_to_svg.py` 已移出仓库；`theme.css` 判定为现役资产，暂不移动。

验收：

- 根目录不再出现 `mysql_*.sql`。
- 第三方源码副本不再占据仓库根目录。
- 根目录临时工具全部有明确去向。
- `git status --short` 中不出现新增的根目录临时文件。

### Task 2：Git 索引清理方案验证

目标：判断 220 个 tracked sqlite 是否可以停止跟踪。

当前状态：已确认这些文件是可再生测试运行产物，并已移入仓库外归档目录。

验收：

- 明确 sqlite 文件是否为 fixture。
- 若不是 fixture，执行停止跟踪方案前有测试验证计划。
- `.gitignore` 与后续 `.gitattributes` 策略一致。

### Task 3：换行符治理

目标：增加根级 `.gitattributes`，减少 CRLF/LF 噪音。

当前状态：已新增根级 `.gitattributes`，覆盖文本默认 LF、Windows 脚本 CRLF、常见图片/字体/压缩包/SQLite 二进制规则，以及 SQL 禁用文本 diff。

验收：

- `git diff --check` 通过。
- 清理提交不包含无关大面积换行符重写。
- 二进制文件与 SQL 文件有明确属性。

### Task 4：agent 配置单源化

目标：避免 `.agents`、`.github`、`.codex` 多处重复维护同一规则。

验收：

- 明确项目规则源头。
- 重复技能或引用文件有处理结论。
- 后续代理读取路径与 `AGENTS.md` 一致。

### Task 5：按领域拆分业务改动

目标：把当前大工作区拆成可验证的后端、前端、shared、文档提交。

验收：

- 每组变更有独立验证命令。
- 每组提交只包含一个领域。
- 不再用一个全仓 diff 承载所有工作。

## 5. 禁止动作

- 禁止使用 `git reset --hard`。
- 禁止直接删除未分类文件。
- 禁止把清理、业务修复、格式化混进同一提交。
- 禁止手工编辑自动生成的后端 API 清单。
- 禁止补跑历史激进迁移文件。

## 6. 当前结论

工作区已经完成现场保护，并已移走根目录低风险大文件、第三方源码副本和已判定的一次性工具文件。tracked sqlite 已确认可再生并移入仓库外归档。前端环境文件已完成安全分类，现有 tracked Vite 公共配置暂保留，并已补充本地 override 忽略规则。下一步可以进入文档删除映射和 agent 配置单源化；这些内容仍需先完成分类验证，避免把仍有价值的业务改动误判为垃圾文件。

## 7. 本批执行后状态

| 指标 | 数量 |
| --- | ---: |
| 工作区总条目 | 1231 |
| 已修改 | 532 |
| 已删除 | 376 |
| 未跟踪 | 323 |

状态条目数量上升是预期结果：220 个此前被 Git 跟踪但不应纳入版本管理的 sqlite 文件，已经从工作区移出并表现为待删除项。后续提交清理批次后，这部分历史污染会从版本管理中消失。

本批新增或变更的治理项：

- 新增根级 `.gitattributes`；
- 新增本盘点文档；
- 将 `php.bat`、`png_to_svg.py`、220 个 tracked sqlite 标记为待删除；
- 将根目录 SQL、第三方源码副本、一次性工具和 sqlite 归档到 `C:\Users\Admin\Desktop\caiwu-local-artifacts\`。

## 8. 继续执行结果

执行时间：`2026-06-23 17:56` 之后。

已补充完成：

- 创建保护分支：`backup/workspace-cleanup-before-20260623-175641`。
- 创建仓库外补充备份：`C:\Users\Admin\Desktop\caiwu-workspace-backups\20260623-175641`，包含 diff、状态清单和未追踪文件压缩包。
- 执行 `git rm --cached -- backend/storage/framework/testing/*.sqlite`，停止 Git 追踪 220 个测试 SQLite；本地文件已由前置步骤归档到仓库外。
- 将 `frontend-user-v4-console/优化审核报告.md` 归位到 `文档/审查/frontend-user-v4-console-优化审核报告-2026-06-18.md`。
- 新增 `backend/scripts/workspace_health_check.mjs`，并在根 `package.json` 增加 `workspace:health`。
- 新增 `文档/开发文档/治理/工作区防复脏规则.md`，明确根目录、文档、生成物和 Agent 配置边界。

验证结果：

- `npm.cmd run workspace:health` 通过。
- 当前仅剩本机已忽略工具目录警告：`.runlogs/`、`.codegraph/`、`.reasonix/`。这些目录不进入 Git，不影响提交清洁度。
