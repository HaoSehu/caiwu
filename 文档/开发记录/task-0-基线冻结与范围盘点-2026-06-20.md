# Task 0 基线冻结与范围盘点

- 文档性质：执行记录
- 对齐时间：`2026-06-20`
- 来源：`git status --short` 与当前目录、文档、路由事实

## 1. 结论摘要

当前工作区处于高脏状态，改动覆盖后端、三个前端、共享层、文档、迁移、脚本、测试与运行产物。不能把当前整棵工作树直接视为“本轮待发布版本”，也不能在没有范围冻结的前提下做全仓联调修复。

本轮可以安全推进的内容应先限制在：

- 文档入口与现役目录真相修复
- 旧路径引用清理
- 可由现有测试直接验证的主链路缺陷

## 2. 当前改动分类

### 2.1 发布内可继续推进

- `文档/` 下与当前结构、执行方案、规范真相直接相关的文档
- 财务主链路直接相关文件：
  - `backend/app/Http/Controllers/Client/InvoiceController.php`
  - `backend/app/Http/Controllers/Client/PaymentController.php`
  - `backend/app/Http/Controllers/Client/RechargeController.php`
  - `backend/app/Services/Finance/*`
  - `backend/tests/Feature/*Invoice*`
  - `backend/tests/Feature/*Payment*`
  - `backend/tests/Feature/*Recharge*`
- 服务开通与控制台主链路直接相关文件：
  - `backend/app/Http/Controllers/Client/ServiceController.php`
  - `backend/app/Services/Provisioning/*`
  - `backend/app/Services/ClientServiceConsole/*`
  - `frontend-user-v4-console/src/pages/client/services/*`
  - `frontend-user-v4-console/src/pages/client/service-console/*`
  - `shared/statusConfig.js`

### 2.2 暂不发布或暂不在本批次处理

- 大量数据库迁移治理与结构工程文件
- 广泛的历史清理、归档、迁移脚本
- 未经验证的全量目录治理移动
- 与本轮修复目标无直接关系的性能优化、实验脚本和运行产物

### 2.3 异常删除或高风险变动，需谨慎观察

- `文档/README.md`
- `文档/目录说明.md`
- `backend/app/Http/Controllers/Client/OrderController.php`
- `frontend-user-v4-console/src/pages/client/orders/index.vue`
- 部分旧模型/资源类删除

这些删除不应在未经判断的情况下被自动恢复或彻底接受，必须结合当前路由、页面和业务迁移事实判断。

### 2.4 生成物与临时产物

- 根目录 SQL 文件
- `backend/storage/app/` 下各类回填、备份、归档输出
- `backend/public/uploads/*`
- 各类 `log`、`demo.html`、临时文本输出

## 3. 本轮执行边界

- 先完成 `Task 1` 文档真相修复。
- 然后仅进入“可测试、可回归”的财务与服务主链路批次。
- 任何扩展到数据库工程化、全仓目录治理、历史迁移补跑的动作，均不属于本轮默认执行范围。

## 4. 风险提醒

- 当前工作区改动面很大，任何“顺手整理”都会提高冲突概率。
- 文档仍存在旧目录名，会直接误导后续执行代理。
- 财务和服务主链路已经有较多在途改动，必须依赖定向测试选择修复点，不能凭肉眼猜。

## 5. 下一步

1. 修复文档入口和旧路径引用。
2. 跑第一批财务主链路定向测试。
3. 根据失败结果确定 `Task 2` 的首个代码修复点。
