# PHP 财务系统页面视觉规范

基于 2026-03-13 收到的 15 张参考图整理。该规范用于统一 `frontend-admin` 和 `frontend-client` 的视觉基线、页面结构和组件表达，后续新增页面默认按本文件执行。

## 一、参考图归类

本次参考图可以归纳为 3 套视觉来源，最终在项目中合并为一套统一设计语言：

1. **实名服务后台 / 服务控制台**
   - 代表页面：控制台、流量包、下载中心、邮件推送、短信服务、日志页、站点配置
   - 关键词：浅灰页面背景、白色内容卡片、蓝色主操作、左侧分组导航、表格和表单优先

2. **更现代的云控制台 / 用户中心**
   - 代表页面：云服务器实例、用户中心、安全中心
   - 关键词：更强的信息层级、图标卡片、模块分区、轻量阴影、局部彩色状态

3. **官网首页 / 登录页**
   - 代表页面：ENTUI 首页、云服务器官网首页、账号登录页
   - 关键词：高留白、黑蓝标题、浅色科技背景、双栏登录、官网 Hero 可配 3D 插画或产品示意图

## 二、整体风格定位

- **风格类型**：企业级 SaaS 控制台 + 云服务官网
- **主题方向**：亮色为主，不使用深色主后台作为默认风格
- **品牌气质**：可信、专业、干净、技术感，但不过度装饰
- **信息优先级**：数据、状态、操作优先于装饰；界面服务业务，不喧宾夺主
- **设计原则**：
  - 背景轻，内容卡片白
  - 强调色集中在品牌蓝
  - 彩色只用于状态、图标底和重点指标
  - 表格、表单、统计卡片都要简洁规整

## 三、视觉 Token

### 1. 颜色系统

| 类别 | 用途 | 建议色值 |
|---|---|---|
| 品牌主色 | 主按钮、激活态、链接、关键数字 | `#165DFF` |
| 主色 Hover | 主按钮 hover | `#0E4FCC` |
| 主色浅底 | 激活菜单、提示底色、图标底色 | `#E8F1FF` |
| 主色描边 | 选中态描边 | `#C9DBFF` |
| 成功色 | 成功状态、成功数字 | `#12B76A` |
| 成功浅底 | 成功标签背景 | `#EAFBF3` |
| 警告色 | 待处理、提醒 | `#F59E0B` |
| 警告浅底 | 警告标签背景 | `#FFF6E5` |
| 危险色 | 失败、拒绝、异常 | `#F04438` |
| 危险浅底 | 危险标签背景 | `#FFF1F0` |
| 紫色辅助 | 邮件/消息/附加业务模块 | `#8B5CF6` |
| 页面背景 | 全局底色 | `#F5F7FB` |
| 二级背景 | 区块、表头、轻提示区 | `#F8FAFC` |
| 卡片背景 | 卡片、弹窗、面板 | `#FFFFFF` |
| 边框 | 卡片边框、输入框边框 | `#E5EAF3` |
| 分割线 | 行分隔、模块分隔 | `#EEF2F7` |
| 主文本 | 标题、正文、指标数字 | `#1F2937` |
| 次文本 | 说明、表头、辅助描述 | `#5B6B82` |
| 占位文本 | placeholder、弱化文字 | `#94A0B2` |

### 2. 字体系统

- **主字体**：`Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`
- **标题字重**：`600` / `700`
- **正文与表格**：`400` / `500`
- **推荐字号**：
  - 页面标题：`28px`
  - 模块标题：`18px`
  - 卡片标题：`16px`
  - 正文：`14px`
  - 辅助文字：`12px`
  - 重点金额 / KPI：`28px` - `40px`

### 3. 圆角与阴影

- 页面大卡片：`16px`
- 常规卡片：`14px`
- 输入框 / 按钮 / 标签：`10px`
- 微型标签：`999px`
- 阴影基准：
  - 轻阴影：`0 2px 10px rgba(15, 23, 42, 0.04)`
  - 中阴影：`0 10px 30px rgba(15, 23, 42, 0.06)`
  - 浮层阴影：`0 20px 50px rgba(15, 23, 42, 0.10)`

## 四、布局规则

### 1. 控制台布局

- 左侧导航固定，宽度建议 `220px`，折叠后 `72px`
- 顶部栏高度建议 `64px`
- 主内容区左右留白 `20px` - `24px`
- 页面采用“背景层 + 白卡片内容层”的结构
- 左侧导航按业务分组显示，分组标题使用弱化灰蓝色小字

### 2. 内容排布

- 页头结构：页面标题 + 描述 + 右侧主操作
- 内容默认以卡片分区，不直接把内容散落在灰色背景上
- 卡片内部顺序建议：
  - 标题栏
  - 指标/筛选/操作
  - 表格、表单、图表或明细

### 3. 留白与间距

- 基础栅格：`8px`
- 区块间距：`16px` / `20px`
- 卡片内边距：`20px` - `24px`
- 大模块分区上下建议 `24px`

## 五、页面类型规范

### 1. 仪表盘 / 控制台

适用参考图：控制台、资源剩余、调用概览、邮件日志统计、用户中心概览

- 顶部允许使用 `3-6` 个概览卡片
- 概览卡片以白底为主，数字大、标签小、注释弱化
- 第二层可放资源进度、趋势图、结构概览、最近记录表格
- 图表面板和表格面板都必须放在独立卡片中
- 资源类数据优先使用横向进度条，不使用重视觉仪表盘

### 2. 列表页 / 日志页

适用参考图：邮件发送日志、实例列表、认证记录

- 结构固定为：标题区 -> 搜索筛选 -> 表格卡片
- 可在表格前放一排简短统计卡，但不要堆叠多层大卡片
- 表头背景使用浅灰色，不使用深色表头
- 状态统一使用浅底标签，不直接用高饱和纯色块

### 3. 配置页 / 表单页

适用参考图：网站信息配置、SMTP 接入配置

- 使用“多个白卡片分段”的组织方式
- 每个卡片顶部有模块标题，内部使用规则行表单
- 单行表单优先使用左右布局：左侧标签，右侧输入区域
- 说明文字放在输入项下方，不写成长段提示漂浮在页面中

### 4. 服务页 / 套餐页

适用参考图：流量包、短信服务、签名管理、下载中心

- 页头可放说明型提示条，底色为主色浅底
- 中间区域先展示概览统计，再展示 Tabs，再展示资源表格或套餐卡片
- 套餐卡片采用白卡 + 红色折扣角标 + 蓝色购买按钮
- Tabs 下的内容必须仍落在卡片中，避免内容散开

### 5. 用户中心

适用参考图：个人中心、安全中心

- 左侧是多级分组导航，右侧是个人概览和业务模块
- 顶部可放用户资料总览卡，包含头像、昵称、认证状态、快捷按钮
- 中间使用 2 列或 3 列卡片布局，右侧适合放安全评分、公告、快捷入口
- 表格和资源列表使用与后台一致的白卡片样式

### 6. 登录页

适用参考图：双栏登录页

- 背景为浅灰蓝渐变或柔和大面积留白
- 登录框采用居中的双栏容器
- 左栏展示品牌价值点，右栏放登录表单
- 左栏允许使用大圆形淡色背景、蓝色小图标、轻装饰块
- 登录表单必须简洁，输入框和主按钮保持规整对齐

### 7. 官网首页

适用参考图：ENTUI 首页、云服务器首页

- 官网允许更强视觉表达，但仍需保持白底、黑字、蓝色强调
- Hero 区可使用：
  - 左文案右插画
  - 黑色大标题 + 蓝色强调词
  - 双 CTA
  - Hero 下方悬浮 3-4 个卖点卡片
- 官网首屏可使用插画、产品模型或 3D 感资源图，但不要用廉价渐变大背景替代内容结构

## 六、组件规范

### 1. 按钮

- 主按钮：蓝底白字，`height: 36px - 40px`，圆角 `10px`
- 次按钮：白底描边，hover 变浅蓝边
- 危险按钮：仅用于删除、清空、禁用等操作

### 2. 卡片

- 白底
- 细边框
- 轻阴影
- 默认圆角 `14px`
- 卡片标题行与内容间用分割线或下边距拉开

### 3. 表格

- 表头底色 `#F8FAFC`
- 行高建议 `44px` - `48px`
- 单元格尽量左对齐，金额列可右对齐
- 状态列使用标签，不直接使用彩色文本

### 4. 表单

- 输入框高度建议 `40px`
- 边框为浅灰蓝
- focus 使用品牌蓝描边和轻微外发光
- 上传区域使用浅底框，不要使用过深虚线框

### 5. 标签 / 状态块

- 成功：浅绿底 + 绿色字
- 失败：浅红底 + 红色字
- 待处理：浅橙底 + 橙色字
- 信息：浅蓝底 + 蓝色字
- 附属业务：可使用浅紫底 + 紫色字

### 6. 图标

- 优先 `@element-plus/icons-vue`
- 图标放在 `32px - 40px` 的浅底色容器中最稳定
- 全站使用同一线性风格，不混用拟物图标

## 七、交互与动效

- hover 只做轻微颜色和阴影变化
- 页面切换动效轻量，`150ms - 220ms`
- 表格、卡片、按钮都不做明显弹跳或夸张位移
- 登录页和官网首页可以有柔和的背景渐变与装饰运动，但控制在弱存在感

## 八、禁止项

- 禁止把后台默认做成深色大屏风格
- 禁止满屏渐变、霓虹发光、玻璃拟态混搭
- 禁止在普通列表页使用官网式 Hero 横幅
- 禁止过多彩色统计卡同时出现
- 禁止正文大量使用浅灰导致可读性下降
- 禁止同一页面混用多套圆角、阴影和主色

## 九、代码落点

本规范默认同步到以下文件：

- `frontend-admin/src/assets/styles/variables.scss`
- `frontend-admin/src/assets/styles/global.scss`
- `frontend-admin/src/layout/AdminLayout.vue`
- `frontend-admin/src/views/admin/Login.vue`
- `frontend-client/src/assets/styles/variables.scss`
- `frontend-client/src/assets/styles/global.scss`
- `frontend-client/src/layout/ClientLayout.vue`
- `frontend-client/src/layout/WebsiteLayout.vue`

后续新增页面时，先复用现有 token、白卡片结构和组件状态，不要重新定义一套视觉体系。
