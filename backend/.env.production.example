# 生产打包专用环境文件模板
#
# 用法：
#   1. 复制本文件为 backend/.env.production（已在 .gitignore 中忽略，不会入库）
#   2. 填入真实生产公开地址
#   3. 打生产包：
#        npm run build:frontends:prod
#      或分别构建：
#        node backend/scripts/build_frontends.mjs --env production --target=admin
#
# 注意：本文件只用于前端构建期读取公开地址，不参与 backend 运行时（运行时仍用 backend/.env）。
# 构建脚本不会回退到本地 backend/.env 的生产地址，缺项会直接报错，防止打出指向 127.0.0.1 的错误产物。
# 四个公开地址必须互不相同，且统一使用 HTTP 或 HTTPS。

# API、媒体、上传、VNC WebSocket 入口
APP_URL=https://api.你的域名

# 官网与用户入口
FRONTEND_URL=https://www.你的域名

# 用户控制台
CLIENT_CONSOLE_URL=https://console.你的域名

# 管理端
ADMIN_URL=https://admin.你的域名

# 官网和控制台需要跨子域共享登录态时填 .你的域名；不需要则留空
CLIENT_SESSION_COOKIE_DOMAIN=.你的域名
