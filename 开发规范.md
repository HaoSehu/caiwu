# 开发规范

## 适用范围

本规范面向当前真实代码目录：

- `backend`
- `frontend-admin-v3`
- `frontend-user-v3-www`
- `frontend-user-v4-console`
- `shared`

旧路径 `frontend-admin`、`frontend-client`、`frontend-user-v3-console` 当前不在仓库中，新增代码和命令不要引用这些目录。

## 通用原则

- 先复用现有模块、服务、样式入口和共享配置，再新增能力。
- 业务代码按领域聚合，不按临时需求散放。
- 页面模板不堆大段请求拼装、状态映射和业务判断。
- 状态展示优先复用 `@caiwu/shared`、`shared/statusConfig.js`、`shared/extraStatusMaps.js`、`shared/components/StatusTag.vue`、`shared/user-v3/components/StatusTag.vue`。
- 用户可见文案使用简体中文；第三方原始错误不直接展示给用户。
- 本地地址统一 `127.0.0.1`。

## 后端

- Laravel 12，PHP 8.2+，Sanctum Token 鉴权。
- 管理端和用户端都走 Bearer Token；权限码走 `permission:{code}` 中间件。
- 控制器保持薄层：接收参数、鉴权、调用 Service、返回 Resource/统一响应。
- 复杂查询、写操作、批量操作、财务/支付/上游相关接口使用 `FormRequest`。
- 响应统一通过 `App\Traits\ApiResponse` / `App\Support\ApiResponseBuilder`；成功 `code = 0`。
- 分页结构固定为 `list`、`total`、`page`、`page_size`。
- 业务逻辑放 `app/Services/<领域>/`，常量放 `app/Constants`，响应字段优先用 `app/Http/Resources/<领域>/`。
- 上游、支付、短信、实名、存储等第三方调用必须走专用 Service/Driver，不在 Controller 里直接 `Http::*`。
- 支付、账单、订单、余额、返佣必须考虑事务、幂等、审计字段和反向流水；`payments` 表禁止物理删除。
- 回调接口必须走签名中间件，业务处理必须幂等并落日志。

## 前端通用

- Vue 3 `script setup` + Composition API。
- 请求统一走各端 `src/api/*` 和现有 request 工具，不直接新建 axios 实例。
- 认证和 session 存取走现有 runtime/auth 工具，不直接操作 token localStorage 键。
- 路由、store、runtime 逻辑放各端现有 `src/app/`、`src/router/`、`src/store/` 体系。
- 新增页面要有加载、空态、错误态和提交 loading/禁用态。
- 不引入与当前端重复的 UI 库、状态库或请求层。

## 管理端 `frontend-admin-v3`

- 技术栈：Vue 3 + Vite + TypeScript + TDesign Vue Next + TDesign Icons Vue Next + Pinia + Less。
- 页面放 `src/pages/`，路由放 `src/router/modules/`，API 放 `src/api/`，样式入口在 `src/style/`。
- 图标只用 `tdesign-icons-vue-next`，禁止混入 Element Plus。
- 沿用 TDesign Starter 管理端布局、浅色控制台、TDesign token 和 Less 变量。
- 管理端普通列表页不要新增说明型 Hero/页头大卡片；从筛选、指标、工具栏或表格卡片开始。
- 权限控制走 `src/permission.ts` 与权限 store，权限码和后端 `AdminPermissions` 对齐。

## 官网与用户入口 `frontend-user-v3-www`

- 技术栈：Vue 3 + Vite + Element Plus + Pinia + Sass。
- 官网、登录注册和用户入口可以更有视觉表现，但仍复用 `src/assets/styles/variables.scss`、`global.scss` 与现有布局。
- 购买、结算、优惠券、恢复下单等流程优先复用 `src/domains/products/*` 和既有 composables/API。
- SEO、sitemap、prerender 逻辑在本项目脚本内维护，不在页面里散落拼装。

## 用户控制台 `frontend-user-v4-console`

- 技术栈：Vue 3 + Vite + TypeScript + TDesign Vue Next + TDesign Icons Vue Next + Pinia + Less。
- 页面放 `src/pages/client/`，领域逻辑放 `src/domains/`，通用组合逻辑放 `src/composables/`。
- 复用 `shared/user-v3` 的控制台组件和 `@caiwu/shared` runtime/status 能力。
- 控制台是高频业务界面，优先信息密度、稳定布局和清晰操作，不做官网式装饰页面。
- 禁止混入 Element Plus 组件、样式和图标。

## 验证

- 改 `backend`：`cd backend && php artisan test`，必要时先跑受影响测试。
- 改 `frontend-admin-v3`：`cd frontend-admin-v3 && npm run build`。
- 改 `frontend-user-v3-www`：`cd frontend-user-v3-www && npm run build`；重构时追加 `npm run verify:refactor`。
- 改 `frontend-user-v4-console`：`cd frontend-user-v4-console && npm run build`；重构时追加 `npm run verify:refactor`。
- 改 `shared`：`npm run typecheck:shared && npm run test:shared`，并按影响端追加对应前端 build。
- 只改文档：自检目录、命令、代码事实是否一致。

### 后端工具链调用约定（Windows + PowerShell）

`vendor/bin/*` 在 Windows 下是 `.bat` 包装，PowerShell 不允许在管道中直接执行，会报"无法在管道中间运行文档"。统一用 `php vendor\bin\<tool>` 调用：

- 静态分析：`php vendor\bin\phpstan analyse --memory-limit=1G`（已生成 `phpstan-baseline.neon` 冻结存量，只报新增）
- 代码风格检查：`php vendor\bin\pint --test`
- 代码风格格式化：`php vendor\bin\pint`（仅格式化本次修改的文件，禁止全局大面积重排，见 §禁止项）
- 依赖安全：`composer audit`
- 全量测试需 `memory_limit=512M`（已配进 `phpunit.xml` `<ini name="memory_limit" value="512M"/>`，无需手动传参）

测试库为独立 `idc_testing`（见 `phpunit.xml` / `.env.testing`），生产库 `idc` 不被测试触碰。`tests/TestCase.php` 会按需把宝塔 `mysql.exe` 路径加入测试进程 PATH。
