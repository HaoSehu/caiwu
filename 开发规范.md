# PHP 财务系统开发规范

## 认证方式

### 后端认证

本项目使用 **Laravel Sanctum Token 认证**：

1. **登录流程**
   - 用户/管理员通过登录接口提交凭证
   - 后端验证成功后生成 Sanctum token：`$user->createToken('token-name')->plainTextToken`
   - 返回 token 给前端

2. **API 路由保护**
   - 使用 `auth:sanctum` 中间件保护需要认证的路由
   ```php
   Route::middleware(['auth:sanctum'])->group(function () {
       // 受保护的路由
   });
   ```

3. **配置要点**
   - `config/auth.php` - 不需要自定义 guard
   - `config/sanctum.php` - 配置 stateful domains
   - `.env` - 设置 `SANCTUM_STATEFUL_DOMAINS=127.0.0.1:5173`

### 前端认证

1. **Token 存储**
   - 管理员 token: `localStorage.setItem('admin_token', token)`
   - 客户 token: `localStorage.setItem('client_token', token)`
   - 用户类型: `localStorage.setItem('userType', 'admin'|'client')`

2. **请求携带 Token**
   ```javascript
   // 在 axios 请求拦截器中添加
   config.headers.Authorization = `Bearer ${token}`
   ```

3. **工具函数**
   - 使用 `src/utils/auth.js` 中的 `getToken()`, `setToken()`, `removeToken()`
   - 不要直接操作 localStorage

## 网络配置

### 统一使用 127.0.0.1

项目中所有 localhost 统一改为 127.0.0.1：

1. **后端配置**
   - `.env`: `APP_URL=http://127.0.0.1:8000`
   - `.env`: `SANCTUM_STATEFUL_DOMAINS=127.0.0.1:5173`
   - `config/cors.php`: `'allowed_origins' => ['http://127.0.0.1:5173']`

2. **前端配置**
   - `vite.config.js`: proxy target 使用 `http://127.0.0.1:8000`

## 启动命令

### 后端启动

**标准启动**：
```bash
php artisan app:serve
```

**开发启动（自动关闭旧进程）**：
```bash
taskkill /F /IM php.exe & php artisan app:serve
```

### 前端启动

```bash
npm run dev
```

## 页面实现规则

### 统一基线

1. 自 2026-03-13 图片参考集起，后台与用户中心统一采用 **亮色企业控制台风格**，不再以深色后台作为默认基线。
2. 默认页面背景使用浅灰蓝，内容承载使用白卡片，主品牌色统一为蓝色系。
3. 新页面优先复用 `页面风格.md` 中的 token、卡片结构、状态色和页面模板，不要自行再定义一套视觉规范。

### 后台与服务页

1. 后台管理页面默认使用标准业务布局：左侧导航、顶部栏、页头、内容卡片。
2. 列表页 / 日志页默认结构为：标题区、筛选区、表格卡片、分页；不要加入首页式 Hero。
3. 配置页 / 表单页必须拆成多个白色分区卡片，每个卡片有清晰标题和说明。
4. 服务页允许使用提示条、统计卡、Tabs、资源表格、套餐卡片，但视觉仍需克制。

### 仪表盘与用户中心

1. 仪表盘允许使用概览卡、资源进度、趋势图、最近记录表格。
2. 用户中心允许使用资料总览卡、安全中心、快捷入口和资源表格。
3. 统计卡允许出现，但必须是白底低饱和样式，禁止高饱和彩虹卡片铺满全页。

### 官网与登录页

1. 官网首页允许使用 Hero、插画、卖点卡片和双 CTA，但必须保持白底、黑字、蓝色强调的企业科技感。
2. 登录页默认采用居中双栏结构：左侧品牌价值点，右侧登录表单。
3. 官网和登录页可以更有视觉表现，但禁止把这种表现方式带进普通后台列表页。

### 组件细则

1. 按钮默认使用 `36px - 40px` 高度，主按钮蓝底白字，次按钮白底描边。
2. 表格表头使用浅灰底，状态统一使用浅底标签，不直接用高饱和纯色文本。
3. 输入框、下拉框、上传框统一使用浅边框和蓝色 focus 态。
4. 图标统一优先使用 `@element-plus/icons-vue`，不使用 emoji 代替功能图标。

## 常见问题

### 1. 认证失败 401
- 检查 token 是否正确保存和发送
- 检查路由中间件是否使用 `auth:sanctum`
- 检查 CORS 和 Sanctum 配置

### 2. 端口占用
- 使用开发启动命令自动关闭旧进程
- 或手动执行：`taskkill /F /IM php.exe`

### 3. CORS 错误
- 确认 `config/cors.php` 中 allowed_origins 包含前端地址
- 确认使用 127.0.0.1 而非 localhost

## 数据库初始化与迁移（禁止ai操作，请手动执行以下命令）

cd C:\Users\USER125536\Desktop\caiwu

# 步骤1：初始化数据库
python backend/scripts/install_db.py

# 步骤2：迁移检查（dry-run）
python backend/scripts/migrate_legacy_dump.py --dump "idc_2026-05-26_19-16-24_mysql_data_ulZAP.sql" --dry-run

# 步骤3：正式迁移
python backend/scripts/migrate_legacy_dump.py --dump "idc_2026-05-26_19-16-24_mysql_data_ulZAP.sql"

# 步骤4：清理缓存
cd backend && php artisan cache:clear




python backend/scripts/install_db.py
python backend/scripts/migrate_legacy_dump.py --dump "idc_2026-05-26_19-16-24_mysql_data_ulZAP.sql" --dry-run
python backend/scripts/migrate_legacy_dump.py --dump "idc_2026-05-26_19-16-24_mysql_data_ulZAP.sql"
cd backend && php artisan cache:clear
