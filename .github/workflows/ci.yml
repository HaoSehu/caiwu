name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: 后端 (Laravel)
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: idc_test
          MYSQL_ROOT_PASSWORD: root
        ports:
          # 避开 runner 自带 MySQL 占用的 3306
          - 13306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    defaults:
      run:
        working-directory: backend
    env:
      APP_KEY: base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
      DB_CONNECTION: mysql
      DB_HOST: 127.0.0.1
      DB_PORT: 13306
      DB_DATABASE: idc_test
      DB_USERNAME: root
      DB_PASSWORD: root
    steps:
      - uses: actions/checkout@v7

      - name: 安装 PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo_mysql, mbstring, openssl, redis
          coverage: none
          tools: composer:v2

      - name: 生成 APP key
        run: echo "APP_KEY=base64:$(openssl rand -base64 32)" >> $GITHUB_ENV

      - name: 校验 composer.json
        run: composer validate --strict --no-check-publish

      - name: 缓存 Composer 依赖
        uses: actions/cache@v5
        with:
          path: backend/vendor
          key: composer-${{ runner.os }}-${{ hashFiles('backend/composer.lock') }}
          restore-keys: |
            composer-${{ runner.os }}-

      - name: 安装依赖
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: 安装 mysql 客户端
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends mysql-client

      # 项目迁移为增量，完整结构以 database/schema/mysql-schema.sql 为基线；
      # 全新测试库须按 install_db.py 同款流程：导入基线 + 跑增量迁移
      - name: 初始化测试数据库
        run: |
          mysql --default-character-set=utf8mb4 -h 127.0.0.1 -P 13306 -uroot -proot idc_test < database/schema/mysql-schema.sql
          php artisan migrate --force

      - name: 代码风格检查 (Pint)
        run: composer format:check

      - name: 静态分析 (PHPStan)
        run: composer analyse

      - name: 运行测试
        run: composer test

  frontend-admin:
    name: 前端-管理端 (admin-v3)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: 安装 Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: 安装依赖
        run: npm ci

      - name: Lint 检查
        run: npm run lint:admin-v3

      - name: 类型检查
        run: npm run typecheck:admin-v3

      # admin 的 test 为 Playwright e2e（需浏览器与运行中的后端），常规 CI 只跑非 e2e 单测与 shared
      - name: 单元测试（shared + admin 非 e2e）
        run: |
          npm run test:shared
          npm run test:api-asset --workspace chuangouyun-admin-v3
          npm run test:log-archive-settings --workspace chuangouyun-admin-v3

      - name: 构建
        run: npm run build:admin-v3

  frontend-www:
    name: 前端-官网 (user-v3-www)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: 安装 Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: 安装依赖
        run: npm ci

      - name: Lint 检查
        run: npm run lint:user-v3-www

      - name: 类型检查
        run: npm run typecheck:user-v3-www

      - name: 单元测试
        run: npm run test --workspace chuangouyun-user-v3-www

      - name: 构建
        run: npm run build:user-v3-www

  frontend-console:
    name: 前端-用户控制台 (user-v4-console)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: 安装 Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: 安装依赖
        run: npm ci

      - name: Lint 检查
        run: npm run lint:user-v4-console

      - name: 类型检查
        run: npm run typecheck:user-v4-console

      - name: 单元测试
        run: npm run test --workspace chuangouyun-user-v4-console

      - name: 构建
        run: npm run build:user-v4-console

  docs:
    name: 文档检查
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: 安装 Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      # check_docs.mjs 仅使用 Node 内置模块，无需 npm ci
      - name: 文档检查
        run: npm run docs:check
